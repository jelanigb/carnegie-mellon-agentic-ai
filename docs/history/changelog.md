# Changelog

**Chronological record for the plan of record — [`implementation_plan.md`](../implementation_plan.md).**
Section numbers (§1–§9) and decision numbers (#1–#20) anywhere in this repository refer
to that file: §-numbers to its sections, #-numbers to the **decisions register in §7**,
which names every decision and links to its full reasoning in
[`decision_log.md`](decision_log.md). A
[document map](../implementation_plan.md#document-map) there lists every document in this
project and when to read it.

**Chronological record of code changes.**
Author: Jelani Gould-Bailey · Last updated: Aug 30, 2026

## Why this file exists

The build is sequenced by dependency and technical risk rather than by the syllabus
calendar — see `implementation_plan.md` §6 for that decision and its rationale. Units
therefore land out of order relative to the order the program teaches and assesses the
material: retrieval (U4) shipped before the walking skeleton (U2), and code feeding
Checkpoint 6.1 exists before the checkpoints between them are due.

That trade is worth making, but it costs traceability: with unit order decoupled from
checkpoint order, nothing otherwise maps shipped code back to the requirement it
satisfies. This file is that map.

## What belongs here

**Code changes, not decisions.** Decisions live in `implementation_plan.md` — the §7
decisions log for open questions, and the per-unit sections for reasoning and findings.
A decision that has not produced code is not a changelog entry. Where a change needs
justification, the row cites the plan section rather than restating the argument.

**One conceptual change per row, however many files it touched.** A change spanning an
agent, a state field, and a config entry is one row naming all three; it is not three
rows. The unit of a row is the change, not the file.

## Conventions

- **A row lands with its commit, not at unit close** (changed Aug 26, 2026 — reasoning in
  [`../design/engineering_standards.md`](../design/engineering_standards.md) §8). A unit is
  six to eight change sets now, and a log written at the end is a reconstruction from
  `git log`, which is the work this file exists to prevent.
- `##` heading per date the work was **done**. Newest date first.
- **Date added** is when the row was written, which is the same day for work logged as
  it lands and a later date for anything backfilled. Rows are ordered newest-first by
  that date, so retroactive entries are visibly retroactive rather than quietly folded
  into the original day's record.
- **Related checkpoint** names every checkpoint a change feeds, or `maintenance` for
  work that serves none — hygiene and defect repair are real work, and forcing a
  checkpoint onto them would make the column less trustworthy everywhere else.
- Work predating the unit numbering is labelled by the `implementation_plan.md` section
  that specifies it (`§2`, `§9`), so it stays findable by the same identifier the plan
  uses.

---

## Sept 2, 2026 — U9.M (M8): the outlook states the error band it compounds from

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.M (M8) | **Every scenario row compounds one modelled rent, and the section now says how far that rent could be off.** `agents/summarizer._projection_rent_error`, rendered beside *"Projected from…"*. Until now the five-year outlook compounded `projection_base_rent` as though it were exact: on `staten-island` that figure carries a metro holdout error of **±$855/mo — 32% of the estimate — against a five-year band spread of 43%**, so the three rows differed from each other by *less* than the error bar on the number all three start from, and a reader had no way to know it. `los-angeles` reads 18% and `overpriced` 16%. Prefers `subject_metro_mae_dollars` over the pooled `model_mae_dollars`, the same order and for the same reason as the Findings table one section above, and renders nothing rather than a hedge when neither is on state. **Language, not calculation** — nothing about which scenarios are selected or what they project changes, which is why **all 30 eval rows come back byte-identical** and no re-record was needed. Raised the same day by the U9 spike on OQ-22's starting-point treatment, which proposed *projecting* from the band and was **not** adopted (the evaluator held its choice on 5 of 8 repeats); stating the band is the half that survives that finding, and it stands whichever way OQ-22 was decided. Closed ahead of U9.11 so the unit does not close with a `TODO` aimed at a subsection already marked done. All three sample reports re-derived | 7.1 |

---

## Sept 2, 2026 — U9.8: gross rent multiplier

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.8 | **The report states the one investor ratio this project's data supports, and names the line it stops at.** `ValuationDetail` gains `gross_rent_multiplier`, its denominator `grm_annual_gross_rent`, the same multiple at the local benchmark, and an unavailability reason; `_attach_gross_rent_multiplier` computes them and a *Price against rent* block renders them. Built on the **modelled** rent, not the listing's stated rents, so the multiple is available on a listing that states none and describes the rent this system will defend. `los-angeles` reads **15.3×**, `staten-island` **9.2×** against its ZIP's **11.0×** — which inverts the impression the escalation banner gives, since the escalated deal is the cheaper one per dollar of rent. **Cap rate is refused rather than approximated**: it needs NOI, which needs operating expenses this system does not model, and the report says so in place of the figure rather than omitting the subject. No flag, no threshold, and no `config` entry — what counts as a high multiple depends on an investor's cost of capital and hold period, neither of which this system knows | 7.1 |
| Sept 2, 2026 | U9.8 | **The benchmark multiple is disclosed as a restatement, not as corroboration.** Both multiples divide by the same rent, so their ratio is *exactly* `price / benchmark_median_sale_price` — the premium the report already states, in a different unit. Measured before building rather than noticed after: 15.28 / 15.26 = 1.0010 = 1,049,000 / 1,047,955. It ships anyway because the unit is what it buys — an investor who thinks in multiples reads the market without converting — but the text says the two are one comparison rather than two agreeing measurements, since presenting them as independent would double-count a single fact. Prompt-inert by construction: neither `summarizer._lede_prompt` nor `scenario_forecast._context_block` reads a `ValuationDetail` field, so **no re-record**, and all 30 eval rows come back byte-identical | 7.1 |

---

## Sept 2, 2026 — U9.7T: the scenario table

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.7Tg | **Two documents lose a premise that expired and gain a measurement.** `design/evaluator.md` and OQ-22 both argued the pairing level must survive because the forecast's search is *"the only reasoning locus in the build"* — which stopped being true on Sept 1 when U9.4 landed `critic.cross_check`. The claim was load-bearing: it is why OQ-22 defers depth 2's redesign rather than deleting the level. Both now record that the cost no longer exists and that the level will be judged on whether it earns its evidence. Both also gain U9.7T's measurement — depth 1 decided by the model on 78 of 78 recorded levels, depth 2 on 39 of 79 — and `evaluator.md`'s **Defect 2** records that U9.3 fixed half of it (`_band_tables` alone, leaving the combined table using the internal band names beside a label that used the same three words for a third thing) and what finished it. The half that is not about naming is now disclosed rather than fixed: nothing guarantees every band reaches a row, and forcing coverage would override the evaluator to satisfy a layout | 7.1 |
| Sept 2, 2026 | U9.7Ta | **A scenario row is named for what it says, not for its rank.** `Scenario.name` held `pessimistic` / `base` / `optimistic`, assigned from the survivors' ordering — while the parenthetical beside each figure used *the same three words* for the band one series drew from. So the row labelled "Optimistic" routinely carried the base rent band, and the report explained the collision in a paragraph. New `scenario_forecast._row_name` is a deterministic 3×3 lookup on `(rent_band, price_band)` — "Central case", "Prices fall, rents hold" — with the departing side named first and the central case defined as *no side departs*, which keeps it true on a one-sided deal and wherever the sort puts it. Cells now carry `summarizer._band_words`' plain words, the same ones `_band_tables` heads its columns with, so one vocabulary serves both tables. **Ordering is untouched**: worst-to-best by combined outcome, kept deliberately by the architect over neutral-first, with the cost stated in the report — the central case need not be in the middle. `_labels_for` retires; no prompt, payload or band moves, and all 30 eval rows are byte-identical | 7.1 |
| Sept 2, 2026 | U9.7Tb | **Every reported row now says how it got into the report.** The ledger has always said why each *discarded* hypothesis lost and nothing said why a survivor was kept, so a reader met three rows and had to assume they had outscored the field — which on 51% of recorded depth-2 levels they had not. `tot.SearchResult.selection_basis_by_id` records `outright`, `tie:<group size>` or `reserved` per survivor; `Scenario.selection_basis` carries it; the scenario table gains a **"Why this row is shown"** column pairing the evaluator's score with the mechanism. The score moves out of the bullets and into that column, where the two are read together. On `los-angeles` the central case reads *0.96 — outscored the pairings left out* while the other two read *0.85 — level with 3 other pairings, kept as the more cautious*; on `staten-island` the central case reads *0.70 — the neutral case, always shown*, which is U9.3's reserved beam slot becoming visible in a report for the first time | 6.1, 7.1 |
| Sept 2, 2026 | U9.7Td | **The report says which measured bands reached no row.** Three scenarios read as spanning the range and on `los-angeles` they do not: the beam kept base/base, base/pessimistic and pessimistic/base, so **neither series' strongest stretch appears in any row** while both are printed in the table above it. A reader taking the bottom row as the upside case was reading a figure the search never claimed was one. Stated as an absence rather than fixed by widening the beam, which would override the evaluation to satisfy a layout. Silent when the rows do cover every band, so the line means something when it appears | 7.1 |
| Sept 2, 2026 | U9.7Te | **A disclosure stops contradicting itself about its own number.** `forecast_branches_near_tied` printed *"separated by 0.050, inside the 0.05 threshold"* — the evaluator returns two-place scores, so a gap of exactly one epsilon arrives as 0.04999999999999993 and three decimal places rendered it as the threshold it was inside of. Now states the bound and drops the figure; the negative-margin branch keeps its figure at two places, where it is exact rather than a rounded artifact. **Wording only — the comparison is untouched.** Whether `within 0.05` should be inclusive is a separate live question: it would widen `tot._rank`'s tie groups and flip 11 recorded depth-2 levels, needing a re-record, and the architect deferred it Sept 2 | 7.1 |
| Sept 2, 2026 | U9.7Tf | **Re-derived and checked against the two change sets.** All 30 eval rows byte-identical — verdicts, confidences, disclosure counts and comp counts all unmoved — which is the check this pass could make, because nothing in U9.7T touches a prompt, a payload or a band. **No re-record.** Both committed sample reports refreshed: 30 lines move on `los-angeles` and 26 on `staten-island`, every one of them traceable to Ta, Tb, Td or Te | 7.1 |
| Sept 2, 2026 | U9.7Tc | **The branch ledger stops attributing to the evaluator a decision the tie-break made.** A candidate cut from the same tie group as the last one kept recorded *"Scored 0.80, outside the top 3 at this level"* — word for word what a genuinely outscored candidate gets — when what decided it was `tot._rank`'s conservatism preference. Measured across the committed recordings by reproducing that grouping: **depth 1 is decided by the model's scores on 78 of 78 levels; depth 2 on 39 of 79, so 51% are tie-break decisions.** `_rank` splits into `_rank_groups`, which keeps the grouping the flatten was discarding, and `beam_search` gains a third prune reason saying the candidate scored level with the last one kept and lost on the stated preference rather than on the score. Collected before any reservation, like `cut_boundary_gap_by_depth` and for the same reason — a displacement is a different fact and keeps its own wording. This is `tools/tot.py`'s own *"pruning that leaves no trace"* failure surviving one layer up. Report text only: no prompt, no payload, no verdict moves, and all 30 eval rows are byte-identical | 6.1, 7.1 |
| Sept 2, 2026 | U9.7Ta (defect) | **`scripts/forecast_evidence.py` had been comparing against the wrong row, and the rename surfaced it.** Its base-case delta found the scenario whose *name* was `base` — but U9.3 reserved a beam slot for the neutral pairing, after which the row labelled base was routinely not base/base at all (on `los-angeles` it was pessimistic-rent with base-price). Now matched on the bands, which is what "the base case" meant all along. Found because content names break the string match loudly rather than because anything failed | 7.1, maintenance |

---

## Sept 2, 2026 — report language, on the architect's read of the three samples

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.4 | **The system line names the consequence instead of the instrument.** *"System check — escalated to human review"* made a reader work out what a system check is and what escalating one implies for them. It now reads **"Flagged by system — needs human review before sharing with investors"**, which is also what the escalation means operationally under either routing rule in `design/personas.md`: a deal-substance flag waits on the agent before the deal reaches the investor, an infrastructure flag waits on IT, and neither should be forwarded first. The cleared branch is reworded to mirror it — *"Cleared by system — no human review needed before sharing"* — so the two read as one question answered two ways rather than as unrelated banners. The axis-separation sentence is untouched | 7.1 |
| Sept 2, 2026 | U9.7T | **"Central case" becomes "Neutral case", and the scenario table's columns are reordered.** *Central* invited "central to what?" — it reads as a claim about a distribution this forecast never estimates, since the bands are observed twelve-month stretches rather than quantiles — and it disagreed with the vocabulary one column over, where the reserved-slot mechanism has always called that row *the neutral case*. Columns now pair each quantity's rate with its own projected level (`Scenario · Rent growth · Rent in year 5 · Price growth · Price in year 5 · Why this row is shown`); previously the two columns a reader most often compares sat furthest apart. "yr" spelled out | 7.1 |
| Sept 2, 2026 | U9.7c | **The demo surface colours the report's structural headings, and the colour lives in the surface rather than in the report.** `#188038` on the report title and on section headings, sub-headings left alone so the colour marks structure rather than everything. **Three selectors, because a `##` heading is not an `<h2>` here** — `_render_report` turns each into an expander label, so styling `h2` alone would colour nothing. **Not applied at the source deliberately:** the report is Markdown committed to a public repository and GitHub strips `style` attributes, so a heading coloured in the report would render green here, plain on GitHub, and as raw HTML in a text editor. Same reasoning `split_report` rests on — the report is the artifact, the app is a lens on it | 7.1 |
| Sept 2, 2026 | U9.7T | **OQ-22 closed on the third of the three conditions it named for itself, and OQ-23 opened.** The entry's own closing conditions were: build the re-purposing, delete the level, **or judge a wording-only fix sufficient and drop the redesign on the record**. The third is what happened, and the entry predicted the ordering — the parallel spike's ~2x finding argued for trying the smaller step first and said the redesign should be decided after the wording change, not before it. The closure record in `design/evaluator.md` carries the three findings a deleted entry cannot hold: a treatment worth ~2x the growth bands cannot be an LLM judgment on this fleet (5 of 8 repeats on the deal eight comps corroborate to 0.5%); the same decision as a deterministic rule agrees with the modal answer, never varies, and needs no re-record; and the treatment must not join depth 1. The weakness itself is unchanged and the final report should say so. OQ-23 is new and is the architect's: the three sample reports run 183–213 lines and every unit since U9.3 has added to the middle without removing anything. **Deliberately not acted on** — almost everything a length pass would cut is a disclosure, and a shorter report bought that way is the failure §1 exists to prevent. All three sample reports re-derived; 30 eval rows byte-identical | 7.1 |

---

## Sept 2, 2026 — U9.9 prerequisites

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.9 | **The demo surface says whether it is being traced, which it could not before.** `app.py` called `configure_tracing` nowhere, so `LANGSMITH_TRACING=true` in the launching shell would still have traced the session — into the project named `default`, with the key resolved only if it happened to be exported rather than sitting in its file, and with nothing on screen saying which. That is the exact case `tools/tracing.py`'s docstring calls worse than no trace: one you believed was captured and was not, discovered after the capture. Now `@st.cache_resource`-wrapped so the status renders once per session rather than on every Streamlit rerun, and stated in the sidebar **either way** — the absence of a trace is the thing worth knowing during a capture. Both states verified through `AppTest` | 5.1, 7.1 |
| Sept 2, 2026 | U9.9 | **The graph diagram regenerated and the topology check passed — byte-identical output, so nothing had drifted.** `scripts/export_graph_diagram.py` asserts decision #9's three properties: exactly one loop-closing edge (`critic → planner`), exactly two branching nodes (`critic`, `planner`), and every node in `nodes.ALL_NODES` reachable. Recorded as a row rather than left silent because "the diagram is unchanged" is a finding about the topology, not an absence of work | 5.1, 7.1 |

---

## Sept 2, 2026 — a third sample report

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.8 | **`docs/sample_reports/overpriced.md` added, and the set now crosses the two axes rather than sampling one.** The two committed reports were *clean + Proceed* and *escalated + Proceed*, which varies axis 1 and holds axis 2 fixed — so nothing showed the recommendation doing work of its own. `overpriced` is the mirror of `staten-island`: the system reports at confidence 1.00 and the **deal** is the problem, asking 55% over its ZIP's recorded sales. It is also the only demo deal whose model/rule cross-check disagrees, so the second reasoning locus renders in full for a reader rather than being described. Its gross multiple states the premium in the investor's own unit — **26.0× against the ZIP's 16.8×**. README gains a table saying which report shows what and why; `architecture.md`'s tree count follows. The two existing reports were re-derived in the same pass and came back **byte-identical apart from the run timestamp**, which is the reproducibility claim holding rather than being asserted | 7.1 |

---

## Sept 2, 2026 — U9.10: three open questions written up

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.10 | **OQ-5 written up and left open on an unmet condition, with a third measurement that changes what the question is.** The entry treated four ToT constants as one question; U9.7T's grouping measurement separates them. Depth 1 is decided by the evaluator on **78 of 78** recorded levels — its constants do what they were set to do — while at depth 2 `TOT_TIE_EPSILON` is load-bearing on **51%**, so on half of all levels it is this project's conservatism preference rather than the model that chooses which pairings are reported. That makes a *tuning* question about the epsilon really a design question about how often the search should defer to policy. The inclusive-versus-strict boundary was priced (26 of 57 nominal-0.05 pairs land "tied"; making it inclusive moves 11 recorded levels and needs a re-record) and declined by the architect. Closing condition unchanged and unmet: no fixture in this project has a known-correct branch, because every one was authored by a pass that already knew the shipped values | 7.1 |
| Sept 2, 2026 | U9.10 | **OQ-10 and OQ-14 close.** OQ-10 keeps the on-disk credential fallback — `ignore/` is gitignored, and requiring env vars would only make a fresh clone harder to run, which is what 7.1 grades — and records that the entry stayed open because it conflated two questions with different answers: whether a key may sit on disk, and whether an account identifier may print into a recording. The second had no defensible yes and was fixed at U9.M. OQ-14 closes as **discharged**: 5.1 asked for design rationale rather than measurable completion criteria, so U4's acceptance-table treatment does not apply, and 6.1's half closed at U7.8. The generalizable practice is kept for the register | 5.1, 7.1 |

---

## Sept 2, 2026 — U9.M: the two things that would have contaminated a recording

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 2, 2026 | U9.M | **Five maintenance items closed, and one of them was a method rather than an edit.** **M7** — `architecture.md`'s repository tree **re-derived against `find src -name '*.py'`**, which the entry named as the only fix that stops it drifting again. It had listed `eval/run_eval.py` and `eval/expected.yaml`, *neither of which was ever built*, marked the Critic and Summarizer unfinished after both shipped, and described `tests/` as one file of 24 cases against seven files and 107. The `✅`/`◐` markers went with it — they encoded a moment in the build and were never updated as it passed. **M4** — §6's U8 row now names U8.9's drop and points at U9.9. **M5** — §6's unit table gains its missing U11 row. **M6** — `comps_retrieval`'s ladder docstring stops claiming floor area is the weakest signal; the shipped model measures it at 0.502 against bedrooms' 0.300, the cause is that the ladder predates the model by a unit, and both cautions against over-reading that are carried. The reorder stays out of scope: it moves comp sets across all 30 rows | maintenance |
| Sept 2, 2026 | U9.M | **M2, and four sites its own scan had not covered.** `main.py --help` said *"the U4 ablation"* — the one reader-facing site the §8 vocabulary scan named — and now says what the switch does and what the before/after is evidence of. The four others are the same defect pointed the other way, a docstring asserting what measurement has retired: `state.Scenario`, `tools/fmr_history.py`, `scripts/forecast_evidence.py` and `tests/test_flag_propagation.py` all still gave the rent/price correlation as `pooled r = -0.309` and used it to justify the pairing search, after #21 established the sign is a property of the *rent series* (+0.222 on market rent, r² never above 0.10). `test_flag_propagation`'s was the furthest off — it described the offline fallback scorer as scoring over that correlation when U9.3 had rewritten it to score departure from the neutral case. §8 asks docstrings to cite precisely, which is exactly why a stale citation there is worse than stale prose | maintenance |
| Sept 2, 2026 | U9.M | **A run prints 4 lines before the report instead of ~190, and the cause was not the library everyone blamed.** The chatter was attributed to HuggingFace and `sentence-transformers`; measured, those libraries are innocent — Python's root logger starts at WARNING with no handler, so their INFO records go nowhere. **Decision #13's MCP reference server turns them on.** `MCPServer.__init__` calls the SDK's own `configure_logging()`, which runs `logging.basicConfig(level="INFO", handlers=[RichHandler(...)])` process-wide, and `agents/scenario_forecast` imports `mcp_server` on every run to build its evidence menu. New `tools/logging_setup.library_logging_unchanged()` restores the root logger around that one construction; `LIBRARY_LOGS=true` keeps the chatter for debugging. **Restoring rather than silencing named loggers is the decision** — a list of noisy logger names goes stale silently, which is the failure `graph.state_serde()`'s allowlist has now hit twice, while the root logger's prior state is a fact the process can read. Fixed at the cause, so `main.py`, `app.py`, the eval harness and all 28 scripts get it without an import-order rule to remember. Verified inert: the LA sample report replays byte-identically and all 30 eval rows are unchanged | 7.1, maintenance |
| Sept 2, 2026 | U9.M | **The account identifier no longer reaches the terminal, closing `diagnostics.py`'s `TODO(security)`.** The full-detail channel exists to print an exception unabridged; the calling account's `user_id` is the one field exempted, in both the JSON-field and bare-token forms, because Week 7's deliverable is a terminal capture and a raw 429 during it would put the id on screen. **Redaction over the env-gated verbosity switch the TODO offered as the alternative**: a switch has to be remembered once, ahead of a capture that cannot be edited afterwards, on a run where something has already gone wrong. Status code, provider message, remedy hint and traceback are untouched. `tests/test_diagnostics_redaction.py` guards both directions, since an over-broad pattern eats the detail the channel exists for and fails just as silently. §8's TODO inventory re-reconciled against the grep in the same pass — **the count is unchanged at six and the composition is not**, `diagnostics.py` closing as `tracing.py` arrived | 7.1, maintenance |

---

## Sept 1, 2026 — U9.7: the Streamlit surface

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 1, 2026 | U9.7a | **Fault injection becomes one implementation with three callers.** `tools/faults.py` — the `Fault` enum and its three patches moved out of `eval/runner._case_environment`, which keyed them off an `EvalCase`; the demo surface and `main.py` need the same failures against a `DemoDeal`. Placed in `tools/` so nothing in the lower layer imports from `eval/`; `eval/cases.py` re-exports `Fault` so every case definition and `eval/README.md` keep the name they have always used. The runner now delegates and its docstring points at the new home rather than carrying a second copy — the defect `hud_fmr.bedroom_field` already names, applied to a fault that would otherwise behave differently in the demo than in the evaluation. `main.py` gains `--fault`, which is also how the combinations are recorded | 6.1, 7.1 |
| Sept 1, 2026 | U9.7a | **The simulated-failure marker stops carrying internal vocabulary, and the safety of changing it was measured rather than argued.** It read *"[eval fault injection, case 'x']"* and reaches a `Flag.detail`, which the report renders verbatim — so under §8 it was internal vocabulary in reader-facing text, and it was also wrong for a demo run, which is neither an evaluation nor a case. Now *"[simulated failure, declared by 'x' — not a real outage]"*. **The wording is only free to change because no prompt embeds it:** `scenario_forecast._context_block` quotes flag *kinds*, `summarizer._lede_prompt` quotes severity *counts*, and neither quotes disclosure text. Confirmed by re-deriving the full batch — no `CacheMiss`, all 30 rows byte-identical — rather than by reading the two call sites | 7.1 |
| Sept 1, 2026 | U9.7b | **The three combinations the surface will replay are recorded.** `chicago --no-retrieval`, `los-angeles --fault geocoder-outage`, `los-angeles --fault stale-rent-index`, each through `main.py`'s full path so the resume and the written summary are captured — the thing U9.5 established the eval batch structurally cannot do, since `run_case` never resumes an escalated row. `los-angeles` sites both faults because it is the one demo deal reporting clean at 1.00, so the contrast is attributable to the fault alone. **16 recordings, all additions** — no recording modified or deleted — and the ablation needed exactly one, the post-resume summary call U9.5 left uncovered. Verified: all four combinations replay with no `CacheMiss` (`llm-unavailable` needs no recording at all), and the 30 eval rows are byte-identical | 3.1, 6.1, 7.1 |
| Sept 1, 2026 | U9.7c | **The demo surface exists.** `src/app.py` — deal picker, guarded invoke held in `st.session_state`, a fresh `thread_id` per run, and the compiled graph behind `@st.cache_resource` so only the first run pays for the Chroma index and the embedding model. Replay is pinned by assigning `config.LLM_CACHE_MODE`/`LLM_CACHE_DIR` the way `eval/runner._case_environment` does, and **restored in `finally`** — which matters more here than in the batch, because this process outlives the run and a switch left flipped would apply silently to every later run in the session | 7.1 |
| Sept 1, 2026 | U9.7c | **The report is split, never re-rendered.** `split_report` cuts the Summarizer's markdown at its own `##` headings; the top matter (title, both verdict lines, the status line) always shows, `Summary` renders inline, the rest collapse. **The app never lays the evidence out from state** — the one place it reads state is the status strip, and every figure there is one the report also prints. This is U9.4's declined "two renderings" taken deliberately in the other direction: two renderings drift the first time either is edited, and the report is the artifact under review | 7.1 |
| Sept 1, 2026 | U9.7c | **`is_recorded` states in advance whether a run can be replayed**, as data rather than by attempting it and catching `CacheMiss`. A miss would tell the reader the run was live only after it had already failed partway through, which makes the mode badge a report on the past rather than a statement about what is about to happen. A live run writes to the development store, never the committed one — recording into `eval/data/` stays a deliberate act, not something a demo click can do by accident. Verified end to end through Streamlit's own `AppTest`: a clean deal renders 1.00 / 4 disclosures / 8 comps matching the CLI exactly, and an escalating deal pauses without an exception | 7.1 |
| Sept 1, 2026 | U9.7d | **The `human_review` interrupt becomes a genuine pause.** `app.py._review_panel` renders the escalation as a request rather than an error: the desk it routed to (U9.2), the flags at warn or critical that caused it, any unanswered questions, and an editable note box with a Release control. `main.py` auto-resumes with a canned note so one command yields one report; here a person reads the grounds and writes their own — which is the human-oversight evidence 7.1 asks for, and the reason this surface is not a report viewer. **The resume is wrapped in the same `run_environment` as the first invoke**, because a fault that lifted between the pause and the resume would produce a report whose written summary succeeded during an outage the report says never ended | 6.1, 7.1 |
| Sept 1, 2026 | U9.7d | **Four controls, one question.** The ablation checkbox, the fault selector, the demo picker and the paste box are the same mechanism with different arguments, and all four are decided by `is_recorded` *before* anything runs: a recorded combination replays and says so, an unrecorded one states that the model will be called and disables Run behind an explicit confirmation. The model liveness check runs only ahead of a live run — in replay it could add nothing but a network round-trip and a way to fail. Fault names are reader-facing (§8): *"The address lookup service is down"*, not `GEOCODER_OUTAGE` | 3.1, 7.1 |
| Sept 1, 2026 | U9.7d | **Verified through Streamlit's `AppTest`, not by eye.** All four recorded combinations render figures identical to the CLI — ablation 0.60/6/0, geocoder outage 0.85/20/8, stale index 0.85/6/8, model outage 0.00/5/0 — with no exceptions. The full oversight path was exercised end to end on `staten-island`: escalate, route to the agent desk, type a novel note, release, and the note appears verbatim in the finished report beside `Proceed ⚖`. **That marker is the serde fix paying off directly** — the disagreement flag lives on `RecommendationDetail`, which arrived at the strip as a bare dict before this session's allowlist repair | 6.1, 7.1 |
| Sept 1, 2026 | U9.7e | **The surface is documented and the README's stale figures are corrected.** README gains a *demo surface* section and the `--fault` / `--no-retrieval` switches; `docs/demo.md` gains a full section on what the app does that the command line does not. **Two numbers in the README were wrong since U9.6** — it said 28 evaluation rows where there are 30, and listed six demo deals where there are eight. It is a graded artifact (7.1), so the correction landed here rather than waiting for U9.M. `architecture.md`'s repository tree gains `tools/faults.py` and marks `app.py` built; **the rest of that tree is stale enough to need re-deriving rather than editing** — it lists `eval/run_eval.py` and `eval/expected.yaml`, neither of which exists — and is recorded as maintenance item M7 rather than half-fixed | 7.1, maintenance |

---

## Sept 1, 2026 — maintenance: credential paths, and a LangSmith key that now exists

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 1, 2026 | maintenance | **The two on-disk credential paths follow the files they point at.** Both token loaders named extension-less paths that no longer exist; `tools/llm_client.py` and `tools/hud_fmr.py` now resolve the renamed files. Nothing about the resolution order changed — env var first, file second — and both were re-verified to load rather than assumed to | maintenance |
| Sept 1, 2026 | maintenance | **`tools/tracing.py` gains the same on-disk key fallback the other two credentials have**, because a LangSmith key exists as of today (OQ-13) and only the *switch* should have to be typed. `_load_key` returns `None` rather than raising — the one way it differs from its two siblings, since the pipeline cannot run without a model and can run without a record of having run — and the key is written back into the environment because the LangChain runtime reads the variable itself. **`LANGSMITH_TRACING` deliberately kept no file fallback:** a key on disk says this machine *can* trace, the variable says this run *should be* traced, and merging the two would start shipping every local run to a hosted service. Both paths exercised | 5.1, 7.1 |
| Sept 1, 2026 | maintenance | **The checkpointer's deserialization allowlist had drifted again, and the recurrence is the finding.** `graph.state_serde()` was missing four types reachable from `DealState` — `ConfidenceBreakdown` (U7), `FlagScope` (U8.5), `Recommendation` and `RecommendationDetail` (U9.4) — so a resumed run deserialized them blocked. **Found the same way as the Aug 22 instance: by reading a log line nobody was reading**, while pre-flighting U9.7's replay path. The rule that would have prevented it was already written in this function's own docstring, which is why the docstring now records that stating a rule beside the list does not keep the list current. **Worse degradation than the enum case:** the two `BaseModel` types yielded a plain `dict` rather than a string, and while Pydantic re-validated at the node boundary — so no report was ever wrong, and both committed sample reports still reproduce byte-identically — `graph.invoke`'s return value carries the raw dict. `main.py` reads only `report_markdown` and the eval runner never resumes, so **U9.7's surface is the first caller that would have hit an `AttributeError`, on the escalated path it exists to demonstrate.** Verified: typed objects and enums survive the round trip, 97 tests pass, all 30 eval rows byte-identical | 6.1, 7.1, maintenance |
| Sept 1, 2026 | maintenance | **The README's setup block names the tracing variables**, marked optional, so a reviewer reproducing a trace is not left to infer them from the source. Still no reference to any gitignored path (§8) | 7.1 |

---

## Sept 1, 2026 — U9.6: the demo deals

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 1, 2026 | U9.6 | **`docs/demo.md` brought back into line with the build, and one claim in it was wrong rather than merely stale.** Sections for both new deals; a "two questions, deliberately kept apart" section for the axes U9.4 built and this document predated; every confidence score and disclosure count re-transcribed from the committed results table. What it had been saying: `overpriced` was a Los Feliz listing in **Los Angeles** (re-sited to Uptown, Chicago at U9.4), `chicago` "reports normally" (it escalates on a critical objection since U8.6e), `los-angeles` carried 3 disclosures (4 — `rent_growth_source` arrived with U9.3), and five of six stat lines disagreed with any run. **The wrong claim:** the U4 ablation was described as the only run landing at exactly 0.60 and escalating on the critical rule rather than the score — `coord-conflict` does too, and `chicago` escalates at 0.70. Corrected to say what the ablation actually is alone in doing: carrying `retrieval_disabled`, which no listing can produce | 7.1 |
| Sept 1, 2026 | U9.6 | **Both new deals recorded, and the published table re-derived at 30 rows.** `eval/data/llm_recordings/` (+8 files, additions only — no recording superseded), `eval/results/results.md`, `eval/results/sensitivity.md`. **Both predictions held:** `chicago-uptown` reports at 1.00 / 5 info / *Proceed*, `los-angeles-current` at 1.00 / 4 info / *Proceed* — the latter raising exactly what `los-angeles` raises, which is the architectural claim its prediction rested on. **The other 28 rows are byte-identical**, so the only diff is the two additions and agreement moving 18/21 → 20/23. The demo path was replayed through `main.py` for both, not just the harness, since U9.5 found an escalating row can leave the Summarizer's prompt unrecorded. Visible result of the shadow: the same property, same $2,861 estimate, stated rents moving from **1% above** it to **6% below** | 7.1 |
| Sept 1, 2026 | U9.6 (defect) | **The sensitivity sweep's live-tier exclusion was silent and had just become load-bearing.** `scripts/confidence_sensitivity.py` — `scoring_cases()` already drops `BASELINE`, so filtering live rows removed nothing while every live case carried one, and the line read as belt and braces. U9.6's two demo deals declare `PREDICTED` verdicts, making that filter the only thing keeping them out of the sweep. The behavior is right and was the original intent (demo deals are calibrated to run clean), but the artifact's header claimed to cover "21 cases with a verdict declared before the run" when 23 now exist. Filter documented; header now states the exclusion and why the two counts differ. **Measured content unchanged** — the 63-of-160 plateau and every grid point are identical | 6.1 |
| Sept 1, 2026 | U9.6 | **A shadow deal, so the anchor change is shown on a listing's own figures rather than asserted.** `demo_deals.py` + `eval/cases.py` + `main.py` — `los-angeles-current` is `los-angeles` with its two stated rents re-based from HUD's schedule ($2,850/$2,950) to the market anchor ($2,650/$2,730, ZIP 90026 at $2,691), and nothing else touched: same address, description, asking price and price basis, since #19 was a rent decision. **Los Angeles chosen over the larger moves deliberately** — re-basing shifts Echo Park −7.2% against Uptown's +14% and Logan Square's +34%, but only on `los-angeles` is the report clean enough for the re-basing to be the sole difference between two otherwise identical reports, showing up as a sign flip (stated rents ~1% above the estimate, now ~6% below). Enters `PREDICTED`, and the prediction is architectural: no flag in this build reads a stated rent (`unit_rents` is read only by the Summarizer's disclosure, and #20 leaves the divergence threshold `None`), so the row should raise exactly what `los-angeles` raises. Scoring population 22 → 23 | 7.1 |
| Sept 1, 2026 | U9.6 | **A sixth demo deal, and the first whose recommendation is read against a benchmark it was not derived from.** `demo_deals.py` + `eval/cases.py` + `main.py` — `chicago-uptown`, an Uptown two-flat at 5100 N Kenmore (the golden fixtures' own address, so listing and fixtures describe one property at several specifications). Price calibrated to ZIP 60640's assessor-derived median with no premium; rents to `market_anchor:2`, the first deal declared on the current anchor. **OQ-21's stated premise had already been met** — U9.4's re-siting of `overpriced` gave the set a second clean axis-1 run — so the deal is built on a restated purpose: `los-angeles` returns *Proceed* on a 0% premium against the metro median its own price came from (OQ-20), and this is the first deal whose *Proceed* is measured against a local figure. Enters as **`PREDICTED`, not `BASELINE`**: the labels distinguish where a verdict came from, not whether the outcome was guessable, and a deal with no published outcome has a claim rather than a transcription. Scoring population 21 → 22 | 7.1 |
| Sept 1, 2026 | maintenance | **The subject-ZIP rule extracted to one home.** `tools/zcta_crosswalk.resolve_subject_zip` — "the listing's stated ZIP, else the point-in-polygon join" had two callers inside `agents/valuation_rent` (U8.8) and gained a third outside the graph entirely when the calibration check began re-deriving the market anchor, which resolves at that ZIP. Reproducing the rule in the script would have let a verifier check a figure no report prints. Takes primitives rather than `DealTerms`, because the third caller parses an address out of listing prose and holds no state object; the agent keeps its private spelling as a delegation. No behavior change — 97 tests pass, all six deals verify identically | maintenance |
| Sept 1, 2026 | U9.6 | **A second rent basis, so a new demo deal can be declared against the anchor the system actually uses.** `demo_deals.py` + `scripts/verify_demo_calibration.py` — `market_anchor:<beds>` joins `hud_fmr:<beds>`, re-derived through `rent_model.anchor_for_row` (the same function training and the Valuation agent call) rather than a second copy of the formula. OQ-21 requires a new deal to declare the current anchor or ship stale on day one; nothing about the four existing listings changes, and all six still verify. **Measured while building it: the FMR-to-market gap is not a fixed offset and not one-signed** — the schedule runs 33.1% under the market index in ZIP 60647, 13.8% under in 60640, and **7.3% over** in 90026, where the FMR lookup returns `used_msa_fallback` and so describes Los Angeles County rather than Echo Park. `demo_deals.py`'s docstring had generalized Logan Square's direction to the whole set; corrected | 7.1 |

---

## Sept 1, 2026 — U9.5: pin the tier, settle Staten Island, re-record

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 1, 2026 | U9.5 | **The demo path is recorded too, and the sample reports and README are current again.** `config.LLM_CACHE_DIR` becomes env-overridable, 6 demo recordings, `docs/sample_reports/*.md` regenerated, `README.md`. **The batch structurally cannot record what the demo surface replays**: an escalating case pauses at `human_review` and `run_case` never resumes it, so no harness run ever reaches the Summarizer on those rows and none of their written summaries were ever recorded. Four demo deals escalate. **And a miss there is fatal, not graceful** — `CacheMiss` subclasses `Exception`, not `LlmError`, so it passes straight by `_lede_section`'s handler and takes the node down; U9.7's replay-by-default surface would have crashed on exactly the deals whose degraded paths demo best. **Fixed by recording rather than by widening the catch**, deliberately: catching it would let a drifted prompt render a report with its summary quietly missing, which is the failure the replay design exists to make loud. Measured exactly as predicted — the two reporting deals needed **no** new recording, the four escalating ones needed **one each**. The two committed sample reports predated both U9.3 and U9.4 and showed a report format that no longer exists; the README's two "in progress" limitation bullets described work that has since landed, and it now states positively that every figure in the repository re-derives from a clone | 7.1 |
| Sept 1, 2026 | U9.5 (defect) | **A premium that rounds to nothing read as `0% above`, which an investor sees as a placeholder.** `agents/critic.recommend`. The clean verdict on `los-angeles` rendered *"The asking price is 0% above the typical sale price for this area"* — arithmetically true, and as English it reads like a value failed to populate. The branch now tests the figure **as rendered** (`round(premium * 100) == 0`) rather than as computed, so the sentence and the number a reader sees cannot disagree, and it needs no threshold constant because the format string's precision *is* the threshold. **Found by regenerating the sample reports rather than by any test** — no assertion covers prose. Worth naming why it fires where it does: under #11 the demo asking prices were derived *from* the metro median, and Los Angeles has no ZIP tier, so the deal is compared against the very figure it was calibrated from. The wording is fixed; the circularity underneath it is OQ-20's and stays out of U9's scope. Verified confined: the whole 28-row batch replays with **zero** live calls afterwards, so no other row's figures round that way | 7.1 |
| Sept 1, 2026 | U9.5 | **All 28 rows re-recorded and the published table re-derived — 147 recordings added, none deleted.** `eval/data/llm_recordings/` (206 → 353), `eval/results/results.md`. The offline tiers had been erroring on **every** row since U9.3: rewriting `_context_block` and `_DEPTH_INSTRUCTIONS` is a prompt change, so `CacheMiss` was the correct and loud failure, and the committed table still dated to Aug 30 (U8.6e) — two units stale, with an `overpriced` row describing the Los Angeles siting U9.4 replaced. **Superseded recordings are orphaned rather than deleted**, so what the old table was produced from stays in the repository. **The check this pass could not use was U8.8's byte-for-byte comparison** — those passes were verdict-inert by design and this one moves every band and adds a verdict — so the substitute is that **every moved row has a named cause**, read row by row | 6.1, 7.1 |
| Sept 1, 2026 | U9.5 (verification) | **Two units of change moved zero verdicts, and decision #6's evidence is byte-identical.** Verdict agreement holds at **18/21**, regression against U7.8 at **6/7**, flag coverage at **30 of 30** — every figure unchanged from the pre-U9.3 table. `scripts/confidence_sensitivity.py` re-run over the new recordings reproduces `sensitivity.md` **exactly**: the same 63-of-160 plateau, the same 11-of-21 escalating, the critical weight still inert across its range. So #6 is unaffected by the forecast re-sourcing and the recommendation, which is worth stating positively rather than leaving as an absence. **Two figures did move and both are accounted for**: `overpriced` 0.85 → **1.00** with its warn gone, which is U9.4's re-siting to Uptown — the market chosen precisely because it returns eight matching comps and relaxes nothing, so the asking price is the only thing firing; and `staten-island` **1 → 0 comps**, which is the tier pin retiring a stale development-cache extraction. **Every disclosure movement traces to U9.3 alone**: `rent_growth_source` is new and fires on every row, the cohort-shift disclosure retired, and `anomalous_period_included` is now decided by the depth-1 framing for *both* series rather than price alone — so it fires per deal according to which framing the search chose, which is why two rows moved *down* while the rest moved up | 6.1, 7.1 |
| Sept 1, 2026 | U9.5 | **Every tier replays; reaching a model is now something someone typed.** `eval/runner._case_environment` drops its `if case.tier is not Tier.LIVE` guard, `run_case`/`main` gain `--live`, and `eval/README.md`'s tier section is rewritten. The demo rows had fallen through to `LLM_CACHE_MODE=read_write` against the **gitignored** development cache, so a row was served from a developer's working store when warm and called the model when not — **the seven figures the report quotes from them could not be re-derived from a clone**, against this harness's own standard that such a figure is an assertion rather than evidence. `--live` and `--record` are rejected together rather than silently ordered: one exists to freeze a live run, the other to run without touching the store. **The same defect was fixed once before and left half-done** — U8.2 pinned the two offline tiers and left the third outside the override, where it stood for a unit; the README now says a reproducibility claim has to be enforced for every case it is made about, because a partial fix reads exactly like a complete one from the prose | 6.1, 7.1 |
| Sept 1, 2026 | U9.5 | **Axis 2 reaches the results table.** `CaseResult.recommendation`/`recommendation_cell`, a `recommendation` column, and two footnotes. U9.4 made the rule deterministic so a batch could score it, and no batch could see it. **Reported, not scored, and the column says why**: nothing in `cases.py` declares an expected recommendation, and authoring 21 of them after the rule exists would score the rule against a reading of itself — the error `VerdictSource` exists one column over to prevent. What it buys is a regression surface, since axis 2 is a pure function and any movement on fixed recordings is a real change in the rule. A `⚖` marks a row where the model cross-check reached a different verdict, which is the only place a batch shows the second reasoning locus (OQ-22) ran at all. **`staten-island` now reads `escalates` / `Proceed ⚖` in one line** — the clearest statement of the two axes anywhere in the build | 6.1, 7.1 |
| Sept 1, 2026 | U9.5 (finding) | **Staten Island's zero comps are structural, and no recording could have been "wrong" to freeze.** Measured against the Chroma corpus directly, no model calls: **0 listings within 4 miles** of 7001 Amboy Rd and **7 within 8, none of them 2-bedroom**, against a 2-bedroom subject. At the widest radius the relaxation ladder reaches, with the square-footage band already dropped, there are zero eligible candidates — so no extraction of this listing can produce a comp, and the published `1` was the stale development-cache extraction the tier pin retires. The deal's own declaration was right all along (*"zero comparables is the real, measured gap this deal exists to show"*). **Checked before recording rather than after**, per the task list, because freezing a wrong extraction would have preserved a defect instead of fixing one. Second-order finding for U9.M: the ladder spends all four iterations on the size band and two radius widenings and **never reaches the bedroom count**, which is the binding filter here — a concrete instance of the relaxation order being inherited from U4 rather than measured | 6.1 |
| Sept 1, 2026 | U9.5 | **Depth 1 is told that treating both series alike is not a reason, and what a real reason looks like instead.** `agents/scenario_forecast._DEPTH_INSTRUCTIONS[1]`. Since #21 both forks ask one question — does the 2020-2022 rate regime belong in the record — of two different series, so symmetry became available to whichever framing claimed it and cost nothing; Los Angeles took it, scoring `f-00` at **0.95** for *"uses the same 2020-2022 window for both rent and price"*. The clause names that argument as empty and redirects to what the exclusion **costs each series here** — how many observations each side loses, where in its history the gap falls, whether what remains still spans enough to band. **It names the asymmetry that was already working rather than only forbidding the one that was not**: a series beginning *inside* the excluded window loses the front of its history rather than a middle segment, which is `staten-island`'s real argument — Richmond's ZORI starts 2020-08, and its off-diagonal `f-01` won at 0.96 against the diagonal's 0.15. Carried from U9.3's `TODO(U9.5)` and landed at the head of this pass because a prompt change invalidates every forecast recording | 6.1 |

## Sept 1, 2026 — U9.4: the recommendation

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Sept 1, 2026 | U9.4 (maintenance) | **The two sale fetchers return per-sale prices, so one definition of "a qualifying sale" serves both scripts.** `scripts/build_sale_benchmarks.py` — `new_york`/`chicago` become `new_york_sales_by_zip`/`chicago_sales_by_zip` returning the grouped prices; `main()` reduces them through the unchanged `_summarize`, and a new `_MARKET_FETCHERS` binds each market key to its fetcher in one place. The dispersion measurement needs the prices themselves — how far a single sale sits from its ZIP's median is a question a table of medians cannot answer — and a second script restating the `$where` clauses would be two definitions that drift apart without either looking wrong. The committed table is untouched: `_summarize` over the same grouping is the same reduction | maintenance |
| Sept 1, 2026 | U9.4 | **What a price premium is worth, measured before a threshold was set.** New `scripts/sale_premium_distribution.py`; `docs/design/recommendation.md`. U9.4 puts a threshold at the center of the report's headline claim and **nothing in the repository said what a premium meant** — `tools/data/zip_sale_benchmarks.json` holds one median per ZIP and no dispersion, so a threshold read off it would have been read off nothing. The script re-pulls the 44,358 individual sales behind those medians and asks what share of real transactions cleared their own benchmark by X%. **ZIP tier: +15% is the 68th percentile, +30% the 80th, +50% the 89th** — wide, but not so wide that nothing can be said, which is the outcome the measurement was run to rule out. Two markets measured separately agree closely and per-ZIP weighting moves nothing below +30%. **Metro tier is roughly twice as wide, the two markets disagree, and Los Angeles is absent entirely** (Proposition 13 publishes assessed value, not transaction price), so every metro-tier figure is extrapolated | 7.1 |
| Sept 1, 2026 | U9.4 (defect) | **The worked example U9.4 was planned around was false, and the measurement is what caught it.** `docs/tasks/task_list_u9.md`, `docs/design/recommendation.md`. The plan read "asking price 55% above the ZIP benchmark → exceeds threshold"; `overpriced` is `1801 N Vermont Ave, Los Angeles` and **90027 has no ZIP benchmark**, so its `price_premium_to_basis = 0.55` is 55% over a *metro* median — the **78th percentile of actual sales**, an ordinary transaction. A rule honest to the evidence returns *Proceed* on the deal named `overpriced`. Resolved by re-siting the deal to a market with a local tier rather than by bending the threshold to fit the fixture; the three declined alternatives are recorded with their reasoning | 7.1 |
| Sept 1, 2026 | U9.4 | **Axis 2: the system says whether the deal is worth buying, and it is a pure function.** `state.Recommendation`/`RecommendationDetail`/`DealState.recommendation`, `agents/critic.recommend`, four thresholds in `config.py`. Until now the report answered only whether the *software* could stand behind its numbers, and readers took that for the other question — on `staten-island`, which escalates on zero comparables while asking 17% below its ZIP median, the banner told a reader the reverse of what the evidence said. Computed in the Critic because that agent already aggregates flags and decides routing, which keeps the Summarizer reporting rather than computing. **Deterministic on OQ-17's evidence**: a model scoring an identical prompt 0.05 then 0.95 would make the same deal proceed on Tuesday and not on Wednesday, and would create a second axis `eval/runner.py` cannot score. Thresholds are stated percentiles of real transactions rather than round numbers, so the report can say what one *means* — "higher than roughly 90% of recorded sales in this ZIP" | 7.1, 6.1 |
| Sept 1, 2026 | U9.4 (defect) | **The first draft let an uncorroborated rent reach a verdict on its own, which re-merged the two axes one line lower.** `agents/critic.recommend`. Whether the comp cross-check could run is largely a fact about *data coverage in this market* — `state.scope_of` classifies the sparse-comp disclosure as market-scoped for that reason — so it belongs to axis 1. Under the first rule `staten-island` read *proceed with caution* about a deal whose only measured fact is that it is **cheap**, which is precisely the misreading the escalation banner already produced. The price finding is now the only thing that can start a verdict and the rent side only ever modifies one; reject still needs both. Staten Island reports **Proceed** on axis 2 while escalating on axis 1, and the distance between those two lines is the point. Caught by running the rule against the demo set rather than by reading it | 6.1 |
| Sept 1, 2026 | U9.4 | **A second reasoning locus: the model proposes, the rule decides (OQ-22).** `agents/critic.cross_check`, `RecommendationDetail.model_verdict`/`model_rationale`/`cross_check_disagrees`, `config.RECOMMENDATION_CROSS_CHECK_ENABLED`. The forecast's search was the only place a model exercised judgment once #12's Critic half was retired at U7.7, so a system described as agentic rested that claim on one node. The model reads the same evidence — in the rule's own rounded figures, and without the confidence score or the escalation decision, which are axis 1 — and reaches its own verdict; **it can never move the rule's, only annotate it**, which is what keeps reproducibility intact under OQ-17. Degrades to no annotation rather than to a flag: a missing second opinion is not a disagreement, and rendering it as one would manufacture a finding out of an outage. Verified live on the re-sited `overpriced` deal, where both readings independently reached *proceed with caution* | 7.1 |
| Sept 1, 2026 | U9.4 | **`overpriced` re-sited to a market whose benchmark can carry the claim it was making.** `demo_deals.py`, `scripts/verify_demo_calibration.py`, `eval/data/geocode_cache.json`. Now `4700 N Racine Ave, Chicago 60640` asking $1,345,000 against that ZIP's $867,500 median over 148 sales — a real +55% where +55% is around the 90th percentile, instead of 55% over a Los Angeles metro median where it is the 78th. **Uptown rather than Logan Square, chosen by running both**: Logan Square's comp set relaxes the size band and escalates the deal, so the report showed a cautionary recommendation beside an escalation and a reader could not tell which the asking price had caused; Uptown returns eight matching comps and reports at confidence 1.00, as the `chicago-uptown-duplex` control does on the same profile, so **the asking price is the only thing that fires anywhere in the report**. A new `zip_sale_benchmark:<zip>` basis kind carries it, because a deal should be calibrated to the benchmark its own report reads — the Valuation agent prefers the ZIP tier, so a deal in a local-tier market calibrated to the metro figure is verified against a number its report never prints | 7.1 |
| Sept 1, 2026 | U9.4 (defect) | **The clean verdict claimed the rent evidence was fine on a deal with zero comparables.** `agents/critic.recommend`. The price position was stated only when a threshold was crossed, so `PROCEED` fell back to a generic line — *"nothing in the asking price or the rent evidence argues against this deal"* — which is true of what the rule tested and false as English on `staten-island`. Every verdict now names the number it turned on: *"the asking price is 17% below the typical sale price for this area."* A verdict that carries its own evidence cannot drift from it that way, and the reader gets a fact rather than a reassurance | 7.1 |
| Sept 1, 2026 | U9.4 | **The report opens with two lines that never merge, and progressive detail below them.** `agents/summarizer._verdict_lines`/`_verdict_reasons_block`/`_disclosure_entry`. The escalation banner was the only verdict a reader met, and they read it as a verdict on the *property* — on `staten-island`, which escalates on zero comparables while asking 17% below its ZIP median, exactly backwards. Now: **Recommendation** and **System check** as two labelled lines in one block, the second saying in as many words that it is a statement about the evaluation rather than the property, and the clean case saying so rather than staying silent. Progressive detail splits **by severity, not by length**: info-severity entries collapse to their opening clause, critical and warn stay open. That keeps both of the module's load-bearing rules intact rather than trading one away — nothing moves below the numbers it qualifies, and nothing is reduced to a count | 7.1 |
| Sept 1, 2026 | U9.4 | **A written summary above the report, additive and deciding nothing.** `agents/summarizer._lede_section`/`_lede_prompt`, `config.SUMMARY_NARRATIVE_ENABLED`. It renders the verdict the rule computed and does not reach one — a summary that could restate the verdict differently would put OQ-17's variance back into the one line a reader takes away. Sits below the verdict block so the reproducible statement is first. **On failure it renders a sentence, not a flag**: a 31st `FlagKind` would break U8's 30-of-30 census unless a declared fault could reach it, and every other flag in this system propagates, while one raised in the terminal node has no consumer but the report already printing it. Quotes only rounded reader-facing figures, so it adds no second instance of OQ-18's float-in-prompt fragility | 7.1 |
| Sept 1, 2026 | U9.4 (defect) | **The summary described evidence it had not been given, twice, and the fix was structural rather than another prompt line.** `agents/summarizer._lede_prompt`. Handed excerpts of the raised disclosures, it wrote *"limited comparable data"* on a run whose comp set was full and whose disclosures were all mechanism notes, and *"zero comparable sales within two miles"* for a **rental** comp count — a report carrying both rental comparables and recorded sales cannot afford that word swapped, since it moves the price benchmark from 148 transactions to none. Two rounds of prompt wording did not hold it. The excerpts were never in this section's brief, and every disclosure renders in full immediately beneath it, so they are gone and the counts remain. Prompt-fitting stopped there deliberately, on U9.3's precedent | 7.1 |
| Sept 1, 2026 | U9.4 | **The suite stopped reaching the network, which it had started doing silently.** `tests/conftest.py`, `tests/test_report_verdict.py` (21 cases). The recommendation cross-check and the written summary are reached by **every test that renders a report**, so the seam belongs in `conftest.py` and is autouse for the reason `offline_scenario_evaluator` gives — a case added later must not reach out by forgetting to ask not to. Measured: the full suite ran **48s before and 13s after**, which is the live calls it had been making. Neither switch changes a verdict or a figure, which is what makes disabling them safe; the cases that exercise those paths re-enable one and substitute a client. New coverage: the four verdicts, the tier-dependent thresholds, that the rule ignores the escalation decision, that a disagreeing model annotates without deciding, that an unreachable one leaves no annotation rather than a disagreement, and both summary-failure sentences | 6.1 |

## Aug 31, 2026 — U9.3: the forecast's rent source, and the premise it rested on

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 31, 2026 | U9.3 | **The band estimator extracted to `tools/growth_bands.py`, so one function serves both series.** New module holds `bands_from_yoy`, `sustained_means` and `is_in_anomalous_period`, moved unchanged from `redfin_data`; `compute_growth_bands` keeps what is about *this* series — reading the year-over-year column, naming the metro when filtering leaves nothing, attaching the price floor's provenance — and delegates the arithmetic. The estimator returns `None` on an empty series rather than raising, because it does not know which series it was handed and the caller does. `SUSTAINED_STRETCH_PERIODS` and the 2020-2022 window move to `config.py` under §8's single-home rule, now that two modules read them; `redfin_data` re-exports both under their existing names. **Verified as a pure extraction**: all 4 metros x both treatments x 18 fields identical across the change, 76 tests passing. Prerequisite for #21, whose whole argument is that the two series must be banded comparably | maintenance |
| Aug 31, 2026 | U9.3 | **Rent growth re-sourced from the HUD FMR schedule to Zillow ZORI (decision #21).** New `tools/rent_growth.py` owns the source selection and normalises both to one shape; `agents/scenario_forecast.py`, `state.py`, `agents/summarizer.py`, `config.py`, `docs/design/data_sources.md`. **#16's own architectural argument selects ZORI once applied to today's system** — it chose FMR because the estimate was `ratio x FMR`, and since #19 the estimate is `ratio x ZORI(ZIP) x FMR-bedroom-step`. **County tier, not ZIP, and the reason is coverage**: ZIP 10307 has no ZORI series at all and it is `staten-island`'s own ZIP, on the deal whose forecast is rent-only — a ZIP-first design would have turned a one-sided forecast into none. 65-95% of ZIPs in this project's market counties start after 2018-01, and where both tiers exist the answer barely moves (LA 90026 +0.68/+2.37/+3.86 against its county's +1.25/+2.51/+4.76). The geography mismatch against the ZIP-anchored estimate is disclosed rather than hidden | 6.1 |
| Aug 31, 2026 | U9.3 | **One window, applied to levels rather than to differences, and it decides published numbers.** `config.FORECAST_SERIES_WINDOW_START`, `tools/growth_bands.window_levels`, `redfin_data.get_appreciation_series`. The two readings of "windowed to 2018+" disagree: windowing the year-over-year observations leaves Chicago's pessimistic rent band resting on a stretch ending 2018-12 that the price series does not cover, which is the mismatched-span defect the window exists to remove (unwindowed, New York's was -22.2%/yr from a stretch ending 2017-05). Windowing the levels gives Chicago +3.03 against +1.58 — a 1.45pp difference. **The band table adopted with #21 was measured the other way and is corrected in `task_list_u9.md` and `design/evaluator.md`.** A no-op on the price side, since Redfin's extract already begins 2018-01 for every configured metro, and written anyway so the shared span is a property of the code rather than of the file on disk | 6.1 |
| Aug 31, 2026 | U9.3 | **The fallback threshold measured, and the first rule tried was not enough.** `config.ZORI_GROWTH_MIN_SUSTAINED_STRETCHES`, `tools/growth_bands.SeriesBands.n_sustained_stretches`. Requiring one contiguous twelve-month run is a genuine cliff — 62% of ZORI's 1,211 counties clear it, and below it the estimator silently redefines its outer bands from "worst sustained stretch" to "worst single month" — but Adams County IL clears it on **14 months** and publishes a five-year projection banded +9.18/+9.86/+10.51. **A thin series does not look unreliable, it looks confident**: median band width runs 0.13pp at 1-3 distinct stretches against 6.15pp at 24-43. The requirement is a full year of distinct stretches, the same twelve the window is built from; it drops 97 further counties to the FMR fallback and, unlike the first rule, sits on a smooth distribution and is recorded as a judgment | 6.1 |
| Aug 31, 2026 | U9.3 | **The cohort-shift screen leaves the forecast and stays as evidence.** `agents/scenario_forecast.py`, `state.py`, `config.py`. The depth-1 rent fork was "screen HUD's national step-ups or not" against the price side's "exclude 2020-2022 or not" — two questions in one fork; it is now the same 2020-2022 question asked of both series. `RENT_GROWTH_COHORT_SHIFT_SCREENED` is **repurposed as `RENT_GROWTH_SOURCE`** rather than deleted: the enum's own rule is that an unraisable kind corrupts U8's coverage census and should leave, and #21 created a disclosure the system genuinely owed and did not have — which series and which geography the rent bands came from. `ForecastDetail`'s five cohort fields are removed rather than carried empty. The screen itself is kept in `tools/fmr_history.py`, because removing FY2023-24 is what collapsed the correlation from -0.317 to -0.197 and that pass is a third of the argument for #21 | 6.1 |
| Aug 31, 2026 | U9.3 (defect) | **A ten-digit HUD entityid was being looked up in a five-digit Census index.** `tools/rent_growth.county_geoid`. `DealTerms.county_fips` carries what HUD wants (`<state><county>99999`, built by `county_crosswalk`) while `tools/zori.py` keys its county medians on the Census GEOID, so every deal would have silently fallen through to the FMR fallback and #21 would have shipped as a no-op. Caught by exercising the fallback deliberately rather than by any test | 6.1 |
| Aug 31, 2026 | U9.3 | **The evaluator's instructions corrected: it was told something false, and it had misread its own task.** `agents/scenario_forecast.py`, `agents/summarizer.py`. The context block asserted a negative rent/price correlation as fact; it now states what was measured — weak, sign varying by market, flipping with the rent series, r-squared never above 0.10 — and says explicitly that this removes the directional question rather than answering it, so neither the diagonal nor its opposite may be preferred. The depth-2 instruction gains the two errors it had made: base/base is the **default the other eight depart from** and needs no evidence of extremity, and the level selects **which projections are worth showing** rather than which is most likely. The deterministic fallback scorer, which rewarded anti-correlated pairings, now scores on how far a pairing departs from neutral — the one ordering a scorer with no model can defend. The report's own statement of the retired premise goes with it | 6.1 |
| Aug 31, 2026 | U9.3 | **Removing the false premise exposed that depth 1 had never been given evidence for its own question.** `agents/scenario_forecast._context_block`. With the correlation gone the framing level had nothing to reason from and began scoring on whether the two series were *treated alike*, choosing "include everything" for symmetry. The block now states what the 2020-2022 window is — near-zero rates pulling price growth above trend, disclosure required wherever it feeds an average, including it treats those conditions as recurrable and excluding it costs sample size — and says to argue about the history rather than about symmetry. Evidence, not a directional rule | 6.1 |
| Aug 31, 2026 | U9.3 (observed) | **Two intermediate states, recorded because each was a real regression and neither was noise.** With only the premise corrected, the Los Angeles evaluator inverted it and marked *opposite-extreme* pairings down as unsupported. With the base-case correction added, it pruned all eight non-neutral pairings below the 0.40 threshold and the five-year outlook rendered with **one row and no range** — which is `evaluator.md`'s predicted hollowing-out of depth 2 arriving in practice, not a prompt typo. The final instruction distinguishes "well founded" from "likely" and the table is three rows again with base/base present at 0.94-1.00. Four live runs, and the iteration stopped there deliberately: OQ-17 measures this model's single-draw scores swinging widely, so further tuning would be fitting a draw | 6.1 |
| Aug 31, 2026 | U9.3 | **A beam slot reserved for the neutral pairing, so the report always has a row answering "what do you actually expect?"** `tools/tot.beam_search(reserved=...)`, `agents/scenario_forecast._is_neutral_pairing`. On the Los Angeles run in `design/evaluator.md`, base rent with base price scored 0.70, cleared the 0.40 threshold and came **fourth** against a beam of three, so the row labelled *Base* was base-rent-with-pessimistic-price and the true base case appeared nowhere. Replaying those nine recorded scores through the beam reproduces that exactly and the reservation fixes it. **Rescues from the rank, never from the threshold** — a neutral case the evaluator judged unfounded should not be forced into a report — and the displaced candidate's ledger row says it was displaced rather than outscored. Kept alongside the instruction fix rather than instead of it: that fix lifted the same pairing to 0.94 on a live run, but a prompt that scores well today is not a guarantee under OQ-17 | 6.1 |
| Aug 31, 2026 | U9.3 (defect) | **The cut-boundary margin is now measured before the reservation, not after.** `tools/tot.py`. Taken after, the Los Angeles replay reported **−0.10**, which fires the U8.6c disclosure saying the pairing left out scored above the one kept and "the order was settled by this system's standing preference for the more conservative reading" — false, since the reservation settled it. The margin exists to expose how close the *evaluator's own* cut was, so it is read off the ranking before any policy overrides it, and the displacement is disclosed on the displaced candidate's ledger row instead | 6.1 |
| Aug 31, 2026 | U9.3 | **The forecast leads with one table per series, so every label describes the band under it.** `agents/summarizer._band_tables`. Defect 2's real fix: a scenario row is a *pairing*, its label comes from the two sides' combined outcome, and a row labelled "Optimistic" can carry the pessimistic price band — the report explained that honestly in a paragraph, and explaining a confusing thing clearly does not stop it being confusing. Split per series, the labels are true again. Nothing is lost: all nine pairings and the full ledger remain below. The price row carries "strongest stretch falls in 2020–2022" beside the number when it does, because an optimistic band resting on the rate window is a bad thing to compound five years forward with the caveat two sections away | 7.1 |
| Aug 31, 2026 | U9.3 | **The search renders as its two questions, and the winner's score is shown.** `agents/summarizer._branch_ledger_block`. It flattened both levels into one list of thirteen headed "What the forecast search considered" and printed **only discarded branches** — so a reader saw three losers and never learned what beat them, at the one point in this build where a model exercises judgment. Now: "Step 1 — which reading of the history?" (4 considered, 1 kept) and "Step 2 — which combinations are worth showing?" (9 considered, 3 kept), best score first, survivors marked. Closes with the sampling caveat OQ-17 requires: the scores come from one call whose repeats move materially, the bands do not | 7.1 |
| Aug 31, 2026 | U9.3 (defect) | **The lead table's window sentence read the price fork and generalised it to both.** `agents/summarizer._band_tables`. On `staten-island` and `overpriced` the search answers the two forks differently, so the summary line said "2020–2022 held out of both" directly above a basis block saying rent included and price excluded. Reads both fields and names the mixed case. Also caught in the same pass: the block first claimed the ranges were arithmetic that "no judgment and no model reached", which is false — *which* stretch of history feeds them is exactly what Step 1 decides, and the text now says so and points at it | 7.1 |
| Aug 31, 2026 | U9.3 | **`TODO(U9.5)` left at `_DEPTH_INSTRUCTIONS`: depth 1 is not told that symmetry of treatment is not a reason.** `agents/scenario_forecast.py`, `tasks/task_list_u9.md`. Since #21 both forks ask the same question of two series, which put a cheap generic argument in reach — Los Angeles scored `f-00` 0.95 for "uses the same window for both". **Measured before deferring, and it is not a defect**: `staten-island` chose the off-diagonal `f-01` at 0.96 against `f-11` at 0.15, on the real asymmetry that Richmond's ZORI series begins 2020-08 *inside* the excluded window; three of four framings have been chosen across five demo deals. Deferred to the recording pass because a prompt change invalidates every forecast recording and U9.5 re-records both offline tiers regardless | maintenance |

## Aug 31, 2026 — U9.1/U9.2: the README, the committed model, and the escalation routing rule

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 31, 2026 | U9.1 | **Root `README.md` written**, 28 bytes to a full project overview — the seven-agent pipeline, Transparent Degradation, where the evidence already lives (sample reports, eval results, the graph diagram), setup and run instructions, a documentation map, and a stated-limitations section naming the forecast rework in progress, Staten Island's live-tier reproducibility gap, OQ-17's non-determinism, the missing Streamlit surface, and the deliberately scoped test suite. Written for reviewing rather than running, per the architect's call: leads with evidence a reviewer can read without executing anything | 7.1 |
| Aug 31, 2026 | U9.1 | **The trained rent model committed.** `.gitignore` — `/data/` was a blanket exclusion; re-stated one directory level at a time (`/data/*`, `!/data/processed/`, `/data/processed/*`, `!/data/processed/rent_model.joblib`) so the ~140 KB `rent_model.joblib` is tracked while the Chroma index, the checkpoint DB, the LLM cache and the source CSVs stay out. A fresh clone can now score a listing without a training pass first | 7.1 |
| Aug 31, 2026 | U9.1 | **Two demo reports committed as sample outputs.** `docs/sample_reports/los-angeles.md`, `staten-island.md` — the exact reports the architect read Aug 31 that prompted this unit's rewrite, committed as-read rather than regenerated, known defects included (the LA forecast's implausible pairing, the Staten Island reviewer-note wording this same date's U9.2 row fixes in code). Linked from the README as the fastest way to see real output | 7.1 |
| Aug 31, 2026 | U9.2 | **`docs/design/personas.md` written.** The four personas (IT/operations, the real-estate agent, the investor, a future calling agent), each with its journey, and the escalation routing rule — infrastructure flags to IT, deal-substance flags to the reviewing agent. Added to `implementation_plan.md`'s document map | 7.1 |
| Aug 31, 2026 | U9.2 | **The escalation routing rule reaches code.** `state.py` — `ReviewDesk` (`IT`/`REAL_ESTATE_AGENT`, the latter named for the persona rather than shortened to `AGENT`, which already means a pipeline node everywhere else in this codebase) and `desk_of(kind)`, classifying `FlagKind` the same way `scope_of`/`FlagScope` already does (a judgment call at the vocabulary, `REAL_ESTATE_AGENT` the actionable default). `_INFRASTRUCTURE_KINDS` names five: `EXTRACTION_UNAVAILABLE`, `GEOCODER_SERVICE_UNAVAILABLE`, `RENT_ANCHOR_UNAVAILABLE`, `RENT_ESTIMATE_UNAVAILABLE`, `FORECAST_UNAVAILABLE`. `agents/human_review.py` — `_routing_desks` derives the desk(s) from the escalation-causing flags (excluding the two mechanism-only kinds that name no cause of their own, `LOW_CONFIDENCE_ESTIMATE` and `REWORK_LIMIT_REACHED`) and the `interrupt()` payload gains a plain-language `waiting_on` field. 76 tests pass; the golden eval tier re-run clean with no change to any verdict | 6.1, 7.1 |
| Aug 31, 2026 | U9.2 | **The resume-note layout bug fixed.** `main.py` — the canned resume text said "the disclosures above", true in a terminal print order that does not hold once the same string is replayed into the report banner, where disclosures render below it (visible in the committed `staten-island.md` sample). Reworded to name the disclosures without assuming a position: "the disclosures in this report" | maintenance |

---

## Aug 31, 2026 — U8.10: the close-out's code half

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 31, 2026 | U8.M | **The rework budget swept and held at 2 — it is behaviorally inert across 1, 2 and 3.** `scripts/rework_budget_sweep.py` (new) runs the replay tier at each budget: **no verdict changes at any value.** Only `chicago-geocoder-outage` reworks at all, it spends whatever budget it is given, and it escalates on the exhausted budget at 0.70 every time. The golden tier is not swept and the reason is a measurement, not a shortcut: a rework needs a *retryable* objection, only I3-on-outage is retryable, and no golden case raises it. **Read as unfalsified, not optimal** — the one rework case carries an outage injected for the whole run, which is exactly the condition under which no budget beats another; a *transient* outage clearing on the second pass is where 1 and 2 would part, and the harness's `Fault` mechanism cannot express one. Consequence for §6: **cut-list item 5 now has a measured price of zero behavior change**, costing the demonstration rather than the behavior, and the row says so | 6.1, report |
| Aug 31, 2026 | U8.M | **The four untuned constants stop naming units that closed.** `config.py` — `RADIUS_EXPANSION_FACTOR` and `COMP_MATCH_SQFT_TOLERANCE_PCT` were set by inspection at U4 and never tuned; `COORDINATE_CONFLICT_THRESHOLD_MILES` still rests on U2's single data point, and the eval batch reaches it through one case, so U8 supplied no more volume than U2 did. Each now states what it was set from and that no unit owns tuning it, rather than pointing at a closed one. Two others were not untuned at all — `MIN_QUALIFYING_COMPS` and `MAX_RETRIEVAL_ITERATIONS` are decision #5's Y and Z, **settled at U4 and mislabeled PROVISIONAL for three units since**. The module header records the reconciliation and the rule that would have caught it | maintenance |
| Aug 31, 2026 | U8.M | **Seven `PROVISIONAL` constants reconciled against what U8 actually measured, and two numbers in `config.py` were stale.** `config.py` — the confidence threshold and severity weights are **held** on #6's sweep rather than tuned; `COMP_MAX_OUTSIDE_MATCH_SHARE`, `COMP_MIN_DISTINCT_LOCATIONS` and `RENT_COMP_DIVERGENCE_THRESHOLD_PCT` are held on U8.6b's straddle pairs, each now citing the fixture pair that sits either side of it; `RENT_MODEL_METRO_ERROR_RATIO_THRESHOLD` is held on a **negative** result — no listing can straddle it, since a subject cannot move its own market's ratio — and its per-metro figures were re-measured under #19 (overall $524.03 → **$452.40**, New York 2.00x → **1.89x**: every figure moved, the shape did not); `SALE_BENCHMARK_MIN_SALES` was never provisional, having been set on U8.8's measured distribution; and the ToT block is retargeted **U8 → U9** per OQ-5. **One comment's own revisit condition had been met without anyone noticing:** `RENT_COMP_DIVERGENCE_THRESHOLD_PCT` said to revisit the whole check if a ZIP-level anchor landed, #19 landed one, and the structural blind spot it fired on is gone — `chicago-uptown-duplex` read +48% under the old anchor and reads −6.1% now | 6.1, maintenance |
| Aug 31, 2026 | U8.M | **The `TODO` inventory reconciled against a grep, and a second class of stale marker named in the standard.** `docs/design/engineering_standards.md` — the `TODO(U8)` row closes, the `TODO(U7)` row records that its expected confirmation arrived from the opposite direction (the critical weight is inert, so the rule was never contingent on the weights), and the row claiming "promoting checks A and B" as one open item is corrected: A closed as #20, **B was never separately decided** and is now OQ-20. The standard gains the failure mode U8.M found — a `TODO` is not the only deferral marker, `config.py`'s `PROVISIONAL` names a tuning unit too, and nothing reconciled those against closed units; six constants named U4 or U8 after both had closed. Grep `PROVISIONAL` at unit close alongside `TODO(` | maintenance |
| Aug 31, 2026 | U8.M | **Every `TODO(U8)` marker cleared — nine at U8's start, six after U11.M, now zero.** `agents/critic.py` ×2, `config.py` ×2, plus the four sites that cited them (`scripts/confidence_evidence.py` including one printed line, `eval/data/golden_fixtures.py`, `critic._kinds`'s docstring). Each is replaced by what U8 found rather than deleted: the demo-set county skew **dissolved** rather than being adjudicated — #19's anchor resolves at ZIP tier everywhere, so the pair co-occurs on 0 of 21 cases; the critical-flag rule is now isolated by **five golden rows** rather than by an ablation invocation, and the sweep confirms it from the other side with the critical weight inert across its whole range including 0.00; and the stated-rent threshold's "set a number or delete it" is answered by **#20** — hold at `None`, do not delete, because the option is foreclosed by evidence rather than by arithmetic. Six `TODO` sites survive, all genuinely deferred: `security` ×3, `geography` ×2, `cut-list` ×1 | 6.1, maintenance |
| Aug 31, 2026 | U8.10 | **Two fixture notes corrected, and neither declaration touched.** `eval/cases.py` — `chicago-uptown-band-over`'s note predicted "reports… so this pair measures the *flag's* brittleness rather than the verdict's", which U8.6e's ungate falsified: I1 now draws a critical objection there, so 200 sq ft of floor area flips the **verdict** and the row is a MISMATCH. A dated line records that beneath the original prediction, left verbatim — editing a prediction after seeing the run is what `VerdictSource.PREDICTED` exists to prevent, and the same rule kept `la-three-bedroom-comp-drift` honest when it failed the other way. `la-oversized-loft`'s note described its refusal as the *output* ratio band when U11.1 moved that guard to the model's **input** domain — same flag kind, different sentence to the reader | 6.1 |
| Aug 31, 2026 | U11.5 | **The retired anchor's vocabulary cleared from live prose, including one line a reader sees.** `agents/valuation_rent.py` (the Act step, and the `RENT_ESTIMATE_UNAVAILABLE` band message — now "a rent {n} times the typical rent for a unit of this size in this area" rather than "a rent-to-FMR ratio"), `tools/model/rent_model.py` (`build_training_frame`, `predict_ratio`, the CV comment, and the printed `MAE (rent/anchor ratio)` line), `config.py` ×3, `scripts/train_rent_model.py`, `scripts/model_form_probe.py`, `eval/cases.py`. **U11.5's list of five was assembled from a partial grep and the real count was thirteen** — flag detail text reaches no prompt (only `f.kind.value` does, per `scenario_forecast._context_block`), so the reader-facing one was recording-safe to correct. Deliberately kept: past-tense mentions of `LinearRegression`, `FMR_BEDROOM_CAP_EXCEEDED`, and the ZORI-vs-FMR measurements, which are about FMR and correct | maintenance |
| Aug 31, 2026 | U8.10 | **Two docstrings claimed a decision that was never taken.** `agents/valuation_rent._attach_benchmark` said "check B was not promoted at U8.7" and `agents/critic._consistency_objections` pointed at U8.7 for both checks — but U8.7 measured and settled **check A only** (the stated-rent comparison, closed as #20). The phrase "checks A and B" carried B along without evidence of its own, and B's confound is a different one: #11 set every fixture's asking price from the *metro* median while U8.8 made the benchmark *ZIP*-level, so the gap it would threshold on is mostly that calibration difference — ZIP 60640 runs 77% above the Chicago metro. Both sites now say B is undecided and cite OQ-20, which carries what closing it would take | 6.1 |
| Aug 31, 2026 | U11.5 | **Four identifiers renamed off a retired anchor and a replaced protocol, and the model retrained to move the persisted key.** `TrainingReport.mae_dollars_at_holdout_fmr` → `mae_dollars` and `holdout_rows` → `rows_scored`; `ValuationDetail.model_holdout_mae_dollars` / `model_holdout_mae_ratio` → `model_mae_dollars` / `model_mae_ratio`. The first is serialized into the bundle and read back by string key at `valuation_rent._attach_model_provenance`, so this needed a retrain rather than an edit — taken over a both-keys read because the shim would protect an artifact that is not in the repository (`data/processed/` is gitignored; every clone retrains). **Verified rather than assumed:** the refit under the fixed seed reproduces every published figure exactly (MAE $452.40, ratio 0.2689, per-metro 855/509/357/343), 76 tests pass, and both offline eval tiers re-run **byte-identical** to the published table — no `CacheMiss`, same confidence, same verdicts, same 30-kind coverage | 6.1 |

---

## Aug 30, 2026 — U8.6e/U8.7: the objection gate, and a negative result about the demo deals

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 30, 2026 | U8.6e | **The divergence gate is scoped to one of the three interaction checks.** `agents/critic.py` — `_interaction_objections` no longer returns early unless `RENT_DIVERGES_FROM_COMPS` was raised. I1 (comps matched outside a priced attribute) and I3 (comps and anchor both on a fallback location) are ungated, because "this comp set is the wrong set for this subject" holds however the numbers came out — agreement between two mis-specified quantities is not evidence. I2 (the median is a point sample) keeps the gate: imprecision degrades a disagreement, and ungating it would make a market's data density a critical objection on every Cleveland and Brooklyn listing. Architect's call, taken from the case written up at U8.6e | 6.1 |
| Aug 30, 2026 | U8.6e | **A precondition replaces the gate rather than nothing.** `agents/critic._cross_check_ran` — I1 and I3 are statements *about* the comp cross-check, so they now require it to have produced a median: below `RENT_COMP_CROSSCHECK_MIN_COMPS` surviving comps `valuation_rent._cross_check` returns without one, and an objection there would describe a comparison the report does not contain. The one place the Critic reads `valuation_detail` rather than flags alone. Both objection texts reworded, since each asserted a disagreement that may now not exist | 6.1 |
| Aug 30, 2026 | U8.6e | **Batch re-run and artifacts regenerated.** `eval/results/results.md`, `eval/results/sensitivity.md` — four golden/replay rows move and one live row does; verdict agreement holds at 18/21 (one mismatch clears on `la-three-bedroom-comp-drift`, one opens on `chicago-uptown-band-over` and is *not* edited away), regression against U7.8 goes 7/7 to 6/7, coverage stays 30 of 30. **No re-record was needed** — the expectation that objection text reaches the scoring prompt was wrong, since the Critic runs after `scenario_forecast`. Live tier re-run on the unchanged build first, so its ±1-row noise is not confused with the change | 6.1 |
| Aug 30, 2026 | U8.6e | **Tests follow the new contract.** `tests/test_critic_interactions.py` — a degraded comp set now objects where the numbers agreed, spatial concentration alone still raises nothing, and no objection is made about a cross-check that never ran; the shared fixture builder gains a `cross_checked` switch. `tests/test_flag_propagation.py`'s Valuation stub now returns a `comp_implied_rent_median` alongside its divergence flag. 76 pass | 6.1 |
| Aug 30, 2026 | U8.7 | **The stated-rent gap measurement is a committed script.** `scripts/stated_rent_gap.py` — reproduces the 13-fixture distribution (mean −11.4%, median −9.7%, range −39.4% to +66.6%) that had existed only as prose, prints beside each gap the flags the report already raised, and computes what re-calibrating `demo_deals.rent_basis` to the market index would produce. De-duplicates on the fixture's `DealTerms` identity, since `la-stale-rent-index` is `la-ordinary-duplex` with a fault injected | 6.1, 7.1 |
| Aug 30, 2026 | U11.5 | **Two false claims removed from the report a reader sees.** `agents/summarizer._rent_basis_section` — the paragraph explaining how the rent figure was reached still said *"A linear regression on bedrooms, bathrooms and square footage"* (gradient boosting since #18) and *"On a held-out slice"* (the single 20% split #18 replaced with k-fold cross-validation plus a full-data refit). The second understated the evidence rather than overstating it: every row is scored once by a fold that never saw it, so the per-metro n is the market's full row count. Pulled ahead of U8.10 because it is the only item on U11.5's list a demo audience can see. Model description reaches no prompt, so no re-record | 6.1 |
| Aug 30, 2026 | U8.7 | **The demo listings' rent basis is stale, and says so.** `demo_deals.py` — the module docstring's "Why FMR and not the corpus for rents" reasoning was superseded by #19, and these rents are now calibrated against a benchmark the system uses only for the bedroom step. `chicago`'s sit ~25% below Logan Square's market index; the other four are within 10%. **Retained as committed rather than re-calibrated**, on `staten-island`'s precedent — nothing computes from a stated rent, so a stale basis makes a listing less lifelike without making the system wrong — with the staleness stated in the docstring and in the Chicago deal's note, plus what would change the answer | maintenance |
| Aug 30, 2026 | U8.7 | **Two negative results, recorded rather than assumed.** First: re-calibrating the demo deals to the market anchor makes their gap `1/ratio − 1` by construction — −5.9% on three deals sharing a unit spec — so the six independent observations that path promised do not exist. Second: every fixture a 20–35% threshold would fire on already carries a flag naming a more specific cause (6 of 6 at 25%), so the emphasis would restate an existing disclosure in vaguer words. `agents/critic._consistency_objections`' docstring corrected — it still claimed the dead ~−29% offset and carried `TODO(U8): promote both once Zillow ZORI settles which baseline is right`. ZORI has settled it | 6.1 |

---

## Aug 30, 2026 — U8.8: the market benchmark stops being metro-wide

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 30, 2026 | U8.8 | **Sub-metro sale-price ingest.** `scripts/build_sale_benchmarks.py`, the committed `tools/data/zip_sale_benchmarks.json` (304 ZIPs, 28 KB) and `tools/sale_benchmarks.py`, plus the `SALE_BENCHMARK_*` block in `config.py`. New York comes straight from NYC Open Data `w2pb-icbu` (27,309 qualifying sales, 164 ZIPs); Chicago joins Cook's sales table `wvhk-k5uv` to the parcel universe `nj4t-kc8j` on an exact `pin` (18,251 sales, **96.8% joined**, 140 ZIPs). Committed rather than fetched at runtime for `EVAL_RECORDINGS_DIR`'s reason — a fresh clone must reproduce the published figures without a municipal portal being up. **Q3's unbounded address-to-parcel join never appears**: a benchmark needs a median over the ZIP, never the subject's parcel | 6.1, 7.1 |
| Aug 30, 2026 | U8.8 | **Three filter decisions taken on measurement.** Filtering New York on unit count alone admits 125 sales labelled ONE FAMILY DWELLING plus vacant land and garages carrying a unit count through a data-entry artifact, so `SALE_BENCHMARK_NYC_CATEGORIES` exists; Cook's own non-arm's-length screens are used rather than an invented floor (20,369 class-211 sales since 2023, **18,335 pass all four**); and `SALE_BENCHMARK_MIN_SALES = 20` is set on the measured per-ZIP distribution (NY median 131, Chicago median 34) rather than picked. The two markets' definitions of multi-family differ irreconcilably — NY 2-4 units, Cook class 211 at 2-6 — so the definition travels with each figure into the report | 6.1 |
| Aug 30, 2026 | U8.8 | **The benchmark prefers the local tier and says which it used.** `ValuationDetail` gains `benchmark_tier` and the ZIP tier's own figures; `valuation_rent._attach_benchmark` resolves the metro figure unconditionally and overwrites the headline with the ZIP median where one exists, keeping the metro one beside it; `summarizer._benchmark_section` renders either tier with its sample size, window, issuing office and definition, and prints the neighborhood-versus-metro contrast. `_resolve_subject_zip` extracted so the rent anchor and the price benchmark cannot describe two different places. **Raises no flag, deliberately** — nothing computes from the benchmark, so its grain is not doubt about the deal's numbers; that argument is recorded at the site and expires if check B is ever promoted | 6.1 |
| Aug 30, 2026 | U8.8 | **Verified verdict-inert and recording-safe.** Both offline eval tiers re-run and byte-identical to the pre-change tables — no `CacheMiss`, same confidence, same verdicts, same 30-kind coverage. The spike had claimed this from reading `_context_block`; it is now measured. Side effect worth naming: the demo listings now read 30-40% below their own ZIP medians, which is #11 having set their asking prices from the metro median, made visible rather than introduced | 6.1 |

---

## Aug 30, 2026 — U8.6c: the forecast search's own scores reach the reader

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 30, 2026 | U8.6c | **The evaluator's per-scenario score is rendered.** `agents/summarizer.py` — `Scenario.evaluator_score` had been populated since U6 and carried on state, and no reader had ever seen it: the search's own judgment of each surviving hypothesis was computed and invisible. Each scenario now renders as `**Base** *(scored 0.80)* — rationale`, beside the branch ledger's existing `id (score) — summary` for the ones that lost, with one paragraph stating what the number is, that the labels come from projected outcome and not from it, and that a single draw of this judge is not a rank (OQ-17) | 6.1 |
| Aug 30, 2026 | U8.6c | **The rank line that decides the reported scenario set is measured.** `tools/tot.py` — `SearchResult.cut_boundary_gap_by_depth` records, per level, the margin at the line the beam width cut on (last survivor minus best discarded), which is a different question from `score_gap_by_depth`'s comparison of #1 against #2 *among survivors*. At depth 2 it governs which pairings reach the report at all — the one comparison at that level whose outcome the reader sees | 6.1 |
| Aug 30, 2026 | U8.6c | **A cut taken inside the tie threshold is disclosed, and the common case is the tie-break's.** `agents/scenario_forecast.py` — `FORECAST_BRANCHES_NEAR_TIED` at INFO when the depth-2 cut margin falls inside `TOT_TIE_EPSILON`. Measured across the golden batch, the margin is often **zero or negative**: the discarded pairing outscored the one reported and lost on `tot._rank`'s conservatism preference. `chicago-uptown-band-under` shows both halves in one report — the ledger prints `f-01-optibase` at 0.60 discarded while the reported pessimistic scenario scored 0.55. The message branches on the sign rather than claiming a margin it does not have. Fires on 6 of 15 golden and 3 of 6 replay cases; INFO costs 0.00, so no confidence, verdict or coverage figure moves | 6.1 |
| Aug 30, 2026 | U8.6c | **Tests for both.** `tests/test_forecast_tie_disclosures.py` — the cut margin recorded distinctly from the top-two gap, a cut inside a tie group recording a non-positive margin, both message branches, silence on a decisive cut, and the two rendering cases. Kept out of `test_flag_propagation.py` on `test_stated_rent_disclosure.py`'s precedent, since an INFO disclosure decides nothing. 73 pass | 6.1 |
| Aug 30, 2026 | U11.M | **The rent model's module docstring still described the anchor U11.3 retired.** `tools/model/rent_model.py` — U11.3's rename pass reached the flag kinds, the `ValuationDetail` fields and the Summarizer's basis line, but not this file's own header, which still said rent is divided by the county FMR and multiplied by today's FMR, and that `tools/rent_drift.py` corrects the drift. Both false since U11.3; the module whose docstring carries the design reasoning was describing the previous design. Rewritten for the hybrid, with the stability assumption restated as what it now is — narrower, cleared at a floor by `scripts/anchor_stability.py`, and open as OQ-19 | maintenance |

---

## Aug 30, 2026 — U11.1: the model-form probe

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 30, 2026 | U11 | **Model-form probe.** `scripts/model_form_probe.py` — LinearRegression vs RandomForest vs GradientBoosting on the identical feature set and FMR-ratio target under k-fold cross-validation, which is the proper-validation condition §6 cut-list 1a attached to reopening model form. Reports cross-validated MAE with fold spread and each candidate's train-vs-holdout gap, per-metro error over the same `INDEXED_MARKETS` grouping the shipped figures use, and every eval fixture run through the **real Valuation agent** with each candidate swapped in — so a flag-set change reported here is one the pipeline would produce. Writes nothing: the bundle is patched at `rent_model.load`, never onto `config.RENT_MODEL_PATH` | 6.1, 7.1 |
| Aug 30, 2026 | U11 | **Gradient boosting adopted; cross-validation replaces the single split.** `config.RENT_MODEL_ESTIMATOR` / `RENT_MODEL_CV_FOLDS`, `rent_model._estimator` and a rewritten `train()`. CV MAE $513.67 → **$450.71** (−12.2%), fold sd 13.51 → 7.29, at a train/holdout gap of $18.34 against random forest's $140.41 — the overfitting risk cut-list 1a's deferral named, which is why the better-scoring form was not the one taken. The artifact is now refit on **all** 5,686 rows; the single-split version shipped a model fit on 80% and discarded the rest. `TrainingReport` gains `cv_folds`, `train_mae_dollars` and `feature_importances` beside `coefficients`, since an ensemble exposes one and a linear model the other | 6.1, 7.1 |
| Aug 30, 2026 | U11 | **The competence check moved from the model's output to its input.** `rent_model.subject_is_out_of_domain`, `TrainingReport.feature_ranges` / `sqft_per_bedroom_bounds`, `config.RENT_MODEL_DOMAIN_PERCENTILES`, and a new branch in `valuation_rent` ahead of the prediction. The old refusal fired because LinearRegression extrapolated an implausible *ratio*; a tree-based form averages training targets already inside the plausible band and cannot, so a 2bd / 100,000 sqft subject priced at 62.21 (refused) under the old form prices at 2.20 (reported) under the new one. Guarded on sqft-per-bedroom rather than per-feature ranges, because 5,000 sqft sits inside the corpus's own 130–9,175 span and only the *combination* is abnormal. Measured bounds travel with the artifact; the percentiles that place them are the tunable | 6.1 |
| Aug 30, 2026 | U11 | **Anchor probe.** `scripts/anchor_probe.py` — five candidate reference series (FMR, ZORI, ZORI×FMR-bedroom-shape, FMR-with-ZORI-fallback, FMR-where-ZIP/ZORI-where-county) scored under one CV with the shipping estimator, in dollars on the rows all five can price, since a ratio to FMR and a ratio to ZORI are not comparable quantities. Reports per-metro and splits the ZORI anchor's error by whether the row was priced at ZIP or county tier | 6.1, 7.1 |
| Aug 30, 2026 | U11 | **ZORI county tier.** `tools/zori.county_medians` and `scripts/zori_county_tier.py` — per-county monthly median ZORI with the ZIP count behind each median, re-pricing §6 cut-list item 6's "drops 27% of the training rows". The gap is a series-start effect (1,515 of 1,539 rows are ZIPs present in ZORI but not yet observed at the corpus vintage), and a county tier recovers 99.0% of it, taking coverage to 99.7% | 7.1 |
| Aug 30, 2026 | U11.3 | **The anchor re-based on ZORI, with FMR kept only for the bedroom step.** `rent_model.AnchorTables` / `build_anchor_tables` / `bedroom_shape` / `anchor_for_row` rewritten, `zori.panel` / `county_median_tables` / `latest_month`, `config.RENT_ANCHOR_*`. The model now learns `rent / (ZORI at the row's own listing month x the FMR schedule's ratio between unit sizes)`, with the ZORI term falling back to the county median where a ZIP's series has not begun. Retrained: CV MAE $452.40, **New York $981 → $855, Chicago $454 → $343**, Los Angeles $450 → $509. Removes the whole Small Area FMR / back-cast apparatus from the anchor path | 6.1, 7.1 |
| Aug 30, 2026 | U11.3 | **The rent-drift correction retired, structurally rather than by deletion.** U8.4b scaled the estimate and the comp-implied figures by a measured ZORI-vs-FMR factor; the anchor is now a market index read at the same month on both ends, so the schedule-versus-market gap is divided out where it arises. `valuation_rent` no longer calls `tools/rent_drift.py`, `_cross_check` no longer takes a factor, and the disclosure it replaced becomes a **staleness** flag on the index's newest observation. `tools/rent_drift.py` is unused as of this change and is scheduled for retirement at U11.M | 6.1 |
| Aug 30, 2026 | U11.3 | **The flag suite gains a ZORI stub, since every estimate now reads the panel.** `offline_valuation` builds a synthetic monthly index spanning the corpus vintage, with the 2019 level set to the fake county schedule times the 1.186 ZORI/FMR ratio U8.0 measured, so a comp normalizes against a market read rather than an administrative one. Two cases retired with the mechanism they tested (the unavailable-correction path and the correction's symmetry property); 70 tests pass | 6.1 |
| Aug 30, 2026 | U11.3 | **Rent-drift correction dropped; the staleness question kept and made coverable.** `tools/rent_drift.py`, `tests/test_rent_drift.py` and `config.RENT_DRIFT_*` removed; `FlagKind.RENT_DRIFT_CORRECTION_APPLIED` retired on the `LLM_RENT_FALLBACK_USED` precedent (a kind nothing can raise corrupts the census) and `RENT_DRIFT_CORRECTION_UNAVAILABLE` renamed to `RENT_ANCHOR_INDEX_STALE`, with `ValuationDetail.anchor_index_month` / `anchor_index_staleness_months` replacing the five `rent_drift_*` fields. The new kind is unraisable by any listing — the committed index is one month old — so `Fault.STALE_RENT_INDEX` pins `zori.latest_month` back and `la-stale-rent-index` covers it. `EvalCase`'s fault guard is scoped to the two extraction-path faults, since it would otherwise reject the only case shape that can reach this seam | 6.1 |
| Aug 30, 2026 | U11.3 | **Anchor-stability tests.** `scripts/anchor_stability.py` — falsification tests for the assumption the re-anchored model rests on: that a property's rent *relative to its ZIP* is stable over the ~7 years between the corpus and today. A naive out-of-time split reported +11.8%, which the script's own composition check showed was mostly geography — the corpus's metro mix swings **51 percentage points** across its window. Held geography constant by splitting within each metro: **+3.6% pooled**. The monthly ratio moves **6.3%** peak-to-trough over the four months carrying 97% of the rows; the unfiltered 46.9% figure was a median over months with as few as three listings | 6.1, 7.1 |
| Aug 30, 2026 | U11.3 | **The report stopped calling the rent anchor a Fair Market Rent figure.** `agents/summarizer.py` — the basis line rendered `ratio 1.06 x FY2026 FMR $2,691 (ZIP 90026)`, which named a source that had stopped supplying the number a commit earlier. Now `ratio 1.06 x market rent $2,691 (ZIP 90026, as of 2026-07-31)`. The *other* FMR sentence in the same file is correct and was left alone: the forecast's rent-growth bands still genuinely come from HUD FMR history (#16), and only the estimate's anchor moved | 6.1 |
| Aug 30, 2026 | U11.3 | **Three flag kinds renamed off FMR, and the fourth deliberately kept.** `state.py` — `RENT_ANCHORED_TO_FMR` → `RENT_ANCHORED_TO_MARKET_INDEX`, `FMR_UNAVAILABLE_FOR_COUNTY` → `RENT_ANCHOR_UNAVAILABLE`, `FMR_ANCHOR_COUNTY_LEVEL` → `RENT_ANCHOR_COUNTY_LEVEL`, with `ValuationDetail.fmr_resolution`/`fmr_zip`/`fmr_year` → `anchor_tier`/`anchor_zip`/`fmr_shape_year` and `DealState.rent_estimate_ratio_to_fmr`/`fmr_anchor_used` → `rent_estimate_ratio_to_anchor`/`rent_anchor_used`. `FMR_BEDROOM_CAP_EXCEEDED` keeps its name: the four-bedroom ceiling really is a property of the federal schedule the hybrid anchor still reads. Mechanical, but it changes `f.kind.value`, which the results table prints and the forecast prompt embeds — so it lands **before** the re-record rather than after | 6.1 |
| Aug 30, 2026 | U11.3 | **Divergence attribution was comparing two different anchors and printing them as one.** `scripts/valuation_evidence.py` — `--diagnose-divergence` normalized the metro population against FMR while the comp set beside it was already on the hybrid anchor, so the three ratios it prints as "normalized identically" were not. Both baselines now read straight off `build_training_frame`, which makes a second normalization impossible rather than merely correct; the subject's own anchor resolves through `anchor_for_row` as the agent resolves it. The conclusion the script exists to support is unchanged — the divergence is the model's — but its stated *reason* was corrected in `valuation_rent._cross_check` too: the model is no longer location-blind below the county, it is blind below the ZIP | 6.1, 7.1 |
| Aug 30, 2026 | U8.6b | **Straddle-pair siting probe.** `scripts/straddle_probe.py` — runs the real comp-retrieval and Valuation agents (the two that produce every quantity a per-flag threshold is compared against, and the two that make no model call) over a grid of candidate subjects, printing each threshold's own input beside it. Exists because U8.6's sweep found the confidence parameters sit in a dead zone, so the verdict-deciding lines are the *per-flag* thresholds and the pairs have to be sited against measurements | 6.1, 7.1 |
| Aug 30, 2026 | U8.6b | **Six straddle fixtures, three clean pairs and one deliberate failure.** `eval/data/golden_fixtures.py`, `eval/cases.py` — comp-drift band (Chicago 1,100 sq ft at 2-of-8 vs 1,300 at 3-of-8), rent-comp divergence (Cleveland 1,000 sq ft at −30.8% vs 1,050 at −28.9%, the tightest in the batch — a 5% floor-area change flips it), and spatial concentration (Bed-Stuy's 8 comps on 1 coordinate vs Hell's Kitchen's 8 on 5, which needed no engineering at all). **`MIN_QUALIFYING_COMPS` is not independently straddleable and the case says so:** a 144-point grid across four metros returned 8 comps at 98 points and 7 at none, and the one real 7-comp siting (northern Bronx) necessarily carries the whole relaxation ladder's disclosures because reaching 7 means running it to the end | 6.1, 7.1 |
| Aug 30, 2026 | U8.6b | **`chicago-uptown-duplex` retargeted rather than re-engineered.** `eval/cases.py` — its target flag stopped firing under the new anchor: +46.6% divergence against FMR, **−6.1%** against the market index, on a listing nothing was bent in. Re-sizing it to make the flag fire again would have destroyed the only property that made it worth having, so it becomes the batch's second control and the 53-point move is recorded as the clearest single statement of what U11.3 did. The divergence kind stays covered by three other cases | 6.1 |
| Aug 30, 2026 | U8.6b | **Two stale baselines in the fixture module corrected.** `eval/data/golden_fixtures.py` — "every LA fixture carries the county-level anchoring warn" and "Chicago is the one county with ZIP-resolution anchoring" were both true under FMR and both false under the market-index anchor, which resolves at ZIP tier in all four markets. Annotated rather than deleted: a stale baseline is how a batch quietly stops measuring what it claims to | 6.1 |
| Aug 30, 2026 | U8.6d | **Disclosures classified by what they are *about*, not only by how much they cost.** `state.FlagScope` / `scope_of` — four kinds (`rent_anchor_county_level`, `rent_estimate_market_error_elevated`, `rent_anchor_index_stale`, `comps_spatially_concentrated`) describe a *market* and fire identically for every listing in it; everything else describes this listing or this run. Enumerated rather than derived, with DEAL as the default so an unclassified new kind reads as something a reviewer might fix rather than something they cannot. **Reporting only** — the two-score alternative was measured and rejected because market coverage propagates into the deal's own numbers, and the enum's docstring carries that constraint so it is not re-litigated by a later reader | 6.1 |
| Aug 30, 2026 | U8.6d | **The confidence score shows its arithmetic.** `state.ConfidenceBreakdown`, `critic.confidence_breakdown`, `DealState.confidence_detail`, and a line under the score in the report: *"0.30 deducted from a starting 1.00: 0.00 from this property, 0.30 from how much is known about this market."* `confidence_from_flags` keeps its signature and delegates, so routing reads the identical number it always did. Measured on `ny-bedstuy-triplex`, the fixture the decision was written about: 0.70, entirely market-scoped, none of it about the building | 6.1 |
| Aug 30, 2026 | U8.6d | **The escalation sentence names its dominant ground.** `critic._review_guidance` — a reviewer handed *"the listing never stated a price"* has something to do; one handed *"no ZIP-level rent index covers this county"* does not, and until now the report did not distinguish them. The largest single deduction's own text is quoted, since it was already written for this reader; no flag names or thresholds reach the sentence (§8) | 6.1 |
| Aug 30, 2026 | U8.6d | **Report disclosures grouped into "About this property" and "About our coverage of this market".** `agents/summarizer._flag_section` — severity still orders within each group and every flag is still rendered in full; what changed is that a reader can see at a glance which half of the list is about the building in front of them. Property-scoped prints first, because that is the half they may be able to act on | 6.1 |
| Aug 30, 2026 | U8.6d | **The Critic stopped telling every reader it was unbuilt.** `agents/critic.py` — it returned `stub_nodes: [AGENT]` from U2, when half of it genuinely was a stub, and U7 completed it without removing the declaration. Every report published since opened with *"Provisional build … `critic` … did not produce their full output"*. `test_report_discloses_stubbed_agents` had been asserting the defect, so it is rewritten to exercise the banner against a state that actually declares a stub, and a companion test now checks the whole set rather than one agent by name — which is why the stale claim survived four units | 6.1 |
| Aug 30, 2026 | U8.6 | **Confidence sensitivity sweep.** `scripts/confidence_sensitivity.py` — re-scores every predicted case's verdict over a grid of thresholds and severity weights and writes `eval/results/sensitivity.md`. Measures **robustness, not optimality**, which is the only question the batch can answer honestly: the cases were written knowing the shipped values, so scoring agreement would measure the fixtures. Re-scoring delegates to the real `critic.confidence_from_flags` with `config` patched, rather than reimplementing the arithmetic, so a tuning script cannot come to be measuring something the system does not do. Also sweeps the critical weight to confirm it is behaviorally inert, and settles OQ-1's causal-pair question | 6.1, 7.1 |
| Aug 30, 2026 | U8.7 | **The ~−29% stated-versus-modelled gap was a property of the old anchor, and it is gone.** Re-measured across the 13 fixtures carrying independently-set rents: **mean −11.4%, median −9.7%, range −39.4% to +66.6%** — dispersed and sign-varying, where it was a near-constant on all three demo listings. `config.RENT_CLAIM_DIVERGENCE_DISCLOSURE_THRESHOLD`'s comment and `summarizer._stated_rents_section` corrected, and **the report's direction-dependent caveat rewritten**: it argued from the dead 40th-percentile offset in the reader's own text. It stays direction-dependent for a narrower true reason — a sitting tenant's rent lags the market, an above-market claim has no benign default explanation. The threshold ships at `None` unchanged; whether to set it, delete it, or promote the comparison to an objection is now a live decision where it was a settled veto | 6.1 |
| Aug 30, 2026 | U8.6e | **The rework cycle stopped being reachable, and only the coverage census noticed.** `critic._interaction_objections` returns early unless `rent_diverges_from_comps` fired this pass — sound as written, invisible while divergence was common, and load-bearing once the hybrid anchor made it rare. I3 is the only *retryable* objection in the system, so with the gate closed `chicago-geocoder-outage` returned 0 reworks and a clean report while still passing every assertion but its own target, and `rework_limit_reached` — the bound §3 requires every cycle to carry — dropped out of the batch. Fixed by re-siting the case's `geocoder_fallback_override` to 41.900000, -87.740000, found by sweeping a 49-point Chicago grid for a fallback that diverges and raises nothing else (+56.1%, 8 comps, 3 locations, exactly one info and one warn). Re-recorded: 2 reworks, escalates on the budget rule | 6.1, 7.1 |
| Aug 30, 2026 | U8.6e | **A Critic objection was telling readers something the anchor change had made false.** `agents/critic.py` I3 argued that a centroid fallback moves the comp set but not the estimate, "because the model is location-blind below the county" — and said so in the report. The hybrid anchor reads the market index at the subject's ZIP, so a fallback moves the anchor and the estimate with it. Both sides of the comparison now shift, and the objection says that narrower true thing instead. `la-three-bedroom-comp-drift`'s `critic_inconsistency` target withdrawn for the same root cause, with its declared verdict **left mismatching on purpose** — re-declaring a prediction after seeing the run is what `VerdictSource` exists to prevent | 6.1 |
| Aug 30, 2026 | U8.6e | **The replay tier was not reproducible, and the cause was upstream of everything it records.** `tools/geocoding.py` gains a disk cache and `config.GEOCODE_CACHE_PATH` commits it beside the LLM recordings. `LLM_CACHE_MODE=replay` covers *model* calls; the Census Geocoder is an ordinary HTTP request, so a timeout correctly raised `geocoder_service_unavailable`, that flag joined the set the forecast's evaluator prompt embeds, and the recorded response for it no longer existed — surfacing as a `CacheMiss` reading like a deliberate prompt change. Roughly one case per batch run and **a different case each time**, which sent the first investigation hunting cross-case state leakage instead of a network flake. Verified: five consecutive full replay runs, clean after the cache warms. **A timeout is never cached** — only outcomes the Census returned — since freezing a transient outage into a permanent one would erase the distinction the retryable/non-retryable flag split exists to carry. The module's original "no disk cache" note was right about cost and silent about determinism, which is the reason that arrived later | 6.1, 7.1 |
| Aug 30, 2026 | U8.6 | **Batch re-recorded and re-derived against the new model, anchor and flag names.** `eval/data/llm_recordings/`, `eval/results/results.md` — 28 cases, **no errors, 18/21 predicted-verdict agreement, 7/7 against U7.8's baselines, and 30 of 30 flag kinds raised** with none uncovered and none unreachable. Five rows carry † (escalated on a critical while the score alone would have reported) and one carries ‡ (escalated on the rework budget), so both independent escalation grounds are shown by rows rather than asserted. All three mismatches triaged as prediction-derivation gaps rather than threshold evidence | 6.1, 7.1 |
| Aug 30, 2026 | U8.6 | **The demo table re-derived, and Los Angeles became the demo set's first perfectly clean run.** `docs/demo.md` — LA 0.85 → **1.00** with zero warnings, Chicago 0.70 → 0.85, overpriced 0.70 → 0.85, coord-conflict 0.45 → 0.60. This document had argued that "a perfectly silent run doesn't exist in this system by design", reasoning from the county-level anchoring warn every LA deal paid; the market-index anchor covers 90026, so the warn stopped firing because the condition stopped being true. **New York's standing-warn floor moved for the third time** — three warns, then two after U8.4c, now **one** — and its per-metro error is re-quoted at $855 against $452 (1.9x) throughout | 6.1 |
| Aug 30, 2026 | U8.8 | **Sub-metro sale-price benchmark: feasibility established for two metros of three.** NYC (`w2pb-icbu`) carries ZIP, coordinates, unit counts, building class and sale price in one table; Cook joins sales (`wvhk-k5uv`) to parcels (`nj4t-kc8j`) on an exact `pin`. **Los Angeles County publishes no sale price** — the roll carries Prop 13 assessed values only — so it keeps the Redfin metro median, disclosed. Q3's feared address-to-parcel join is not on the critical path: a benchmark needs an aggregate over the subject's ZIP, not a match to its parcel | 6.1 |

## Aug 29, 2026 — U8.4c / U8.6c: a stale assumption removed, and a flag split in two

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 29, 2026 | U8.4c | **"Redfin doesn't cover New York" was this build's own filter, not a fact about Redfin.** `tools/redfin_data.py` — `TARGET_METROS` was a trio-only mapping written before New York was indexed and never revisited, so `agents/valuation_rent._attach_benchmark` reported its output as a coverage fact. Checked against the raw extract: **"New York, NY metro area" is present with 102 fully-populated months at 700–950 multi-family sales each.** Membership moved to `config.REDFIN_TARGET_METROS` (§8 — it predated config.py and never migrated), New York added, and asserted equal to `INDEXED_MARKETS`' label set so the price series' reach cannot silently diverge from the market list again; `load_redfin` now raises on a configured region missing from the file instead of returning an empty frame that reads downstream as absent coverage. Same defect class as U8.2b: "the source publishes nothing" and "this system never looked" read identically in a report | 4.1, **6.1** |
| Aug 29, 2026 | U8.4c | **One market resolver instead of two, and the weaker one was wrong.** `agents/valuation_rent.py` — `_resolve_market_label` is now the single matcher for both the Redfin benchmark and the per-metro error figure, over `config.INDEXED_MARKETS` with state + word-boundary city matching. The benchmark previously matched a city against the metro *label* alone with no state check, which no borough name could ever satisfy — so a Brooklyn subject read as outside the New York market. That was the second half of the New York misdiagnosis. `src/demo_deals.py` — `staten-island`'s note corrected: its $875,000 was set without a benchmark because of the stale belief, and now measures ~11% below the metro median (~$981K), so the figure stands as committed rather than being recalibrated | 4.1, **6.1** |
| Aug 29, 2026 | U8.6c | **`FORECAST_BRANCHES_NEAR_TIED` was two disclosures sharing one kind; the pairing half is now INFO.** `agents/scenario_forecast.py` — a *framing* tie (depth 1) stays WARN: `TOT_FRAMING_BEAM_WIDTH` is 1, so the losing reading of the data is discarded and the whole forecast rests on a tie-break. A *pairing* tie (depth 2) is INFO: `TOT_BEAM_WIDTH` is 3, so both tied pairings appear in the reported scenario set, labels come from projected outcome rather than score rank, and band provenance comes from the framing they share — nothing a reader sees turns on which led. It was also measured by a scorer whose repeat-run variance exceeds the 0.05 tie threshold by an order of magnitude (OQ-17), so the 0.15 penalty was pricing model noise as deal doubt. Both messages now state that one-sample caveat. **Consequences, both real:** the `chicago` demo moved 0.55/escalates → 0.70/reports and was re-baselined, costing the demo set its one "escalates on accumulated warns alone" case; and `chicago-geocoder-outage` recovered its rework path (the near-tie had been front-loading escalation before the budget could be spent), restoring 2 reworks | **6.1**, report |
| Aug 29, 2026 | U8.4c | **Batch re-derived once for U8.4b + U8.4c + U8.6c together.** `eval/` — recordings re-cut, `eval/results/results.md` regenerated. **Coverage 31/31 kinds** (both new drift kinds covered; `staten-island` supplies the unavailable path organically, no engineered case needed). Baselines 6/7 — `chicago` re-baselined above, deliberately. Predicted verdicts 10/14: the two prior tuning signals plus `la-unpriced-triplex` and `la-duplex-near-usc`, which now pay `rent_drift_correction_unavailable` because ZIPs 90089 and 90007 are a USC campus ZIP and its neighbour, outside or later than ZORI's coverage. Both kept as declared mismatches rather than re-sited to covered ZIPs — re-siting would make the batch systematically avoid the ZIPs where the system degrades, the same "calibrated to run clean" failure the demo deals are criticized for. `chicago-uptown-oversized`'s note corrected: it no longer isolates the critical-flag rule from the score (the drift-corrected prompt empties the ToT beam, adding a second critical), so U7.8's request for a listing-reachable case for that rule is open again — `chicago--no-retrieval` still carries the evidence | **6.1**, report |
| Aug 29, 2026 | OQ-18 | **A replay row missed its recordings once and the cause was not found — recorded rather than dismissed.** `docs/open_questions.md` — three replay cases raised `CacheMiss` in one full batch and have passed in every run since. Ruled out by direct test: live-tier contamination (each live case run ahead of the failing case — all passed), golden-tier contamination (same, all passed), and the Census geocoder (20 rapid calls, 0 failures; 6 repeat calls per address returning identical coordinates and ZCTAs). Left open because U8.4b newly coupled replay reproducibility to ZIP resolution — the scoring prompt embeds the rent estimate, which now depends on the drift factor, which depends on the subject's ZIP | maintenance |

## Aug 29, 2026 — U8.4b (landed late): the rent-drift correction

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 29, 2026 | U8.4b | **Per-subject rent-drift correction, U8.0's consequence, landed out of order.** `tools/rent_drift.py` — the subject ZIP's `(ZORI today / ZORI vintage) × (FMR vintage / FMR today)`, both FMR ends at the anchor's own grain and bedroom count, refusing on a grain mismatch between fiscal years, a vintage substitution past `config.ZORI_MAX_VINTAGE_SUBSTITUTION_MONTHS`, or a factor outside the new `config.RENT_DRIFT_FACTOR_MIN/MAX` band; provenance returned as data, flags built by the agent. `agents/valuation_rent.py` applies it to the estimate **and, symmetrically, to the comp-implied p25/median/p75** — both are a vintage ratio times today's FMR, so correcting one side would have injected a ~−12% false gap into `RENT_DIVERGES_FROM_COMPS`; the factor cancels out of `divergence_pct` exactly, asserted by test. Two new kinds: `RENT_DRIFT_CORRECTION_APPLIED` (INFO) and `RENT_DRIFT_CORRECTION_UNAVAILABLE` (WARN). Measured at landing: factors 0.744 (LA 90026) to 0.934 (Bed-Stuy 11216) — wider than U8.0's ZIP-anchored-only range, because county schedules drifted hardest; Staten Island's 10307 has no ZORI coverage, so the demo set covers the WARN kind organically. Live LA run: estimate $4,073 → $3,031, verdict unchanged, stated-vs-modelled gap −29% → −4%. Batch re-derivation deferred to U8.4c so recordings are re-cut once | **6.1**, report |
| Aug 29, 2026 | U8.4b | **Two reader-facing sentences the correction made false, corrected with it.** `agents/valuation_rent.py` — `RENT_ANCHORED_TO_FMR` claimed the stability assumption was "one nothing in this project verifies"; U8.0 verified it and found drift, so the sentence now branches on whether the correction applied. `agents/summarizer.py` — the stated-rent caveat said an independent market-rent series "is named as future work"; it is now applied where coverage allows, and the caveat says so (full re-measure of that comparison stays at U8.7). `tests/test_rent_drift.py` added; `offline_valuation` stubs the drift seam so the suite is hermetic against the ZORI file's presence; all 71 tests pass | **6.1**, report |

## Aug 29, 2026 — U8.5: pass-scoped flags

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 29, 2026 | U8.5 | **Every `Flag` stamped with the pass that raised it.** `state.py` — `Flag.planner_invocations` (default 0, a sentinel no real pass ever uses); `DealState.flag()` bound method auto-stamps `self.planner_invocations` for the ~40 call sites that already hold `state`; the free `flag()` function now requires `planner_invocations` explicitly rather than defaulting it, so a helper that forgets to thread it fails loudly instead of silently mis-stamping. Threaded as an explicit parameter through the four helpers with no `state` in scope: `valuation_rent._attach_metro_error`, `extractor._resolve_geography`, `scenario_forecast._disclosure_flags` and `_distinctness_flags`. Corrects Q4's own estimate: of the six helpers it named, `_cross_check` and `_extraction_failed` already receive `state` and needed no signature change, and `expand` never calls `flag()` at all — only three (now four, `_attach_metro_error` postdates Q4) actually needed the threaded parameter | 6.1, report |
| Aug 29, 2026 | U8.5 | **The Critic reads only the current pass (OQ-15).** `agents/critic.py` — `_kinds` now judges each source agent on its most recent examination: an agent in `state.plan` this pass is read from this pass's flags alone (superseding, not accumulating, its earlier ones); an agent absent from `state.plan` — skipped, not cleared — carries forward its flags from the last pass it ran. A hand-built state with no `state.plan` (every case in `test_critic_interactions.py`) falls back to the pre-U8.5 behaviour of reading every accumulated flag, so those tests are unaffected. Clears `critic.py`'s `TODO(U8)`. `agents/planner.py` — `_geocode_is_worth_retrying` filters to `planner_invocations == state.planner_invocations` so a resolved geocode stops re-triggering extraction on later laps; clears `planner.py`'s `TODO(U8)`. New tests in `test_critic_interactions.py` (the stale-objection scenario the old docstring named, the skipped-agent carry-forward, and the supersession case that motivates it) and `test_flag_propagation.py` (the geocode-staleness regression), since OQ-16 means the eval batch still contains no rework lap to exercise this — `tests/` is the only place it is asserted. All 64 tests pass; full eval batch re-run with no change to any published row (7/7 baselines, 12/14 verdicts, 28/29 coverage) | 6.1, report |
| Aug 29, 2026 | U8.5 | **A prior pass's centroid fallback was being read back as caller-supplied coordinates.** `agents/extractor.py` — `_supplied_coordinates` now returns `None` on any pass after the first, since a rework only ever happens through I3's `GEOCODER_SERVICE_UNAVAILABLE` objection, the only retryable one that exists, and that flag is only ever raised when no caller supplied coordinates to begin with — so anything present on a later pass is necessarily this pipeline's own prior fallback, never a caller's. Left unfixed, the second pass silently swapped `GEOCODER_SERVICE_UNAVAILABLE` for the non-retryable `GEOCODING_UNAVAILABLE`, both breaking `_geocode_is_worth_retrying`'s continued-retry check and misdescribing system-derived coordinates as caller-given in the report. Found by driving a rework through the real graph while building the OQ-16 case below — the first time this had ever happened in the project | 6.1, report |
| Aug 29, 2026 | U8.5 | **OQ-16: richer fault injection closes the last uncovered flag kind.** `eval/cases.py` — `EvalCase.geocoder_fallback_override`, meaningful only with `injects=Fault.GEOCODER_OUTAGE`; `eval/runner.py` — `_case_environment` patches `tools.geocoding.city_centroid` to force the fallback to a chosen point (the address's own real Census geocode, so only the outage is simulated) rather than the real corpus-wide average U8.2 showed never both diverges and stays clear of a confound. `chicago-geocoder-outage` (U8.2) updated in place: targets now include `REWORK_LIMIT_REACHED`, verdict `ESCALATES`, 2 reworks measured, reproduces identically across three replay runs. Architect's decision, recorded in `open_questions.md`: leave `route_after_critic` unchanged rather than reorder escalation-before-rework. **Coverage: 28 → 29 of 29 kinds** — full coverage, 7/7 baselines, 12/14 verdicts unchanged | 6.1, report |
| Aug 29, 2026 | U8.5 | **Results-table escalation footnote was wrong for a row that didn't exist yet to be wrong about.** `eval/runner.py` — `CaseResult.escalated_above_threshold` used to mean only "escalated on the critical-flag rule"; now that `budget_exhausted` (rework-limit) can also escalate a clearing score, `has_critical`/`budget_exhausted` properties and a second footnote marker (`‡`) say which of the three independent escalation grounds actually fired, rather than one caption naming the wrong one | maintenance |
| Aug 29, 2026 | OQ-17 | **Live model calls are not perfectly deterministic even at temperature 0, recorded as its own open question rather than left in commit prose.** Found while building the OQ-16 case, reproduced organically in a live re-run of the `los-angeles` demo deal mid-session. `docs/open_questions.md` — new OQ-17, no unit assigned, accepted as an inherent property pending a future pass on whether it needs a system response. `agents/scenario_forecast.py` — `TODO(reliability)` at the near-tie disclosure site | maintenance |
| Aug 29, 2026 | OQ-17 | **Diagnosed with a direct experiment rather than left as a guess, and the finding corrected the original hypothesis.** Eight identical live calls to `nvidia/nemotron-3-nano-30b-a3b` at `temperature=0.0` landed on four different OpenRouter backend providers running three different `vllm` versions; pinning the provider removed that layer but a larger one survived — scores for the same candidate still swung 0.05 to 0.95 across repeated identical calls, and `seed` made no measurable difference (temperature 0 has no sampling step for it to control). Neither mitigation adopted — both tested, neither worth the trade (provider-pinning gives up OpenRouter's automatic failover for a benefit that did not materialize). `docs/design/architecture.md` §3 — new "Known limitation" subsection with the full mechanism (temperature vs. logit computation, MoE amplification, OpenRouter's multi-provider routing); `docs/open_questions.md` OQ-17 and `agents/scenario_forecast.py`'s `TODO(reliability)` corrected to match | maintenance |
| Aug 29, 2026 | OQ-17 | **`LlmClient.complete` stops discarding OpenRouter's `provider`/`system_fingerprint` fields.** `tools/llm_cache.py` — `ResponseCache.put` gains optional `provider`/`system_fingerprint` parameters, stored alongside the existing `prompt`/`response` fields (backward-compatible: existing recordings and `.get()`'s read path are untouched). `tools/llm_client.py` — every live call logs which backend answered via `diagnostics.log_note` (the observability seam OQ-17's diagnostic used manually) and passes the same values to the cache write, so any future `--record` run captures backend provenance going forward. Verified end to end: a fresh live call logged `answered by DeepInfra` and the resulting cache file carries `"provider": "DeepInfra"`. Not wired into any decision — logging and persistence only, per the architect's explicit scoping | maintenance |

---

## Aug 29, 2026 — U8.3–U8.4: recorded extractions, and the New York rent-error disclosure

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 29, 2026 | U8.4 | **Per-metro holdout breakdown, closing OQ-3's measurement half.** `tools/model/rent_model.py` — `train()` now carries `df.index` through its existing `train_test_split` call so the holdout rows can be traced back to `state`/`cityname`; `_mae_dollars_by_metro` groups the same residuals already scored (no second fit) by `config.INDEXED_MARKETS`, matched the way `scripts/metro_shortlist_ablation.py` established as correct rather than round-tripped through `kaggle_data.filter_markets`'s `ignore_index` trap. New `TrainingReport.mae_dollars_by_metro`, which travels with the model artifact. Measured: overall $524.03; Chicago $492.14, Los Angeles $530.46, Cleveland $452.49 (all within 1.1x); New York $1,048.38 (2.00x) | **6.1**, report |
| Aug 29, 2026 | U8.4 | **`FlagKind.RENT_ESTIMATE_MARKET_ERROR_ELEVATED`.** `state.py` — new WARN-severity kind, plus `ValuationDetail.subject_metro*` fields carrying the subject's own market figure independent of whether the flag fires. `agents/valuation_rent.py` — `_attach_metro_error`, matched to a market the same way `_attach_benchmark` resolves a Redfin metro, runs off `terms.city`/`state` and the persisted report alone so it does not depend on the county or the estimate later succeeding. `config.RENT_MODEL_METRO_ERROR_RATIO_THRESHOLD = 1.5` (a ratio to the overall holdout MAE rather than a fixed dollar figure, so a retrain does not require re-tuning it), marked `PROVISIONAL` for U8.6 alongside `RENT_COMP_DIVERGENCE_THRESHOLD_PCT` and `COMP_MAX_OUTSIDE_MATCH_SHARE` | **6.1**, report |
| Aug 29, 2026 | U8.4 | **The market's error rendered on every report, not only a flagged one (Q2(a)).** `agents/summarizer.py` — the "Estimated rent" table cell and the "How the rent figure was reached" narrative both append the subject's own metro figure whenever it resolves, e.g. "± $524 overall, ± $1,048 in New York", so a reader in a market that is not elevated sees what not-elevated looks like next to the one that is | **6.1**, report |
| Aug 29, 2026 | U8.4 | **`ny-bedstuy-triplex`, the market-error flag tripped by a real comp set rather than the `staten-island` demo's zero-comp accident.** `eval/data/golden_fixtures.py` — a fixture at a real, Census-geocoded Bedford-Stuyvesant address, chosen because it returns 8 comps rather than the zero the Staten Island case escalates on for an unrelated reason; `eval/cases.py` — declared `reports` per the mechanical rule (both targets warn-severity), measured `escalates` once New York's standing county-anchor warn stacks with the other two, a second tuning signal alongside `chicago-five-bedroom`. `staten-island` picks up the new flag automatically (9 disclosures, was 8) with its verdict unchanged. Rent model retrained to persist the new per-metro field; full batch re-run: **coverage 27 → 28 of 29 kinds**, 7/7 baselines still match, 12/14 predicted verdicts agree | **6.1**, report |
| Aug 29, 2026 | U8.3 | **`Fault.LLM_UNAVAILABLE`, for the one extraction flag no listing or recording can reach.** `eval/cases.py` — a second `Fault` member alongside `GEOCODER_OUTAGE`; `eval/runner.py` patches `tools.llm_client.LlmClient.complete` at the class (there is no instance to patch ahead of `_extract_terms` building its own), one layer above transport, so the Extractor's real `except LlmError` branch still decides. Left unrestricted for the rest of the run rather than scoped to one call — `scenario_forecast`'s later `LlmClient` hits it too, which is the honest behaviour of a model that is actually down and cascades into its own `forecast_unavailable` rather than crashing the run | **6.1**, report |
| Aug 29, 2026 | U8.3 | **`extraction_retry_exhausted` recordings, hand-authored rather than live-recorded.** `scripts/record_retry_exhausted_fixture.py` — `ListingExtraction` has no required fields, so an ordinary model response validates and provoking three organic failures on demand is not reproducible; the script instead reproduces `call_with_schema`'s exact prompt and retry-prompt construction and writes three responses chosen to fail schema validation for three different reasons, verifying each against the real schema before committing it | **6.1**, report |
| Aug 29, 2026 | U8.3 | **Five recorded-extraction cases, closing the five kinds U8.2's census attributed here.** `eval/cases.py` — a listing missing `price` but not `unit_count` (a stated "3-unit", not an inference); a bare "duplex" the model must infer; the hand-authored retry-exhaustion fixture; a declared `LLM_UNAVAILABLE` outage; a real, verified-live address in a corpus-covered city that Census cannot match. **Coverage: 22 → 27 of 28 kinds**; only `rework_limit_reached` remains, for the reason U8.2 already found. 12/13 predicted verdicts agree (the one mismatch is U8.2's, untouched here); all 7 baselines still reproduce the U7.8 table. **One case re-sited mid-build**: the `unresolved_field` listing first ran in Cleveland and reproduced (three re-runs) the same comp-concentration critical objection `cleveland-triplex` already evidences, burying the flag it targets — moved to Los Angeles, where it lands clean. Two case notes guessing Los Angeles County's Small Area FMR coverage would keep `fmr_anchor_county_level` off these rows were measured wrong and corrected in place | **6.1**, report |

---

## Aug 28, 2026 — U8 planning: stale premises corrected before they were built on

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 28, 2026 | U8.2 | **The eval harness's committed recordings actually wired up.** `eval/runner.py` — `_case_environment` points `config.LLM_CACHE_DIR` at `EVAL_RECORDINGS_DIR` and replays for the golden and replay tiers, with `--record` to re-record deliberately; unwound in `finally` alongside the ablation switch, so a mid-batch failure cannot leave later rows running under an earlier case's conditions. **Closes a property that was asserted and unenforced:** `eval/README.md` claimed tiers 1–2 make no model calls, but `agents/scenario_forecast` calls the model twice per Tree-of-Thought level whatever the tier, so every golden row was live, quota-dependent and unreproducible — and `EVAL_RECORDINGS_DIR` had existed since U3 with nothing reading it | **6.1**, report |
| Aug 28, 2026 | U8.2 | **Declared fault injection.** `eval/cases.py` — `Fault` and `EvalCase.injects`; `eval/runner.py` patches `tools.geocoding.geocode_census` to raise, so the failure enters at the seam a real outage does and the pipeline's own branch still chooses between service-outage and unresolvable-address. Declared on the case and printed in the results table's tier column, so a simulated failure can never read as a real one. Exists because the retry path is gated on a live network failure that no fixture and no recording can produce | **6.1**, report |
| Aug 28, 2026 | U8.2 | **Eight engineered cases and their fixtures.** `eval/data/golden_fixtures.py` — complete `DealTerms` across all three inference metros, each naming what is engineered and what is real, with coordinates committed from a Census geocode rather than resolved at run time; `eval/cases.py` — the cases, each verdict derived from its target flag's severity and the shipped escalation rule rather than from an observed run, and two of them predicting `reports` so the agreement figure is two-sided. Closes five of the eleven uncovered kinds. **6 of 7 golden verdicts agree; the disagreement is `chicago-five-bedroom`, whose info-severity target costs nothing yet which escalates at 0.10 because a five-bedroom subject also empties the comp set** — a tuning signal under the triage rule fixed in advance | **6.1**, report |
| Aug 28, 2026 | U8.2 | **Results table gains provenance, rework count, and an above-threshold marker.** `eval/runner.py` — the tier column carries any declared injection; `CaseResult.escalated_above_threshold` marks rows escalated on a critical disclosure while the score alone would have reported, which is the only direct evidence the two escalation grounds are independent and is what U8.6 needs before deciding whether they could be collapsed | **6.1**, report |
| Aug 28, 2026 | U8.2b | **FMR anchor mislabelled as ZIP-resolution wherever HUD publishes no Small Area schedule.** `agents/valuation_rent.py` — `used_msa_fallback` means "a ZIP was asked for and none matched" only for a county that *has* Small Area FMRs; for a county with none HUD returns a flat record and the field is `False`. Reading it alone recorded county-wide anchors as ZIP-resolution in **all five New York counties**, printed a bare "(ZIP)" beside the anchor (`agents/summarizer.py:489`) and **suppressed `FMR_ANCHOR_COUNTY_LEVEL`** — a disclosure missing from every New York report, worth 0.15 of confidence wherever the score is not already floored (the `staten-island` demo is at 0.00 on zero comps, so it gains the sentence rather than a score change). Now requires both halves: `is_safmr` and a matched ZIP. The flag's message was wrong in the other direction and is fixed with it — it asserted HUD publishes no ZIP schedule, which is false of Los Angeles's 474, so the cause now branches while the kind stays one | 4.1, **6.1** |
| Aug 28, 2026 | M3 | **`_resolve_geography`'s `supplied` parameter renamed to `supplied_lat_long`.** `agents/extractor.py` — the old name said where the value came from without saying it was a coordinate pair. `supplied_lat_long` rather than the plainer `lat_long` because the function's whole job is comparing the caller's coordinates against the geocoder's, so the provenance half of the name is load-bearing. Rename only; both renamed branches exercised against a live geocode rather than only compiled | maintenance |
| Aug 28, 2026 | U8.1b | **Geography no longer skipped for callers supplying complete terms.** `agents/planner.py` — a third reason to route through the Extractor, restricted to the first pass so an unresolvable address is not re-planned every rework lap (U7.1b's distinction; caught by the existing suite). `agents/extractor.py` — a geography-only path that skips `_extract_terms`, branching between geocode-and-county and county-alone so supplied coordinates are never re-derived. `state.py` — `is_complete()` and `geography_is_incomplete()` move onto `DealTerms`, since two agents ask each and the alternative was one agent importing another. Closes a report that said HUD publishes no FMR schedule for a county when nobody had looked one up. Found by U8.1's harness; latent in production because `main.py` always supplies raw text | maintenance |
| Aug 28, 2026 | U8.1b | **`FlagKind.LLM_RENT_FALLBACK_USED` removed.** `state.py`, `agents/valuation_rent.py`, `eval/runner.py` — the coverage census identified it as the one kind nothing in the build can raise (cut-list item 3 was taken; the estimator was never built), which is the exact condition `FlagKind` already cites for having retired `COUNTY_FROM_PRINCIPAL_COUNTY`. `RentEstimateSource.LLM_FALLBACK` deliberately kept. 29 kinds → 28, and `UNREACHABLE_BY_ANY_CASE` is now empty | maintenance |
| Aug 28, 2026 | U8.1 | **Eval harness: case schema and batch runner.** `eval/cases.py` — `EvalCase` carrying a verdict *declared before the run*, its tier, and the flag kinds it targets, with `verdict_source` separating predicted claims (which U8.6 may score) from transcribed baselines (which it may not); `__post_init__` rejects a golden fixture without coordinates, since skipping the Extractor also skips the geocoder. `eval/runner.py` — invokes the real compiled graph per case, restores the ablation switch in `finally`, and turns a failing case into a row rather than ending the batch | **6.1**, report |
| Aug 28, 2026 | U8.1 | **Flag coverage census, and the demo deals as eval rows (U10 absorption).** `eval/runner.py` — `set(FlagKind)` minus the kinds the batch raised, with `UNREACHABLE_BY_ANY_CASE` naming kinds no case can raise and why, so an unbuildable path is not counted as an uncovered gap. First run: **17 of 29 kinds covered, 11 uncovered, 1 unreachable**, and all seven demo rows reproduce the U7.8 table exactly. Output committed at `eval/results/results.md` | **6.1**, report |
| Aug 28, 2026 | U8.0 | **ZORI, the independent rent check (#16, OQ-6).** `tools/zori.py` — loader for Zillow's ZIP-level Observed Rent Index, with month columns identified by date-parseability rather than by a negative identity set; `scripts/zori_evidence.py` — measures rent/FMR at the corpus vintage and today over the ZIP-anchored training rows, weighting the FMR denominator by the corpus's own bedroom mix at both ends; `config.py` — vintage month, display minimum, and the 12-month substitution limit. **Finds the ratio moved −11.8%, and decomposes which side moved: FMR +51.9% against market rent +33.5%** | report, 6.1 |
| Aug 28, 2026 | U8.0 | **Anchor comparison: would ZORI be a better anchor than FMR?** `scripts/zori_evidence.py --anchor-comparison` — evidence for §6's new cut-list item 6, anchoring each corpus row at its own listing month rather than a fixed date. Finds ZORI covering 5,662 of 5,686 corpus ZIPs against ZIP-level FMR's ~1,100, producing a *looser* ratio (CV 36.3% vs 33.1%) but *tighter* per-city means (0.172 vs 0.257) — mixed, and reported as mixed | report |
| Aug 28, 2026 | U8.0 | **Ablation docstring corrected to the past tense.** `scripts/retrieval_ablation_llm.py` — its model-selection note asserted in the present tense that `config.MODEL_*` "still holds the unverified placeholders", a premise decision #8 closed on Aug 16. Kept as dated history rather than deleted, and the locally-named models left alone: running two sizes is a property of the ablation, not of the pipeline's model choice | maintenance |

## Aug 27, 2026 — U7.6–U7.8: the confidence mechanism evidenced, the ToT half retired, the unit closed

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 27, 2026 | U7.6 | **Evidence script for decision #6's mechanism.** `scripts/confidence_evidence.py` runs the real compiled graph (real LLM extraction, geocoding, Chroma retrieval, HUD FMR, rent model, ToT forecast — not hand-built agent calls, since the Critic's score depends on every upstream agent's real flags in the real Planner order) across all six demo deals, and reports the per-deal flag/penalty breakdown and which escalation rule fired. The weight-tunability half of decision #6 was already built in U2/U7.4 and already covered by `test_a_single_critical_flag_escalates_regardless_of_score` and `test_confidence_does_not_decay_across_rework_laps`; this closes the "evidenced" half, per Q3 mechanism-only scope | 6.1 |
| Aug 27, 2026 | U7.6 (finding) | **The "two-warn floor" claimed from a three-deal sample does not hold across all six.** No warn-severity flag is common to every demo deal. `fmr_anchor_county_level` fires on the three Los Angeles-county deals because that county has no HUD Small Area FMR, and not on `chicago`, whose county has one — a per-county HUD-coverage fact, not a constant every deal pays. The prior note's claim that `chicago` raised that flag is also contradicted by two independent live runs. `critic.py`'s and `config.py`'s `TODO(U8)` comments corrected in place; `chicago`'s 0.55 now attributed to three deal-specific warns rather than an unconditional pair | 6.1 |
| Aug 27, 2026 | U7.6 (finding) | **No demo deal currently isolates the critical-flag-independent-of-score escalation rule.** Every deal carrying a critical flag already sits below threshold on the score alone — `coord-conflict`, which `decision_log.md` records escalating at exactly 0.60 on the critical rule, now measures 0.05. The rule is real (decision #6, U2's boundary-case fix) and still proven by the hermetic test above; it is simply not exercised live by any current demo deal. Recorded rather than fixed — a question for U8's eval-case design, not this unit's to resolve | 6.1 |
| Aug 27, 2026 | maintenance | **`_consistency_objections()`'s docstring corrected to what U7 actually shipped.** `agents/critic.py` — it still described checks 3 (scenario-base) and 4 (comp-concentration) as "live, TODO(U7)" after Q5 (Aug 24) had already retired both: 3 is unfalsifiable by construction, 4 is already built in the Summarizer. Rewritten to point to the interaction checks (I1–I3, U7.2) and comp attribute drift (E, U7.3, built in `agents/comps_retrieval.py`) that replaced them, and to drop the stale claim that the function returns an empty list — it has not since U7.4 wired it | — |
| Aug 27, 2026 | U7.7 | **Decision #12's Critic half retired on evidence.** `agents/critic.py` never imports `tools/tot.py` — the checks that shipped in U7.2/U7.3 are pure functions over `state.flags`, with no LLM call and no generated candidates, so there is nothing for a beam search to select between. `config.py`'s ToT block now names Scenario/Forecast as its sole consumer; `TOT_TEMPERATURE`, retained solely for the Critic's search, is removed along with the now-inert comment on `LLM_TEMPERATURE`. `tools/tot.py`'s module docstring corrected to match. §7 register (decision #12) and `history/decision_log.md` updated with the retirement reasoning | 6.1 |
| Aug 27, 2026 | U7.7 | **`open_questions.md` OQ-2 and OQ-11 closed; OQ-9 narrowed to the Summarizer.** OQ-2 (which checks, and the search over them) is answered by U7.7's retirement. OQ-11 asked whether the Critic's rent-vs-comp check would need a `query_comps` MCP tool — it never needs one, since the Critic makes no LLM call at all; `mcp_server.py`'s `TODO(U6)` referencing that check corrected to record why it will not be built. OQ-9's Critic half resolved: `config.MODEL_CRITIC` stays untested by construction, not by omission — only the Summarizer's model role remains open, for U9 | 6.1 |
| Aug 27, 2026 | U7.8 | **Three propagation cases for the flags U7 introduced.** `tests/test_flag_propagation.py` — `COMPS_OUTSIDE_MATCH_CRITERIA` reaching the report from a grounded Chicago run (the corpus is dense enough there to return eight comps and not dense enough to return eight *similar* ones, so the set spans 510–2,000 sq ft against a 950 sq ft subject), `CRITIC_INCONSISTENCY` reaching it from the Critic itself, and the negative case where two ordinary warns do not combine. The interaction case is the one worth reviewing: it escalates at **0.70 — above the 0.60 threshold** — on the critical-flag rule alone, over a flag raised in the same pass, which is the path that existed only on paper until U7.4 fixed `has_critical`. Each falsified in the direction it guards. Tests at 60 | 6.1 |
| Aug 27, 2026 | U7.8 | **`REWORK_LIMIT_REACHED` asserted through `assert_reaches_report` rather than a substring match.** `tests/test_flag_propagation.py` — the old assertion matched the flag *kind*, which the Summarizer prints from the enum; the detail is the only place a reader learns how many passes were spent and that the loop stopped deliberately. A report naming the kind and dropping the sentence would have satisfied propagation while losing the reason | 6.1 |
| Aug 27, 2026 | U7.8 | **The demo table is re-derived by script instead of transcribed by hand.** `scripts/confidence_evidence.py` prints the `main.py --deal` table — comps, confidence, disclosures, outcome — from the same live run that produces the confidence evidence, and runs `chicago --no-retrieval` as a seventh invocation so the U4 ablation row comes from the same code path. The U3-era table went stale across two units' worth of new flags because refreshing it meant seven runs and hand-typed rows; it is now one command. `history/decision_log.md` carries the re-measured table, with the U2/U3 tables kept as history rather than overwritten | 6.1 + report |
| Aug 27, 2026 | U7.8 (finding) | **The critical-flag rule *is* exercised by a live run — U7.6's finding was one invocation short.** `chicago --no-retrieval` lands at exactly **0.60 with one critical flag** (`retrieval_disabled` costs 0.40; `0.60 < 0.60` is false), so the score does not escalate it and the critical-flag rule does — the same arithmetic boundary the U2 defect sat on, now landing on the correct side. It is a live case for the rule but not a *deal*: only the ablation flag raises that kind, so U8 still owes an eval case reaching the boundary through a property of the listing. `critic.py`'s `TODO(U8)` corrected in place, and the script now checks the claim rather than printing it | 6.1 |
| Aug 27, 2026 | U7.8 (finding) | **The clean baseline is re-based rather than restored.** No run raises zero flags any more and none will again — `los-angeles` pays `fmr_anchor_county_level` because Los Angeles County publishes no Small Area FMR, which is a fact about HUD rather than about the deal. What the original argument needed is still present: `los-angeles` and `overpriced` report at 0.70 with no critical flag and a full comp set, `chicago` stops at 0.55 on three deal-specific warns, and the three degraded cases stop at 0.05 and below with critical flags naming what failed. The clean row is now *the clean reporting path*, not *the flagless run* | 6.1 + report |
| Aug 27, 2026 | U7.8 | **U7 closed.** §6 unit table row marked complete with what the unit actually built; §7 register — decision **#6** split to `U7 → U8` 🟨 (mechanism landed and evidenced, numbers stay provisional because the demo deals were calibrated to run clean and cannot exercise the range) and decision **#8** marked part-open with its Critic half closed by construction. `history/decision_log.md` gains a U7 record under Orchestration: why the four checks §1 named did not survive contact with the build, the placement rule that replaced them (*a check belongs to the agent that already holds both its inputs*), the redefinition of `critic_rejected`, and the three latent defects that surfaced only once the back edge carried traffic | 6.1 |
| Aug 27, 2026 | U7.8 | **`open_questions.md` reviewed at unit close; nothing deleted, three entries retargeted.** OQ-1 (#6's numbers) and OQ-3 (New York rent error) were labelled U7 and move to U8 with U7's half stated as closed; OQ-14's 6.1 half is discharged — the checkpoint's evidence exists as build artifacts — and its 5.1 half stands. A question retargeted with its reason recorded is not the same as one closed | 6.1 |
| Aug 27, 2026 | maintenance | **`agents/critic.py`'s module docstring updated to the finished agent.** It still opened *"PARTIAL in U2. Cross-agent checks are U7"* and described `_consistency_objections()` as returning an empty list, both true when written and neither true since U7.4. Rewritten to state what the agent is, why it was built in two passes, and — carried at the top of the file because it is the unit's central finding — that the four checks U2 named were replaced by the one judgment no other agent can make: whether a *combination* of disclosures changes what the result means. Two stale docstring lines below it corrected the same way |

## Aug 26, 2026 — U7.3: comp drift, and what it cost the demo baseline

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 26, 2026 | U7.5 | **The report now shows the seller's rent claim beside the rent it derived.** `agents/summarizer.py` `_stated_rent_section` (new), `tests/test_stated_rent_disclosure.py` (new). Until this landed the report rendered `rent_estimate` and never `deal_terms.unit_rents`, so a ~29% disagreement between the two was invisible in the output — the system held both numbers and showed one. Renders per-unit and totalled, names the comparison basis rather than leaving it implicit, discloses when the stated-rent count disagrees with the stated unit count, and renders when the listing states no rents at all. **The caveat is direction-dependent:** the structural offset explains rents *below* the estimate, and printing that explanation over rents *above* it would excuse the one case worth questioning. No flag, no objection, no effect on confidence or routing — a disclosure, not a check, because the gap is a property of the demo calibration rather than of any deal | 6.1 |
| Aug 26, 2026 | U7.5 | **A deliberately mispriced demo listing, so the asking-price disclosure can be seen to work.** `demo_deals.py` — `overpriced`, a real Los Angeles address asking 55% above the metro median with everything else held ordinary, plus a `price_premium_to_basis` field. Every other demo listing has its asking price calibrated *to* the same Redfin median the report reads it against, so that disclosure printed 0% on all of them: a real check this repository's own fixtures could not exercise. Declaring the premium keeps the figure re-derivable from a live source instead of merely invented — `scripts/verify_demo_calibration.py` now checks that a price sits where the deal says it sits, and measures +54.9% against a declared +55%. Renders as **55% above that benchmark**; the run reports normally at 0.70 | 6.1 + report |
| Aug 26, 2026 | U7.5 | **`config.RENT_CLAIM_DIVERGENCE_DISCLOSURE_THRESHOLD`, shipped as `None`.** Gates emphasis on the stated-rent gap. `None` rather than tuned because the measured gap is ~-29% on all three demo listings and that offset is structural — FMR is a 40th-percentile rent, the corpus behind the model rents at ~1.40x it, and #11 calibrated these listings to the anchor itself, so a threshold set against those three numbers would be measuring the fixtures. The branch is exercised by test rather than left for U8 to find unreachable | 6.1 |
| Aug 26, 2026 | U7.5 (finding) | **Q7's floor is the count, not the pair.** A third deal measured: `overpriced` also starts from 0.70 with two warn flags, but they are `fmr_anchor_county_level` and `comps_spatially_concentrated`, and it raises no forecast near-tie at all. So the two constant flags named the day before are not constant; what has held across all three deals is that **two warns land before anything deal-specific is observed**. `TODO(U8)` on `critic.confidence_from_flags` and the `config.py` note corrected to the stronger, more general claim | 6.1 |
| Aug 26, 2026 | maintenance | **U10 folded into U8.** U10 was end-to-end runs across the three metros capturing traces, screenshots and diagrams; U8 is already a batch runner over engineered listings. The demo deals now run through the same harness, so the end-to-end evidence is a harness output rather than a separate pass — one code path instead of two, and no failure mode where the demo evidence and the evaluation evidence are generated differently and disagree. Taken to buy schedule under the Sept 4 freeze, kept because it is the better structure: **the demo becomes a row set in the evaluation, not a separate performance.** §6 unit table, `open_questions.md` OQ-14 | maintenance |
| Aug 26, 2026 | maintenance | **Changelog rule changed: log at commit, not at unit close.** `design/engineering_standards.md` §8, `CLAUDE.md`, and the conventions in this file. The original rule dates to Aug 10, when a unit *was* a change set and the two moments were the same; *How a unit is built* broke that. Measured at the time of the change: U7 had five commits landed across three days and zero rows, recoverable only because the reasoning was still in `tasks/task_list_u7.md`. Closing a unit becomes a review of rows that already exist rather than a reconstruction | maintenance |
| Aug 26, 2026 | maintenance | **Schedule constraint resized; §6 "The hard constraint" revised.** The submission date moved earlier than the plan recorded, and the full frozen week the section asserted was written during first drafting rather than decided. Now nine days of build to a **Sept 4 code freeze**, with the final weekend for the report and video. Streamlit stays in scope — cut-list item 4 is kept rather than taken, because its fallback is available late, which is what makes it safe to hold | maintenance |
| Aug 26, 2026 | U7.3 | **Comp drift measured against the criteria originally searched for.** `agents/comps_retrieval.py` `_outside_match_criteria` (new), `FlagKind.COMPS_OUTSIDE_MATCH_CRITERIA`, `config.COMP_MAX_OUTSIDE_MATCH_SHARE = 0.25`. Built in Retrieval rather than the Critic because that agent already holds both the subject terms and the comps it returned — putting it in the Critic would have repeated the mistake Q1 caught. **Mean drift was measured first and rejected as the statistic**: it separates Los Angeles from Chicago by 4.5pp (+13.3% vs +17.8%) while one set holds a 2,000 sq ft comp against a 950 sq ft subject, because the outliers cancel. Counting comps outside the *unrelaxed* band separates them 0-of-8 against 3-of-8. Critic interaction check I1 repointed onto this flag from `RELAXED_MATCH_CRITERIA`, which records only that criteria were loosened — relaxing a filter permits dissimilar comps without producing them | 6.1 |
| Aug 26, 2026 | U7.3 (finding) | **The clean demo baseline was already gone, and U7 did not do it.** `history/decision_log.md` records Los Angeles at 1.00 confidence with 0 disclosures; measured today it is 0.70 with 4, and Chicago has moved 0.85 → 0.55 and now escalates to human review. `fmr_anchor_county_level` and `forecast_branches_near_tied` arrived with U5 and U6, after that table was last measured in U3. The table is now marked stale in place rather than rewritten, because §6's argument leans on the row it invalidates: *a clean run raising no flags is what establishes that the other rows mean something*. Full re-measurement is U7.8's | 6.1 |
| Aug 26, 2026 | U7.3 (finding) | **`TODO(U8)` on `critic.confidence_from_flags`: the score may be spending its budget before it reads the deal.** Two warn flags were raised on every deal measured. If that holds across the eval batch, 0.30 of the 0.40 between a clean run and the escalation threshold is committed before anything deal-specific is observed — an effective threshold of 0.90 while `config` says 0.60, which is why one ordinary new disclosure was enough to tip Chicago. Recorded, not fixed: the correct repair depends on which reading is true, and only U8's batch tells them apart. Re-pricing warn treats a symptom; an unconditional disclosure is reporting a mechanism rather than a weakness, which is already this file's definition of info severity. `config.py`'s three PROVISIONAL notes retargeted from U7 to U8 to match | 6.1 |

## Aug 25, 2026 — U7.4: two defects the rework cycle was hiding

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 25, 2026 | U7.4 | **Confidence no longer decays across rework laps.** `agents/critic.py` `confidence_from_flags` de-duplicates on `(source_agent, kind, detail)`. `DealState.flags` is append-only by design so raw history stays inspectable, but a rework re-runs every upstream agent and each re-raises what it raised before: a deal carrying two warn flags scored 0.70, then 0.40, then 0.10 without anything about the deal changing. **A deal does not get worse because the pipeline looked at it twice.** It escalated on collapsed confidence before `MAX_REWORKS` was reached, which left the cycle bounded by an arithmetic accident rather than the explicit counter §3 requires. De-duplication is deliberately *not* on `kind` alone — one retrieval pass can raise `RELAXED_MATCH_CRITERIA` twice for two different relaxations, and those are two real observations. `FlagKind.CRITIC_INCONSISTENCY` added to `_DERIVED_KINDS` | 6.1 |
| Aug 25, 2026 | U7.4 | **The Critic's own critical flags could not trigger the Critic's own escalation.** `agents/critic.py` — `has_critical` now reads the flags this pass raises as well as those it inherited. A CRITICAL objection would have set no route and reported as a normal result: the same class of miss as the U2 boundary case the critical-flag rule was written for | 6.1 |
| Aug 25, 2026 | U7.4b | **A rework skipped the Extractor, so the retryable geocode failure was never retried.** `agents/planner.py` `_geocode_is_worth_retrying` (new). `REQUIRED_DEAL_FIELDS` carries no coordinate, so `deal_terms_are_complete` returned true and the rework re-ran only the steps that could not fix the problem — the retry path existed and did nothing. The Planner docstring claimed a rework "only needs comps re-run"; corrected to the system that exists | 6.1 |
| Aug 25, 2026 | U7.4b | **Q6 accepted rather than fixed: nothing distinguishes *raised this pass* from *ever raised*.** A rework that succeeds still re-raises the objection it was sent back to fix, so the report can describe a condition already repaired. Scheduled as §6 cut-list item 2a and `open_questions.md` OQ-15, with `TODO(U8)` at both sites — `critic._interaction_objections` and `planner._geocode_is_worth_retrying`. Accepted because it is wrong text rather than a wrong number, it surfaces only on a rework lap, and every rework lap ends at human review by construction, so the one audience guaranteed to see the stale sentence is also looking at the flag list beside it | 6.1 |

## Aug 24, 2026 — U7.1 and U7.2: the Critic's cross-agent checks

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 26, 2026 | U7.2 | **Cross-agent interaction objections — the Critic's first live output.** `agents/critic.py` `_interaction_objections` (new) and the `Objection` NamedTuple carrying `message`, `severity` and `retryable`, so `planner.route_after_critic` does not have to infer whether a rework can fix anything. Three checks, all gated on `RENT_DIVERGES_FROM_COMPS` and each a pure function of accumulated state: comps outside the match criteria, spatially concentrated comps, and geocode fallback. They exist to say something a sum cannot — *this measurement does not mean what it appears to mean* — where the confidence score can only ever say *more doubt*. `tests/test_critic_interactions.py` (new), hermetic | 6.1 |
| Aug 26, 2026 | U7.1b | **An unreachable geocoder is now disclosed as distinct from an unresolvable address.** `tools/geocoding.py` `GeocodeResult.primary_unavailable`, propagated through `city_centroid`; `FlagKind.GEOCODER_SERVICE_UNAVAILABLE`; `agents/extractor.py` branches on it. One is worth retrying and one is not, and collapsing them meant the rework cycle could not tell. `GeocodeSource` deliberately gains no third member — the fallback used is still the centroid; what changed is why | 6.1 |
| Aug 26, 2026 | maintenance | **Internal vocabulary removed from reader-facing text, and the rule written down.** `design/engineering_standards.md` §8 and `CLAUDE.md`: flag messages, objection text and Summarizer prose reach an investor or a demo audience who cannot resolve a section number, a decision number, a unit label or a `config` constant name. Docstrings and comments are the opposite and should cite precisely. `agents/critic.py` objection text and `agents/summarizer.py` cleaned accordingly; a test walks all 31 flag-set combinations against a banned-pattern regex, and caught a violation introduced in the same change set | maintenance |
| Aug 26, 2026 | maintenance | **"Can it fail? Is it already made? Would it fire on the clean baseline?" added to `design/engineering_standards.md` §8.** U7's planning pass proposed eight consistency checks and measurement killed six: one compared a field against the value it is assigned from three lines earlier, two were already computed and rendered by the Summarizer, and three would have fired on the run that is supposed to raise nothing. The pass had verified that each check's input fields *existed*, which establishes only that a comparison is expressible. The project's own *a check that cannot fail is not a check* standard, moved one step earlier — to the plan rather than the result | maintenance |
| Aug 26, 2026 | U7.1 | **`agents/critic.py`'s module docstring and `TODO(U7)` comments corrected to the system that exists.** They described four consistency checks and a `value_estimate` that decision #15 made permanently `None`. Landed first, with no logic, so the behavioural diffs that follow are read against an accurate file | maintenance |


## Aug 24, 2026 — documentation restructure

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 24, 2026 | maintenance | **`docs/` reorganised into `design/` and `history/`.** The six current-state documents moved to `docs/design/`, `changelog.md` to `docs/history/`. `implementation_plan.md`, `open_questions.md` and `task_list.md` stay at `docs/` root, which is now the load-every-session tier — the path says when to read a file. `implementation_plan.md` did **not** move: 17 source files cite it by path. Code comments repathed in `config.py` (×2) and `tools/county_crosswalk.py` | maintenance |
| Aug 24, 2026 | maintenance | **`implementation_plan.md` split: 1,302 lines → 277.** The §7 decision detail (680 lines), the U2 walking-skeleton retrospective and the U4 ablation record moved to the new `docs/history/decision_log.md`, grouped by system area rather than chronology. §7 becomes a 17-row register naming each decision and linking to its reasoning; §-numbering is unchanged because code comments cite it. One duplicated paragraph in the free-tier item removed | maintenance |
| Aug 24, 2026 | maintenance | **`docs/open_questions.md` (new).** Open questions only, extracted from §7 where 4 of 6 "open items" were already closed. Grouped by system area, each naming the unit that closes it and what closing looks like. Kept short deliberately: it is loaded at the start of every session, so a closed entry is deleted rather than struck through | maintenance |
| Aug 24, 2026 | maintenance | **`docs/task_list.md` (new) + U7 planned.** Per-unit breakdown into commit-sized subsections, written and approved before coding. The U7 pass found two defects in `agents/critic.py`'s `TODO(U7)`: of the four consistency checks it names, one is already built in `agents/valuation_rent.py` and one is dead, since decision #15 made `value_estimate` permanently `None` | 6.1 |
| Aug 24, 2026 | maintenance | **`task_list.md` split into `docs/tasks/`, one file per unit.** `task_list_u7.md`, `maintenance.md` for work not tied to a unit, and a `README.md` holding the conventions and indexing the set. A single file meant every session paid the context cost of every completed unit's plan, which is exactly what a session does not need. A finished unit's file stays rather than being trimmed — it is the record of planned-against-shipped, and it costs nothing once it is not loaded by default | maintenance |
| Aug 24, 2026 | maintenance | **Seven duplicated "Section Links" blocks (63 links) replaced by a header note.** Each document now states that §-numbers and #-numbers resolve to `implementation_plan.md`, and links there. Fixes a real navigation failure: a bare "decision #8 in §7" gave a reader no way to find §7 or to learn what #8 was | maintenance |
| Aug 24, 2026 | maintenance | **`design/engineering_standards.md` §8 — "How a unit is built" added; "one unit per change set" revised.** The old standard optimised for batching; review throughput is the binding constraint and optimises the other way. Now one *logical* change per set, with maintenance landing separately from logic. Revision recorded inline, per the precedent that section already set | maintenance |

## Aug 24, 2026 — U6 complete: the Scenario/Forecast agent

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 24, 2026 | U6 | **Scenario/Forecast agent built for real.** `agents/scenario_forecast.py` (replacing the U2 stub), `tools/tot.py` (new), `graph.py`, `agents/summarizer.py`. Beam search over an enumerated space — 4 framings, then 9 rent/price band pairings — with an LLM evaluator that scores each level in one batched call. Projects 5 years from the modelled rent and the **asking price**, closing decision #15's open question. The Summarizer renders three blocks: the scenarios, how the bands were built, and what the search discarded | 4.1 |
| Aug 24, 2026 | U6 | **Decision #17 — enumerate the hypothesis space rather than sampling it.** `tools/tot.py`, `config.py`. Checkpoint 4.1 specified sampling *b*=5 at `TOT_TEMPERATURE = 0.7`; over a four-point framing space that would make the model invent growth rates, which every rule in this project forbids. The ToT paper's own *propose*-vs-*sample* split points the same way for constrained spaces. Nothing is sampled, the branching factor is data-determined, and **the pipeline is now deterministic end to end** — `TOT_TEMPERATURE` is unused | 4.1 |
| Aug 24, 2026 | U6 (state) | **`Scenario`, `ForecastDetail`, `BranchLedgerEntry`; `scenarios` stops being an untyped dict.** `state.py`, §5. Same result/provenance split `ValuationDetail` established. The ledger carries an `operator.add` reducer because the Critic's search appends to it in U7 and because a rework pass re-runs the node. Three new flag kinds: `FORECAST_UNAVAILABLE`, `RENT_GROWTH_COHORT_SHIFT_SCREENED` (separate from `ANOMALOUS_PERIOD_INCLUDED` because the two series' anomalous windows do not overlap), `FORECAST_BRANCHES_NEAR_TIED` | 4.1 |
| Aug 24, 2026 | U6 | **`AppreciationTier` and `src/enums.py` removed.** `enums.py` (deleted), `tools/redfin_data.py`, `state.py`, `graph.py`, `mcp_server.py`, §5. The enum typed a fallback ladder with one reachable rung — the ZIP tier is closed on sample size, and no all-residential extract exists in this project (checked, including `data/old/`). A type advertising fallbacks the build cannot reach describes a design rather than a system. `appreciation_source` now carries a description of the series, which tells a report reader more than a tier label did | 4.1 |
| Aug 24, 2026 | U6 (defect) | **Three scenarios were resting on three different framings.** `tools/tot.py`, `config.py`. With beam width 3 at every level, the Chicago report showed an optimistic rent of +19.03%/yr directly beneath a basis block stating FY2024 had been screened out — and 19.03% *is* Chicago's FY2024 figure, arriving from a framing that had not screened it. Scenarios built on different treatments are not commensurable and cannot share one provenance statement. Beam width is now per-depth, `TOT_FRAMING_BEAM_WIDTH = 1`. Found by reading the report, not by a test | 4.1 |
| Aug 24, 2026 | U6 (defect) | **The prune threshold emptied the beam on a deal with complete data.** `tools/tot.py`, `config.py`. At a uniform 0.40, an evaluator applying ordinary skepticism scored all four Los Angeles framings below it and the forecast vanished. A threshold asks whether a hypothesis survived contact with the data — a real question about a pairing, a category error about a framing, since framings are enumerated from treatments the evidence supports. Now per-depth, `TOT_FRAMING_PRUNE_THRESHOLD = 0.0`, with the framing prompt made explicitly comparative | 4.1 |
| Aug 24, 2026 | U6 (defect) | **A one-sided deal produced no forecast at all.** `agents/scenario_forecast.py`. Redfin does not cover Staten Island, so every candidate carried a rent band and no price band; the evaluator, told only that rent and price growth are negatively correlated, scored them all below threshold. The deal had a perfectly good rent forecast and the report said nothing — the exact degradation failure this project exists to prevent, caused by describing a two-sided problem to a one-sided deal. The context block now names which sides exist | 4.1 |
| Aug 24, 2026 | U6 (defect) | **Scenario labels contradicted their own numbers.** `agents/scenario_forecast.py`. Ordering survivors by projected rent alone labelled a path "pessimistic" while it projected a price of $1.29M against the "optimistic" path's $390K. Ranking is now the sum of the two growth multiples — a stated labelling convention, not a return model — and the report says that a single column need not fall in label order, since the two quantities are negatively correlated | 4.1 |
| Aug 24, 2026 | U6 (evidence) | **`scripts/forecast_evidence.py` — does the search change the forecast?** Compares the search against a linear baseline (first framing, diagonal pairings): across all four subjects it kept the first-enumerated framing **0 times** and chose an all-diagonal set **0 times**, with Cleveland's base case differing −22.0% on rent and +26.4% on price. States what it would have shown had the search been decoration, and reports each of those outcomes whether or not it holds | 4.1 |
| Aug 24, 2026 | U6 (tests) | **Suite stays at 36 cases and hermetic.** `tests/test_flag_propagation.py`. A second autouse fixture forces the Scenario evaluator down its deterministic fallback — left live, the suite made real model calls and ran 223s instead of 14s. `FakeFmrClient` gained the `area_name`/`is_safmr` fields the rent-growth series reads. The rework-cycle test now stubs the Scenario node too: three agents have pre-empted it by correctly reporting a degradation, so the general rule is recorded there | 6.1 |
| Aug 24, 2026 | U6 | **`MODEL_SCENARIO` and `TOT_MAX_EVIDENCE_CALLS` added; evaluator evidence pulls run through the MCP registry in-process.** `config.py`, `agents/scenario_forecast.py`. The tool menu is built from `mcp_server.server.list_tools()` and dispatches to the same functions an external host reaches, so there is one definition of the tool surface rather than two that can drift — without a subprocess, an async rewrite, or a tracing gap. Decision #13's accounting is unchanged: MCP buys portability and a second consumer, not capability | 4.1 |

## Aug 22, 2026 — comp location precision, U5 complete, U6 rent-growth series

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 22, 2026 | U5 | **ZIP-resolution FMR anchoring, where the data supports it.** `tools/zcta_crosswalk.py` (new), `hud_fmr.get_fmr_zip_table`, `rent_model.anchor_for_row` / `_zip_anchor_tables`, `agents/valuation_rent.py`, `state.ValuationDetail.fmr_resolution`, `FlagKind.FMR_ANCHOR_COUNTY_LEVEL`. The FMR anchor was the only channel through which location entered a rent estimate — `RENT_MODEL_FEATURES` excludes market identifiers by design — and it was county-level. Chicago, the one inference county with published ZIP schedules in the corpus's FY2019 vintage, improved from −30.4% to **−9.9%** divergence. Los Angeles and Cleveland stay county-anchored and now disclose it. Training, comp cross-check and inference share one `anchor_for_row` so they cannot drift onto different denominators | — |
| Aug 22, 2026 | U5 (finding) | **The back-cast looked like a success and was an artifact — caught before it shipped enabled.** SAFMR coverage is younger than the corpus (LA: 474 ZIP schedules for FY2026, **zero** for FY2019), so `_zip_anchor_tables` can reconstruct a ZIP schedule by carrying each ZIP's within-county position back from the current year. Enabled, it converged divergence across all three markets (−10.7% / −14.3% / −13.9%) and was briefly reported as working. It was circular: the same reconstructed schedule normalizes both training rows and comps, so shared error cancels in the comparison and survives in the estimate. The independent test — does the anchor explain rent variation? — says published ZIP cuts ratio dispersion 44.3%→35.9% (−19.1%) while back-cast *raises* it 34.0%→36.2% (+6.6%). `config.RENT_MODEL_BACKCAST_ZIP_FMR = False`, machinery kept, measurement recorded | — |
| Aug 22, 2026 | U5 (defect) | **Inference could anchor at a resolution the model was never fit on.** `rent_model.TrainingReport.zip_anchored_counties` + a gate in `agents/valuation_rent.py`. HUD publishes ZIP schedules for Los Angeles *today* but published none in FY2019, so its training rows are county-anchored — and asking HUD "is there a ZIP schedule?" at inference time would have multiplied a county-relative ratio by a ZIP-level figure. The persisted report now carries the counties actually fit at ZIP resolution and inference consults that instead | — |
| Aug 22, 2026 | U5 (tests) | **Suite to 36 cases.** `tests/test_flag_propagation.py` — `FakeFmrClient` gains a real published ZIP 90026 schedule and a `zip_table_enabled` switch, so both the ZIP path and the non-SAFMR county path are exercised; new case for `fmr_anchor_county_level`. The out-of-bounds refusal fixture was **re-derived** after the retrain shrank the bedrooms coefficient (−0.44 → −0.33) and 5bd/950sqft stopped tripping the bound — a test pinned to a threshold has to move when the threshold's basis moves, and the old one would have kept passing while asserting on a path it no longer reached | 6.1 |
| Aug 22, 2026 | U6 (finding) | **U6's stated premise measured and disproved before the unit was built.** §2, `agents/scenario_forecast.py`, plan §6/§7 decision #16. §1 specifies ToT over "rent-growth/appreciation scenarios, informed by metro-level housing trend data" — i.e. inferring rent growth from Redfin's sale-price series. Using HUD FMR's published FY2017–2026 history as the rent series, rent and price growth are **negatively** correlated: pooled r = −0.309 over 24 metro-years, negative in all three metros independently (−0.135 / −0.226 / −0.530), with price outrunning rent by 8.9 points across 2021–22. A rent forecast on that series would point the wrong way. §2's Problem 2 predicted the divergence from general reasoning; this measures it against the project's own data | 4.1 |
| Aug 22, 2026 | U6 | **Decision #16 — rent and price forecast separately.** Redfin keeps the price side, which is what it measures. Rent growth moves to HUD FMR history: ten fiscal years at county and ZIP resolution through the client already cached, and the only candidate consistent with the anchoring design by construction (the estimate is `ratio × FMR`, so projecting the anchor forward holds the mechanism fixed). Zillow ZORI adopted as the independent check, not the primary series — it also doubles as the only available test of the model's largest unverified assumption. FMR's methodology jumps (Chicago +19.0% FY2024) recorded as the thing U6 must screen for | 4.1 |
| Aug 22, 2026 | docs | **`docs/data_sources.md` — the dataset-to-consumer map that did not exist.** One page per source: vintage, loader, row counts, **the finest geography it carries vs. the geography this system actually uses**, exact consumers, and what it is forbidden from feeding. Written because "which dataset is used in which process" required reading six files, and because §2 — the natural place to look — had drifted from the code. The two geography columns are the point: their gap is where precision is lost by choice, and it surfaced that ZIP-level HUD FMR exists for all three inference counties and is unused | maintenance |
| Aug 22, 2026 | maintenance | **`INDEXED_MARKETS` moved from `scripts/build_comps_index.py` into `config.py`.** The third of three metro scopes, and the only one not in `config.py` — which is why "which markets does the system cover?" had three answers in three places and one of them was invisible. §8 makes `config.py` the only home for a tunable. Behaviour-preserving: the index still resolves to 3,880 listings | maintenance |
| Aug 22, 2026 | maintenance | **§2's training-set size corrected in place.** `data_strategy.md` stated 21,768 rows for the training shortlist; the real figure is 5,717, and 21,768 is a state-level rollup (the six states hold 22,323 between them). The correction already existed in §7 and `config.py`, but not at the point the wrong number appeared — in the section titled *Training vs. Inference*, where anyone would look first. Completeness counts on the same paragraph corrected too (98,923 / 99.4%, not 99,007 / 99.5%) | maintenance |
| Aug 22, 2026 | U5 (defect) | **`--diagnose-divergence` now prints the baseline that makes it correct.** `scripts/valuation_evidence.py` reported only the whole-metro population, which is the comparison that produced the wrong conclusion above. It now prints the radius-matched candidate pool alongside it and separates the neighborhood effect from the ranking effect. Reuses `vector_store.haversine_miles` row-wise rather than adding a second copy of the formula | — |
| Aug 22, 2026 | U5 | **Valuation agent built for real, closing U5.** `agents/valuation_rent.py` — replaces the U2 stub. Predicts the rent-to-FMR ratio from the subject's bed/bath/sqft and anchors it against the current FMR for its own county, emitting `rent_anchored_to_fmr` on every estimate with the ratio, the FMR and the fiscal year in the detail text so a reader can check the arithmetic. Every path that cannot anchor produces no rent figure rather than a fallback one. Measured across five subjects: LA $4,046/mo, Chicago $2,482, Staten Island $3,947 (model alone, no comps), no-geography none | — |
| Aug 22, 2026 | U5 | **Comp cross-check as the agent's Observe step.** `tools/model/rent_model.anchor_comp_rents`, `FlagKind.RENT_DIVERGES_FROM_COMPS`, `config.RENT_COMP_CROSSCHECK_MIN_COMPS` / `RENT_COMP_DIVERGENCE_THRESHOLD_PCT`. Each comp's rent is divided by the FMR for *its own* county and fiscal year, then re-anchored at the subject's current FMR — the training-set normalization run in reverse, so the comparison is vintage- and location-corrected rather than a raw comp mean. Threshold set from measured divergence, not chosen; Chicago straddles it and the config says so | — |
| Aug 22, 2026 | U5 (finding) | **The rent estimate is location-blind below the county — and the first explanation for that was wrong.** `scripts/valuation_evidence.py --diagnose-divergence`, §2. The estimate came in below the comp median on all five subjects. Comparing each comp set to its metro's whole 2BR population indicted retrieval, and that reading reached three code comments before it was checked: comps are drawn from a 2–4 mi radius, so that baseline measures the *neighborhood*. Against the candidate pool at the same radius, ranking moves the median +2.7% / +21.6% / +4.2% while the neighborhood moves it +5.1% / +40.1% / +66.2%. The divergence is the model's, structurally: `RENT_MODEL_FEATURES` excludes any market identifier and the FMR anchor is county-level, so nothing represents sub-metro variation. Flag reworded, threshold kept, limitation recorded | — |
| Aug 22, 2026 | U5 | **Value estimate: decision #15, not produced.** `state.DealState.value_estimate` stays `None`; `ValuationDetail.benchmark_median_sale_price` carries Redfin's metro median instead, rendered under its own heading as a market reference. Redfin's extract exposes zero individual sales (306 rows, one median per metro-period), and the demo asking prices were calibrated to that same median — so emitting it as an estimate would have matched asking price to within $140 for circular reasons. Smoothed over 3 periods on measured volatility (Cleveland swings 6.9% month-over-month, max 14.4%) | — |
| Aug 22, 2026 | U5 | **`ValuationDetail` and two new flag kinds.** `state.py`, `graph.py`, §5. One nested state object for report provenance — holdout error band, FMR fiscal year, cross-check counts, market benchmark — kept separate from the five result fields U6 consumes, so the downstream contract stays stable while the disclosure surface grows. `RENT_ESTIMATE_UNAVAILABLE` (no trained model / an unresolved feature / an out-of-bounds ratio) and `RENT_DIVERGES_FROM_COMPS` | — |
| Aug 22, 2026 | U5 | **Training metrics persisted with the model.** `tools/model/rent_model.save/load`, `scripts/train_rent_model.py`. The joblib now carries the `TrainingReport` and a trained-at timestamp, so the report prints `± $519` beside the estimate and the quoted error is always the one the shipped artifact scored. Retrained; figures reproduce exactly (holdout MAE $518.96, R² 0.173) | — |
| Aug 22, 2026 | U5 | **`fmr_unavailable_for_county` raised from `warn` to `critical`**, and §2's spec corrected. The `warn` was written when the design still had a state/national FMR fallback behind it; that fallback was removed because the only real substitute is an unanchored comp mean, so the flag now means there is no rent figure at all. Severity follows consequence, as `SPARSE_COMPS` already does | — |
| Aug 22, 2026 | maintenance | **Serde allowlist completed — four types were missing.** `graph.state_serde()` — `DealStatus`, `LocationPrecision`, `RentEstimateSource` and `AppreciationTier` were never allowlisted, so every resumed run logged a blocked deserialization. Nothing broke and no output was ever wrong: `StrEnum` members *are* `str`, so the block degraded to the bare string and Pydantic coerced it back. Found only because a U5 test asserted on a resumed run and surfaced the log line. The docstring now states the rule as "every type reachable from `DealState`, enums included" | maintenance |
| Aug 22, 2026 | U5 | **U5 evidence script.** `scripts/valuation_evidence.py` — the four subjects `retrieval_evidence.py` uses, run end to end against live HUD FMR, real county polygons and the real comp index, printing the anchor arithmetic, the cross-check, and the degradation paths. `--diagnose-divergence` reproduces the attribution above | — |
| Aug 22, 2026 | U5 (tests) | **Flag-propagation suite to 35 cases.** `tests/test_flag_propagation.py` — every valuation path: the anchored estimate and its arithmetic, no-county refusal, the bedroom cap, a missing feature, an out-of-bounds ratio, divergence, and its negative case on Los Angeles. `FakeFmrClient` carries **real** published LA schedules for FY2019/2020/2026, because a flat synthetic one would let every comp normalize to itself and the cross-check would test nothing | 6.1 |
| Aug 22, 2026 | U5 (finding) | **The fitted `bedrooms` coefficient is negative** (−0.44), recorded in `config.py` because it looks like a defect and is not: the target is a ratio to FMR, and HUD's schedule climbs with bedroom count faster than real rents do. Consequence is bounded — a high bedroom count on a small footprint drives the predicted ratio under `RENT_MODEL_MIN_RATIO` and the agent refuses the estimate. Found by an accidental test fixture (5 bedrooms in 950 sqft); now pinned by a test | — |
| Aug 22, 2026 | U4 (defect) | **Comp coordinates disclosed as city-area placeholders where they are.** `state.Comp.location_precision`, `FlagKind.COMPS_SPATIALLY_CONCENTRATED`, `build_comps_index.py`, `vector_store.py`, `agents/comps_retrieval.py`, `agents/summarizer.py`, `config.py` (`COMP_MIN_DISTINCT_LOCATIONS`, `COMP_DISTANCE_DECIMALS`). 92% of the corpus carries no street address and those rows sit on placeholder coordinates, so a comp set can satisfy `MIN_QUALIFYING_COMPS` while describing one place — the Cleveland demo returns 8 comps from a single point. The report now states the composition of every comp set beside the existing source-concentration line, and reported distances dropped from 3 decimals (~1.6 m implied precision) to 1. Not used to rank or filter: coverage is 42% in Chicago and 2% in Cleveland, so preferring addressed comps would empty the Cleveland set. §2 | 3.1, 6.1 |
| Aug 22, 2026 | U5 | **`Comp.listed_date` indexed, closing the standing `TODO(U5)`.** `build_comps_index.py` stores `listed_epoch`; `vector_store.py` decodes it. Landed in the same corpus rebuild as `location_precision` — both are metadata changes and a re-index is the cost either way. Lets each comp be normalized against the FMR for its own fiscal year rather than one assumed vintage, which matters because the corpus straddles the FY2019/FY2020 boundary | — |
| Aug 22, 2026 | U5 | **FMR-normalized rent regression.** `tools/rent_model.py`, `scripts/train_rent_model.py`, `config.py` (`TRAINING_METROS` per decision #4, plus the `RENT_MODEL_*` block). Trains on rent ÷ that row's own county-and-fiscal-year FMR across the eight-metro shortlist. Measured: 5,717 shortlist rows, 0 lost to county resolution, 0 to FMR lookup, 29 to ratio bounds; 15 counties, FY2019–2020. Holdout MAE 0.403 ratio / $519 against a mean-ratio baseline of 0.481 / $630, R² 0.173 — reported against that baseline because an MAE alone cannot show the features carry signal (§8) | — |
| Aug 22, 2026 | U5 | **Batch county resolution.** `county_crosswalk.county_fips_for_points` — `gpd.sjoin` fast path, with any unmatched point deferring to the existing per-point function so the nearest-county tolerance and New England exclusion stay single-sourced. Measured 0.03s vs 2.51s for 5,717 rows, zero disagreement between the two paths. The docstring records that the expected justification (no spatial index) was wrong — shapely 2.0 vectorizes `contains`, and the per-point loop actually *wins* below a few hundred points | — |
| Aug 22, 2026 | maintenance | **`hud_fmr.bedroom_field()` made public**, and `get_fmr_for_bedroom` routed through it. The 4-bedroom cap rule now has one home; the rent model needed the mapping without a client call per row, and a second copy would have let the training set be capped differently from the inference path | maintenance |
| Aug 22, 2026 | U4 (defect) | **`Comp` carries its own coordinate, and the place count became exact.** `state.Comp.latitude/longitude`, `vector_store.py`, `agents/comps_retrieval._distinct_locations`, §5. The count had been keyed on rounded distance, which merged two buildings equidistant from the subject in opposite directions and coupled a disclosure threshold to the display setting `COMP_DISTANCE_DECIMALS`. Costs no re-index — `query_comps` already read both values to compute the haversine and discarded them. Also unblocks per-comp FMR normalization (§2), which any comp-derived rent figure requires. No behaviour change on the three measured cases: exact and proxy counts agreed at 3 / 2 / 1 | 3.1, 6.1 |
| Aug 22, 2026 | U5 | **Metro-shortlist ablation — decision #4's rationale tested and upheld.** `scripts/metro_shortlist_ablation.py`. Three tests, since breadth matters differently by target: on held-out inference-trio properties all three variants land within $4 (trio $519.45, five $515.49, eight $517.72 — a wash); on transfer to an *unseen* metro the eight-metro set wins 3/3 (LA $537.87 vs $612.17, Chicago $612.23 vs $635.97, Cleveland $455.85 vs $457.92). Shortlist kept. Separately surfaced: New York predicts at ~$1,065 MAE against ~$518 for the trio under either training set — a disclosure requirement for U7/U8, not a shortlist problem | — |
| Aug 22, 2026 | maintenance | **Defect in the first ablation, corrected before it reached config.** The original run concluded the opposite — trio-only better everywhere — because membership was taken as `.index` off `kaggle_data.filter_markets`, which concatenates with `ignore_index=True`; passing those labels to `df.loc[...]` selected positionally, yielding a "trio" of 2,354 LA rows, 1,233 mixed Cleveland/Cincinnati rows, and no Chicago. The script now masks on the frame's own index and prints trio composition by state each run. No config or model change was made on the bad numbers | maintenance |
| Aug 22, 2026 | U4 (tests) | **Flag-propagation suite to 27 cases.** `tests/test_flag_propagation.py` — spatial concentration reaching the report (Cleveland, 1 location), its negative case (Los Angeles, which clears the threshold exactly and so also pins it), and the retrieval contract for `location_precision` and `listed_date`. The negative case is required by §8: a disclosure that fired on every comp set would tell a reader nothing | 6.1 |
| Aug 22, 2026 | U6 | **The rent-growth series HUD FMR can actually support.** `tools/fmr_history.py` (new), `config.py` forecast block, `src/tools/data/fmr_cohort_panel.json` (committed). `RentGrowthSeries` differences one county's published FMR history for the subject's own bedroom count; `RentGrowthBands` returns worst-year / geometric-mean / best-year with the fiscal year each band names, the IQR beside them, and cohort provenance. Flag-worthy conditions are returned as data so the module never imports `state.py`, matching `redfin_data.py`. `exclude_cohort_shift_years` is the rent-side counterpart of `exclude_anomalous_period` — the fork U6's Tree-of-Thought branches over, not a setting | 4.1 |
| Aug 22, 2026 | U6 (finding) | **The two series' anomalous windows do not overlap, and sharing one constant would have inverted the screen.** `scripts/fmr_history_evidence.py` (new), §2, `data_sources.md`. `config.ANOMALOUS_PERIOD` is calendar 2020–2022, correct for Redfin. On FMR those three fiscal years run 2.73% / 5.22% / 3.09% against a 4.17% baseline — entirely ordinary — while the surge lands in **FY2023 (+5.10pp) and FY2024 (+7.48pp)**, because FMR is administrative and lags: the FY2024 schedules were published Sept 2023 on 2021–22 data. Applying the price window here would have dropped three normal years and kept both distorted ones | 4.1 |
| Aug 22, 2026 | U6 (correction) | **"Methodology jump" withdrawn as an attribution the data cannot support.** `data_sources.md`, plan §7 decision #16. Both documents named Chicago's +19.0% FY2024 a HUD methodology change. Measured across ten distinct HUD FMR areas, **all ten moved that year** (cohort median 11.65%, min 6.5%); Chicago's move is 61% cohort and 7.4pp local, and Los Angeles's +14.5% is mostly cohort. A cohort-wide shift is equally consistent with a methodology change and with the 2021–22 surge reaching an administrative series two years late, and FMR contains no test separating them. The screen now measures **co-movement**, which is observable, and claims nothing about cause | 4.1 |
| Aug 22, 2026 | U6 (defect) | **Band construction rejected by its own smoke test.** `config.py`, `tools/fmr_history.py`. The first implementation paired an arithmetic-mean base with p25/p75 bands, which does not order: Chicago reported base 4.34% against optimistic 4.09% unscreened, and base 1.80% against **pessimistic** 1.92% screened — a base case outside its own band in both directions. Replaced with worst-year / geometric-mean / best-year, coherent by construction and the correct estimator for a compounding projection (the arithmetic mean overstates by 0.07–0.16pp/yr here). A sustained-window construction, the closest analogue to the price side, was rejected separately: screening FY2023–24 breaks the run of consecutive years and drops FY2025–26 out of every qualifying window | 4.1 |
| Aug 22, 2026 | U6 (defect) | **HUD renames FMR areas between fiscal years, and keying on the name split a county's history in two.** `scripts/fmr_history_evidence.py`. Cuyahoga is served as "Cleveland-Elyria, OH MSA" through FY2025 and "Cleveland, OH HUD Metro FMR Area" from FY2026, so a name-keyed panel produced eleven areas from ten counties' worth of data and silently dropped one year-over-year observation out of the cohort. The panel now keys on `entityid` with the latest year's name as canonical. Caught by the script's own section 7 printing a row that should not have existed | 4.1 |
| Aug 22, 2026 | U6 (evidence) | **`scripts/fmr_history_evidence.py` — nine sections, four of which could have gone the other way.** States what each check would have returned had the design been right as written (§8's standard), builds the committed cohort panel, and reproduces every constant the forecast rests on. Section 9 tests estimator durability directly, because min and max are extreme order statistics that can only widen as the series lengthens: recomputed at n=5/6/7, the minimum has not moved in any metro and the maximum moved less than p75 did in Cleveland | 4.1 |

## Aug 19, 2026 — invariant rewording

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 19, 2026 | maintenance | **"No agent calls another agent directly" reworded to "Agents communicate only through shared state."** `docs/architecture.md` §3 (canonical), `engineering_standards.md`, `implementation_plan.md` ×2, `src/graph.py`, `src/agents/critic.py`, plus the private onboarding notes and `claude.md`. The original phrasing paired the prohibition with "routing lives in edges," which read as though routing were the communication channel — it is not. Data moves through `DealState`; a `route_*` function returns only the *name* of the next node and carries no payload. Same invariant, stated as what it actually is | maintenance |

## Aug 18, 2026 — decisions #12–#14, MCP reference server

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 18, 2026 | U6 (partial) | **MCP reference server over the read-only data layer (decision #13).** `mcp_server.py` — four tools: `get_fmr`, `get_growth_bands`, `get_appreciation_history`, `list_available_metros`, all annotated `readOnlyHint`. Wraps `tools/hud_fmr.py` and `tools/redfin_data.py` without modifying either. Two consumers: the U6 ToT evaluator's per-branch evidence pulls, and any MCP host during evaluation and the Week 7 demo. **The pipeline does not import it** — the honest case is portability and a second consumer, not capability, since LangChain `@tool` would give in-process dynamic tool selection with no protocol hop. Recorded that way in the module docstring rather than overstated | 4.1 |
| Aug 18, 2026 | U6 (partial) | **Tool returns carry provenance, not just values.** Every tool returns what a caller would need to disclose the result — `used_msa_fallback` and `is_safmr` on FMR, `includes_anomalous_period` and `optimistic_stretch_in_anomalous_period` on growth bands. Unknown metros and HUD 404s return `available: false` with a reason and the valid alternatives rather than raising, so a missing tool degrades instead of killing the server | 4.1 |
| Aug 18, 2026 | U6 (partial) | **ToT tunables named ahead of the unit that consumes them (decisions #12, #14).** `config.py` — `TOT_BRANCHING_FACTOR`, `TOT_MAX_DEPTH`, `TOT_BEAM_WIDTH`, `TOT_PRUNE_THRESHOLD`, `TOT_TIE_EPSILON`, `TOT_TEMPERATURE`, `TOT_PERSIST_FULL_TREE`, plus `MCP_SERVER_NAME` and `MCP_APPRECIATION_HISTORY_PERIODS`. All provisional and tuned in U8. `TOT_TEMPERATURE = 0.7` is the documented exception to `LLM_TEMPERATURE = 0.0`, whose comment already read `# deterministic by default; ToT overrides` | 4.1 |
| Aug 18, 2026 | maintenance | **`mcp>=2.0` added to `requirements.txt`**, noted as a server-process dependency rather than a runtime dependency of `main.py`. The 2.0 SDK exposes `MCPServer`, not the older `FastMCP` | maintenance |

## Aug 16, 2026 — review follow-ups: diagnostics, response cache, eval scaffolding, decision #8 closed

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 16, 2026 | U3 | **Demo listings calibrated against real market data (decision #11).** `demo_deals.py` — the deals move out of `main.py` into a module carrying each figure *and* its basis: asking price anchored to Redfin's median sale price for Multi-Family (2-4 unit) in that metro, stated rents to HUD FY2026 FMR for the county the listing's own address geocodes to. An import-time check fails if a structured figure and the prose it mirrors diverge. Pipeline behaviour across all five deals is unchanged — the figures were already roughly right; what they lacked was provenance | 2.1, 4.1 |
| Aug 16, 2026 | U3 | **Calibration is verified, not asserted.** `scripts/verify_demo_calibration.py` — re-derives every figure from live Redfin and HUD, exercising the real geocoding and county-resolution path rather than a county written into the fixture. Two deals are expected to report NO BASIS (`staten-island`'s price, since Redfin has no New York coverage; all of `no-geography`) and the script prints those as results rather than skipping them, so a case that quietly acquires a basis is visible | 4.1 |
| Aug 16, 2026 | U3 | **Decision #8 closed: `nvidia/nemotron-3-nano-30b-a3b`, paid variant.** `config.py` — selected on four bake-off passes. Correctness ties across all seven candidates on the paid tier (3/3 valid, 23/23 fields, correct assumptions, no schema retries), so the choice came down to latency and price; this one is perfect on every pass, cheapest of its family, second-fastest overall. Alternatives and their trade-offs recorded in the config comment so the choice stays reviewable | 2.1 |
| Aug 16, 2026 | U3 | **Bake-off no longer confounds availability with capability.** `scripts/extraction_evidence.py` — 429s back off `(5, 15, 30)` and are counted in their own column rather than scoring as extraction failures, which is what made `gemma-4-31b` look incapable when it was queued. Adds `--tier paid\|free`, and forces the response cache **off** for this section, since a replayed response would report the recording's latency as today's | 2.1 |
| Aug 16, 2026 | U3 | **Free vs. paid variants of one model name are not the same deployment.** `gemma-4-26b` scored a spurious assumption on both free passes and neither paid pass, identical prompt — recorded in §7 because it means a free-tier measurement is not evidence about the paid variant, or the reverse | 2.1 |
| Aug 16, 2026 | U3 | **Whole pipeline re-measured on the real Extractor.** All five demo deals plus the retrieval ablation, live end to end (§6). Los Angeles still runs clean at confidence 1.00 with zero disclosures, and Staten Island still finds zero comps — the two properties the real-address change was protecting | 5.1, 2.1 |
| Aug 16, 2026 | maintenance | **Full error detail to stdout, sanitized detail to the report.** `tools/diagnostics.py` + nine call sites — the U3 sanitizer keeps account identifiers out of published reports, which also discarded the metadata a person debugging actually needs. Both audiences now get the right text from the same failure. The largest gain was not the LLM path: `tools/geocoding.py` was swallowing `GeocodingError` entirely, so "Census was down" and "Census found no match" produced identical output despite calling for opposite responses | maintenance |
| Aug 16, 2026 | maintenance | **Model response cache.** `tools/llm_cache.py` + `config.py` — one file per entry, atomic writes, keyed on model/system/prompt/temperature. Three modes, and `replay` is the load-bearing one: a miss raises rather than falling through to a live call, so an evaluation cannot silently re-sample half its cases and report the mixture as one result. Cached at the transport layer so a replayed run reproduces the schema-retry loop faithfully rather than skipping it. Measured: 0.06 ms per hit against 9.9–23 s per live call | 6.1 |
| Aug 16, 2026 | U8 (partial) | **Eval scaffolding and the two-tier case design.** `src/eval/README.md`, `data/`, `data/llm_recordings/`, `results/` — most flag kinds are raised downstream of extraction, so most eval cases supply a complete `DealTerms` and skip the Extractor entirely, which needs no new mechanism because decision #9's pre-flight Planner already routes past extraction when the terms are complete. Only extraction and geography flags need recorded model responses. Harness and cases remain U8 | 6.1 |

---

## Aug 16, 2026 — U3, the real Extractor

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 16, 2026 | U3 | **Extractor built for real.** `agents/extractor.py` — the regex stub is replaced by a schema-validated LLM call through `call_with_schema`, with `ListingExtraction` kept deliberately separate from `DealTerms` so derived geography is never a field the model can fill. The regex is deleted rather than kept as a fallback: a second parser that runs only on failure is one nobody reviews, and it would become the primary path the first time a free-tier model went down | 2.1 |
| Aug 16, 2026 | U3 | **Assumption disclosure, and the prompt rule that makes it mean something.** The model fills an inferred field *and* names it in `assumptions` with a basis, which becomes an `assumed_field_value` flag rendered verbatim. First run over-flagged — "Three-unit building" and "three-family home" were reported as inferences — so the rule was made operational rather than enumerative: a phrase carrying the number states it; only a numberless type word (`duplex`, `triplex`) is an inference. Verified all three cases correct after the change | 2.1, 6.1 |
| Aug 16, 2026 | U3 | **Geocoding wired in, four-tier.** `agents/extractor.py._resolve_geography` — Census parcel match outranks caller-supplied coordinates (the report prints the address, so retrieval must be anchored to it); supplied coordinates are used when the address will not resolve; a city centroid is used when nothing was supplied; nothing resolves to a critical flag. The conflict check runs only against a parcel match, never a centroid, which would otherwise manufacture conflicts out of metro-scale distance. Closes the `TODO(U3)` in `extractor.py` | 2.1 |
| Aug 16, 2026 | U3 | **New flag kind: `supplied_coordinates_conflict`** (critical). `state.py`, `config.py` — a supplied coordinate more than `COORDINATE_CONFLICT_THRESHOLD_MILES` from the address's geocode escalates rather than being resolved, because the system cannot tell which input was meant. Both coordinates are written into the flag so a reviewer sees what was discarded | 6.1 |
| Aug 16, 2026 | U3 | **New flag kind: `extraction_unavailable`** (critical). `state.py` — kept distinct from `extraction_retry_exhausted` because "no model was reached" and "the model never produced valid output" call for different responses from whoever reads the report | 6.1 |
| Aug 16, 2026 | U3 | **Model liveness check.** `tools/llm_client.verify_models_live()` + `main.py` — configured IDs are checked against the live catalogue before the graph is built, so a dead ID fails at launch rather than mid-extraction. Answers the staleness lesson §7 recorded when all four IDs died in six days. `config.py` model constants repointed to a verified-live value | 5.1 |
| Aug 16, 2026 | maintenance | **A rate limit crashed the pipeline; now it degrades.** `tools/llm_client.py` — SDK exceptions are funnelled into `LlmError`, which the Extractor already flags on. Before this, a 429 (`openai.RateLimitError`, not an `LlmError`) propagated out of the node and killed the graph. Found by exhausting the free tier's 50-request daily cap during the bake-off, then verified end to end: the run escalated with a critical flag and a full report instead of a traceback | 6.1 |
| Aug 16, 2026 | maintenance | **Provider error text sanitized before it reaches a report.** `tools/llm_client._transport_failure` — `str(exc)` on an OpenRouter error is the whole JSON body, including the calling account's `user_id`, and flags are rendered verbatim into reports that are portfolio artifacts. Now only the provider's own message and HTTP status are kept | maintenance |
| Aug 16, 2026 | U3 | **Evidence and bake-off in one script.** `scripts/extraction_evidence.py` — three synthetic listings, each engineered to fire a different branch of Loop 1 (clean / term-of-art assumption / missing-and-ambiguous, the last also trapping per-unit-vs-building bedroom counts), plus `--bakeoff` across every free candidate in the live catalogue with schema-validity, retries, wall-clock and hand-checked field accuracy | 2.1 |
| Aug 16, 2026 | U3 | **Flag-propagation suite extended to 24 cases, and made hermetic on purpose.** `tests/test_flag_propagation.py` — an autouse fixture stubs the Extractor's three outbound calls (model, geocoder, county boundary file) for every case, so each U3 flag is now *forced* rather than obtained as a side effect of a listing that happened to omit a price. Ten new cases cover assumption, retry exhaustion, unreachable model, all four geography tiers, the conflict threshold and its negative case, and transport-failure conversion | 6.1 |
| Aug 16, 2026 | U3 | **Demo deals rebuilt on real addresses.** `main.py` — invented street addresses resolve to no parcel and fall back to a city centroid, so every run would have raised a geography flag and §6's clean-LA-run evidence would have been lost. Deal terms remain entirely invented. `--deal no-coords` retired for `--deal no-geography` (an address verified to resolve through neither tier) and `--deal coord-conflict` added; `--coords` now means "check against the address", not "trust" | 5.1, 2.1 |

---

## Aug 15, 2026 — decision #10 follow-on, county resolution rewrite

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 15, 2026 | U3 (partial) | **County crosswalk replaced with a spatial join.** `tools/county_crosswalk.py` — `lookup_county_fips` now takes `(latitude, longitude)` and resolves the county via point-in-polygon geometry against Census's county boundaries (cached locally), replacing the hand-maintained 29-city `(cityname, state)` table. Prompted by reviewing a neighborhood-vs-city naming gap in decision #10 ("Wynwood" vs. "Miami"); the geometric fix turned out to strictly dominate a string-based one — see decision #10's follow-on detail, §7 | 2.1, 4.1 |
| Aug 15, 2026 | U3 (partial) | **Cost measured before committing.** `geopandas` installs in ~3.3s from prebuilt wheels (~31MB); the Census county boundary file loads in ~3.3s and is cached after the first pull. Corrects §2's original "not worth the dependency" judgment, which was never actually tested — see `data_strategy.md`'s Gap 1 | — |
| Aug 15, 2026 | U3 (partial) | **Live verification.** `scripts/verify_county_geometry.py` — reproduces all three inference-trio entityids exactly, resolves Miami-Dade County where the old table had no entry at all, and resolves the old table's two hand-special-cased cases (Richmond VA's independent-city status, Denver's consolidated city-county) correctly with no special code, each cross-checked against a live HUD `listCounties` call | 2.1 |
| Aug 15, 2026 | U3 (partial) | **New England flagged as future work, not solved.** A resolved point in one of the six New England states now returns `None` (HUD prices FMRs by town there, not county, and a county join can't produce that entityid) rather than a wrong value. `TODO(geography)` — same status the old table already carried for the region | — |
| Aug 15, 2026 | maintenance | **Retired flag kind.** `state.py` — `FlagKind.COUNTY_FROM_PRINCIPAL_COUNTY` removed rather than left permanently unraisable; the principal-county approximation it described no longer exists now that county resolution is exact. `requirements.txt` gains `geopandas` | 6.1 |

---

## Aug 11, 2026 — decision #10, geocoding

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 11, 2026 | U3 (partial) | **Geocoding client.** `tools/geocoding.py` — Census Geocoder primary (free, no key, parcel-accurate), corpus-derived city-centroid fallback computed from `kaggle_data.load_clean()` rather than a hand-maintained table. Closes decision #10 (§7); not yet called from `agents/extractor.py` — see that file's `TODO(U3)` | 2.1 |
| Aug 11, 2026 | U3 (partial) | **Live verification.** `scripts/pull_geocode_sample.py` — real calls proving all three paths: a complete address resolves via Census, a city/state-only input falls through to the centroid, and a corpus-uncovered city correctly resolves to neither | 2.1 |
| Aug 11, 2026 | U3 (partial) | **Two new flag kinds.** `state.py` — `COORDINATES_FROM_CITY_CENTROID` (warn) and `GEOCODING_UNAVAILABLE` (critical), added ahead of the code that raises them, same precedent as `COUNTY_FROM_PRINCIPAL_COUNTY` in U1 | 6.1 |
| Aug 11, 2026 | maintenance | **Shared normalization.** `tools/county_crosswalk.py` — `_normalize_city`/`_normalize_state` promoted to public `normalize_city`/`normalize_state` so `tools/geocoding.py`'s centroid fallback folds city names identically to the county crosswalk instead of risking two normalizers drifting apart | — |

---

## Aug 10, 2026 — U2, walking skeleton

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 10, 2026 | U2 | **Graph assembly.** `graph.py` — eight nodes wired on the pre-flight Planner topology, one `Critic → Planner` back edge, two conditional edges, compiled with a checkpointer; conditional-edge targets validated against `nodes.ALL_NODES` at import | 5.1 |
| Aug 10, 2026 | U2 | **Planner and routing.** `agents/planner.py` — writes the execution plan into state and owns both `route_*` functions; `state.py` gains `plan` and `planner_invocations`, the latter making the "at most `1 + rework_count` invocations" invariant assertable | 5.1 |
| Aug 10, 2026 | U2 | **Bounded rework cycle.** The counter increments on Planner re-entry rather than on Critic rejection, so escalation is not miscounted as rework; exhaustion routes to human review instead of raising `recursion_limit` | 5.1, 6.1 |
| Aug 10, 2026 | U2 | **Human-review escalation.** `agents/human_review.py` — `interrupt()` node surfacing confidence, warn/critical flags and unanswered questions; resumes from checkpoint into the Summarizer; `state.py` gains `human_review_note` | 6.1 |
| Aug 10, 2026 | U2 | **Critic confidence and escalation.** `agents/critic.py` — flag-severity aggregation using the `config.py` weights, escalation decision, and `_consistency_objections()` as a live seam for U7's cross-agent checks. Derived flags excluded from their own score to prevent feedback across rework passes | 6.1 |
| Aug 10, 2026 | U2 | **Escalation defect fixed.** A lone critical flag scored exactly at the 0.60 threshold and failed to escalate, so zero-comparable deals reported as ordinary results. `agents/critic.py` now escalates on low confidence **or** any critical flag; `agents/summarizer.py` banner reworded to stop attributing every escalation to the score. Regression test added (plan §6, finding 1) | 6.1 |
| Aug 10, 2026 | U2 | **Summarizer.** `agents/summarizer.py` — real markdown, disclosure-first: every flag rendered in full with its source agent and severity guidance, comp table with `listing_source`, source-concentration disclosure, and absence stated rather than omitted | 2.1, 6.1 |
| Aug 10, 2026 | U2 | **Stubbed specialists.** `agents/extractor.py` (deterministic regex parse, real `unresolved_field` flags and crosswalk county lookup), `agents/valuation_rent.py`, `agents/scenario_forecast.py`. Stubs emit no estimates, so no unanchored figure reaches the report; `state.stub_nodes` carries build status outside the flag stream | 5.1 |
| Aug 10, 2026 | U2 | **Flag-propagation test suite.** `tests/test_flag_propagation.py` (14 cases) and `tests/conftest.py`; `pytest` added to `requirements.txt`. Covers first-node-to-report propagation, multi-agent accumulation, all three reducer annotations incl. the negative case on `comps`, full-text rendering, cycle termination, and interrupt/resume | 6.1 |
| Aug 10, 2026 | U2 | **Generated architecture diagram.** `scripts/export_graph_diagram.py` renders `docs/diagrams/deal_evaluator_graph.{mmd,png}` from the compiled graph and asserts the topology — one back edge, two branching nodes, all declared nodes registered. The hand-drawn placeholder in `lang_graph_onboarding.md` §4 was deleted and replaced with the generated output | 5.1, 7.1 |
| Aug 10, 2026 | U2 | **Entrypoint.** `main.py` — five runnable paths (dense, moderate, thin, no-coordinates, retrieval-off), interrupt/resume handling, and a SQLite checkpointer | 5.1, 7.1 |
| Aug 10, 2026 | U2 | **Observability wiring.** `tools/tracing.py` — env-driven LangSmith project setup that reports on every run whether tracing is actually on, so a run believed captured and silently not captured is not a failure mode | 5.1 |
| Aug 10, 2026 | U2 | **Checkpoint serialization.** `graph.state_serde()` registers the six state types with LangGraph's msgpack allowlist, clearing deprecation warnings that would have blocked the resume path in a future version and switching deserialization to deny-by-default | 6.1, maintenance |
| Aug 10, 2026 | U2 | **HUD cache hardening.** `tools/hud_fmr.py` — `_DiskCache.set()` now writes to a temporary file and renames atomically, so a crash mid-write can no longer truncate the accumulated cache. Clears `TODO(U2)`; residual concurrency limit documented as accepted | maintenance |

---

## Aug 9, 2026 — U4, comps retrieval

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 10, 2026 | U4 | **Hybrid vector store.** `tools/vector_store.py` — Chroma client, embedding function, and `query_comps`: metadata filters for the hard constraints (bedrooms, geography), semantic similarity over description and amenity text for ranking. Radius search implemented as a bounding-box filter Chroma can express, then trimmed to the true circle by haversine | 3.1 |
| Aug 10, 2026 | U4 | **Comp index built.** `scripts/build_comps_index.py` — 3,880 listings across the inference trio plus New York, one document per listing and never chunked. New York is indexed deliberately as the sparse-comps case rather than excluded | 3.1 |
| Aug 10, 2026 | U4 | **Adaptive relaxation loop.** `agents/comps_retrieval.py` — the first real agent: relaxes exactly one criterion per pass, ordered by how much accuracy each concession costs (sqft band, then radius, then bedroom tolerance), each concession naming itself in a flag. Exits on the comp threshold or the iteration cap | 3.1, 2.1 |
| Aug 10, 2026 | U4 | **Sparse-comps degradation path.** The loop exits with a `sparse_comps` flag and whatever it found, rather than presenting a weak comp set as a strong one — critical severity at zero comps, warn otherwise | 3.1, 6.1 |
| Aug 10, 2026 | U4 | **X/Y/Z tuned against measured density.** `config.py` — initial radius raised from 1.0 to 2.0 mi after density curves showed every market but Los Angeles relaxing on the first pass, which made the relaxation flag fire on essentially every run and therefore carry no information. Y=8, Z=4 | 3.1 |
| Aug 10, 2026 | U4 | **Retrieval evidence script.** `scripts/retrieval_evidence.py` — three density cases (LA dense / Chicago moderate / Staten Island thin) plus the `RETRIEVAL_ENABLED` config-flag ablation, producing each of Checkpoint 3.1's required elements as an artifact rather than as prose | 3.1 |
| Aug 10, 2026 | U4 | **Ungrounded-LLM ablation.** `scripts/retrieval_ablation_llm.py` — two free-tier models asked for comps with no corpus access; 0 of 16 returned comps exist in the evidence base, and rent dispersion collapses from CV 19.7% to 3.1%/4.3%. Also the first live exercise of `tools/llm_client.py`, whose retry loop fired for real | 3.1 |
| Aug 10, 2026 | U4 | **Verification honesty fix.** `scripts/retrieval_ablation_llm.py` now reports `id_format_matches_corpus` alongside the corpus lookup, because corpus ids are 10-digit numerals and the models returned `LA001`-style ids — so zero of sixteen could have matched on format alone. The null result was structural, not earned (plan §8) | 3.1, 7.1 |
| Aug 10, 2026 | U4 | **`Comp.listing_source` surfaced.** `state.py` — carries the originating site so a citation points somewhere checkable, and so the 91%-single-aggregator concentration in this corpus is detectable rather than hidden behind a comp count | 3.1, 6.1 |

---

## Aug 8, 2026 — U1, foundation

| Date added | Unit | Work done | Related checkpoint |
| --- | --- | --- | --- |
| Aug 10, 2026 | U1 | **State schema.** `state.py` — Pydantic v2 `DealState`/`DealTerms`/`Comp`/`Flag`, `flags` and `clarifying_questions` carrying `operator.add` reducers so flag loss is structurally impossible; geography fields grouped by provenance (observed / parsed / derived) | 5.1, 6.1 |
| Aug 10, 2026 | U1 | **Closed flag vocabulary.** `state.py` — `FlagKind` and `Severity` as `StrEnum` rather than string constants, so Pydantic rejects an unknown kind at construction and the set is enumerable for U8's coverage assertion | 6.1 |
| Aug 10, 2026 | U1 | **Tunable parameters centralized.** `config.py` — X/Y/Z retrieval loop, confidence threshold, `MAX_REWORKS`, severity weights, Redfin price floor, rent bounds, model IDs and embedding config, each carrying its evidence or a `PROVISIONAL` marker naming the unit that tunes it | 5.1 |
| Aug 10, 2026 | U1 | **Node name constants.** `nodes.py` — node names centralized so a typo in an edge definition fails at import rather than at invoke time | 5.1 |
| Aug 10, 2026 | U1 | **LLM client.** `tools/llm_client.py` — OpenRouter wrapper over the OpenAI-compatible SDK with `call_with_schema`: validates against a Pydantic model and re-prompts with the `ValidationError` text on failure, which is the mechanism the Extractor's clarification loop depends on | 2.1 |
| Aug 10, 2026 | U1 | **Corpus cleaning, single path.** `tools/kaggle_data.py` — cp1252 decoding, 84 exact-duplicate ids dropped, rent bounds applied, and word-boundary city matching that rolls "Cleveland Heights" into Cleveland while rejecting Queensbury-as-Queens and Bronxville-as-Bronx. Centralized so a data-quality decision is made once and cannot drift between the index, the regression, and the evidence scripts | 3.1 |
| Aug 10, 2026 | U1 | **County crosswalk.** `tools/county_crosswalk.py` — 29 `(city, state) → county FIPS` entries, every entityid read from a live HUD `listCounties` response rather than written from memory, plus `verify_against_hud()` to re-run that check. Bridges the corpus's missing county column, which the whole FMR anchoring strategy keys on | 4.1 |
| Aug 10, 2026 | U1 | **Redfin appreciation source.** `tools/redfin_data.py` — metro filtering at load, the $10,000 minimum-price floor, locally computed rolling-3 median, and optimistic/base/pessimistic growth bands returned as data with `includes_anomalous_period` and `tier` for the agent to convert into flags | 4.1 |
| Aug 10, 2026 | U1 | **Anomalous-period discontinuity fixed.** `tools/redfin_data.py` — excluding 2020–2022 leaves a gap that a naive `.rolling()` splices across, silently averaging 2019 onto 2023 and reporting it as consecutive months. The sustained-stretch calculation now segments on month adjacency first; the fix moved Chicago's excluded pessimistic band from −1.92% to −1.56% | 4.1 |
| Aug 10, 2026 | §9 | **HUD FMR API client.** `tools/hud_fmr.py` — auth, on-disk cache, client-side 60/min throttle, and normalization of the flat and Small Area FMR response shapes into one return shape so callers never branch on it. Bedroom lookups cap at 4BR and report it rather than raising | 4.1 |
| Aug 10, 2026 | §9 | **Live FMR smoke test.** `scripts/pull_fmr_sample.py` — real (not mocked) pulls for the candidate counties across two fiscal years, confirming both response shapes are genuinely exercised and that a repeat call hits the cache without a second HTTP request | 4.1 |
| Aug 10, 2026 | §2 | **Metro selection evidence.** `scripts/verify_metro_selection.py` — reproduces the density check across both datasets that overturned the original New York / Chicago / Philadelphia hypothesis and settled the inference trio on Chicago, Los Angeles, Cleveland | 3.1, 4.1 |
