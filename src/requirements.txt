requests>=2.31
python-dotenv>=1.0
pandas>=2.2

# County resolution (tools/county_crosswalk.py): point-in-polygon join against Census
# county boundaries. Pulls in shapely/pyproj/pyogrio; all ship as prebuilt wheels on
# macOS/Linux, no system GDAL install needed (measured install: ~3s, ~31MB, ~103MB on
# disk — cheaper than this project's original docstring assumed before it was tested).
geopandas>=1.1

# Orchestration
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
langsmith>=0.1

# State schema / validation
pydantic>=2.7

# LLM access (OpenRouter is OpenAI-API-compatible)
openai>=1.40

# Retrieval
chromadb>=0.5
sentence-transformers>=3.0

# Modeling
numpy>=1.26
scikit-learn>=1.5

# Demo UI (local only)
streamlit>=1.37

# Testing. Scoped deliberately (§8): the flag-propagation suite and the U8 eval
# harness, not broad unit coverage.
pytest>=8.0
